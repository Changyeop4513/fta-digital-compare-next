# FTA 디지털협정 조항 비교 서비스 기획서 (PRD)

- 작성자: 이창엽
- 작성일: 2026-07-22
- 최종 개정: 2026-08-01 (**v4.0 — 5차 사이클: 미발효(서명·타결) 협정 5개 확장, 29개 × 7주제 = 203칸**)

## 개정 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| v1.0 | 2026-07-22 | 최초 기획 |
| v1.1 | 2026-07-24 | 1차 사이클 완료 후 as-built 반영 (구현·검증·배포 결과) |
| **v2.0** | **2026-07-25** | **2차 사이클 기획.** 주제 3개는 그대로 두고, **대상 협정을 4개 → 한국이 체결한 발효 협정 전체(당시 25개로 산정)로 확대**한다. 이에 따라 ① 데이터 확보 방식을 **포털 직접 수집**으로 전환(9-2) ② **협정 확대 규모용 선택 UI** 신설(5절 기능 2·3) ③ **각주 처리 규칙** 명문화(9-4) ④ **한-싱가포르 FTA와 싱가포르 DPA 분리**(9-1) |
| **v2.1** | **2026-07-25** | **2차 사이클 구현 결과(as-built) 반영.** ① 대상 협정 **25개 → 24개 확정**(한-싱가포르 FTA 제외, 9-1) ② 데이터 **44건 수록 완료**(주제별 15·15·14 / 수록 19개 + 3주제 모두 없음 5개) ③ **협정 표기 규칙 신설**(9-5) — 화면 표시는 **상대국명만**, 비교 뷰 칸 머리는 **정식 명칭** ④ **표시 순서 = 발효일 오름차순** 확정 ⑤ 조항 유무 **배지는 협정 이름만** 표기로 확정(5절 기능 3) ⑥ **간접 규정 엄격 판정 기준** 명문화(9-4) |
| **v3.0** | **2026-07-25** | **3차 사이클 — 비교 결과 내보내기(기능 4) 추가·구현 완료.** 브라우저 인쇄 전용 스타일로 **A4 가로 나란히 인쇄 / PDF 저장**. PDF 라이브러리는 도입하지 않음(정적 앱·CSP·한글 폰트 부담). 접힌 원문은 인쇄 시 **전부 펼치고**, 조항 **출처를 인쇄물에만** 표시하며, 머리글에 **주제·비교 협정·검색어·출력 시점**을 남긴다. 협정·주제·데이터는 변경 없음(24개 · 44건). |
| **v3.1** | **2026-07-25** | **주제 확대 — 3개 → 5개.** DEPA 조문 구조를 기준으로 **전자송장(`e_invoicing`)·전자지급(`electronic_payment`)** 신설(선정 근거는 5절). 24개 협정 × 신규 2주제 = **48칸 전부 판정**, **6건 수록**(DEPA·싱가포르 DPA·UAE 각 2건) · 전수 원문 대조 통과. 전체 **24 × 5 = 120칸**, 수록 **50건**. 함께 **영국 특송화물 재확인 항목 해소**([CHECK.md](CHECK.md) v3.0). |
| **v3.2** | **2026-07-29** | **주제 7개 완성.** **정부조달(`government_procurement`)·과세 예외(`taxation_exception`)** 추가. 범위는 사용자 결정 — 정부조달은 **디지털 경제 관련 조항만**(전통 FTA의 정부조달 장 전체는 제외), 과세 예외는 **24개 협정 전부**. **24 × 7 = 168칸 전부 판정**, 조문 **72건** 수록. 과세 예외는 20개 협정 21건(튀르키예는 기본협정·투자협정 2조문). 영국 과세 예외는 국문본 확보 불가로 **"번역 없음"** 유지. |
| **v4.0** | **2026-08-01** | **5차 사이클 — 미발효(서명·타결) 협정 5개 확장.** 대상을 발효 24개에서 **29개**(+ 에콰도르 SECA·GCC FTA·조지아 CEPA·말레이시아 FTA·**EU DTA**)로 확대. **29 × 7 = 203칸 전부 판정** · 조문 **99건**(신규 27건, 신규분 54칸 자동 감사 전문 일치). 미발효 협정은 ① 선택 패널·배지에서 「미발효(서명·타결) — 문안 변동 가능」 그룹으로 분리 ② 칸 머리 정식 명칭에 "(미발효)" ③ 기준일 라벨에 타결/서명 상태 명시 — 발효 협정의 확정 문안과 오해되지 않게 한다(사용자 결정: 라벨+기준일 구분 방식). 발효 시 처리 절차를 constants.js 주석과 CLAUDE.md에 규칙화. 세르비아 CEPA는 협정문 미확보로 제외. 한 칸 2조문 사례 추가: 에콰도르 특송화물(제4.7조 + 제12.11조). |
| **v3.5** | **2026-07-29** | **수록 조문 전수 기계 재검증 완료.** 72건 × 영·한 = **144칸 전부**를 로컬 협정문 PDF와 기계 대조했다. ① 텍스트 레이어와 **순서 유지 조각 대조**로 자동 감사(130칸 일치) ② 레이어로 판정 못 한 칸만 **배율 3종 OCR**(10칸)·**포털 장별 PDF 대조**(5칸)·**OCR 3중 검산**(4칸, 영국 무관세 국문 신규 포함)으로 확정. 정정은 **아포스트로피 모양 3곳**(미국 특송·싱DPA 2건, `'`→`’`)뿐 — 캄보디아 `Minister's`는 원문 활자가 곧은 모양이라 유지(**원문이 기준, 통일이 기준이 아님**). 상세는 [CHECK.md](CHECK.md) v4.3. |
| **v3.4** | **2026-07-29** | **사용자 제공분 3건 기계 재검증.** 텍스트 추출이 안 돼 사용자가 눈으로 확인해 제공했던 **칠레·인도·EU**를 OCR로 재검증했다. **EU는 기존 값이 정확**, **칠레 2곳**(따옴표 모양·띄어쓰기)과 **인도 3곳**(쉼표)은 오류가 발견돼 정정. 이로써 **72건 전부 기계 검증분**이 되었다. 새 규칙 3가지를 명문화 — ① **JBIG2 스캔 PDF는 Windows 내장 렌더러**로 그린다(pdfjs는 빈 쪽) ② **텍스트가 읽히는 PDF도 문장부호는 못 믿는다**(뒤 낱말 앞으로 밀려 나오므로 뷰어 복사본도 틀리다 — 문장부호·띄어쓰기는 OCR 근거) ③ **웹 공포본은 PDF 원문의 대체물이 아니다.** 조문 수·판정 범위 변동 없음(24개 · 7주제 · 72건). |
| **v3.3** | **2026-07-29** | **국문 72/72 완비.** 마지막 미확보였던 **영국 과세 예외 한국어본**을, 폰트 인코딩이 깨진 국문 PDF의 해당 쪽을 **이미지로 렌더링해 OCR로 복원**해 수록했다. OCR 결과를 그대로 쓰지 않고 ① **배율 3종 OCR 대조** ② **깨진 텍스트 레이어가 보존한 글자 수·공백 구조와 대조**(다수결로 확정, 미해결 글자 0) ③ **승계 원조문 한-EU 제15.7조와 대조**(낱말 일치율 93.2%, 차이는 전부 당사자 표기) — 셋을 모두 통과한 것만 넣었다. 이에 따라 **"번역 없음" 0건**. 재사용 가능한 절차를 [CLAUDE.md](CLAUDE.md)「깨진 국문 PDF에서 원문을 복원할 때」에 규칙으로 명문화. 협정·주제·조문 수는 변동 없음(24개 · 7주제 · 72건). |

> **1차 사이클 산출물(운영 중, 별개):** https://fta-digital-compare.vercel.app
> **2차 사이클(이 문서):** 저장소 `Changyeop4513/fta-digital-compare-next` · 사이트 https://fta-digital-compare-next.vercel.app

---

## 1. 추진 배경 (문제 정의)
_누가, 어떤 업무에서, 어떤 불편을 겪는가_

재정경제부 내부 실무자를 우선 사용자로 하며, 통상 실무자·연구자(로펌, 연구기관, 무역 관련 대학원생 등)까지 확대해 사용한다. 이들은 한국이 체결한 여러 FTA의 디지털 통상 조항을 검토한다.

1차 사이클에서 **협정 4개**(싱가포르 DPA·RCEP·영국·미국)를 수록해 "한 화면에서 원문 그대로 비교"라는 핵심 가치는 검증했다. 그러나 실무에서 검토 대상이 되는 협정은 4개가 아니라 **한국이 체결한 발효 협정 전체**다. 4개 밖의 협정을 확인하려면 결국 협정문 PDF를 직접 열어야 하므로, 1차에서 없애려 했던 수작업이 그대로 남는다.

특히 기능 3(**조항 보유 협정 자동 찾기**)은 대상이 전체일 때 비로소 제 값을 한다. "어느 협정에 전자적 전송물 무관세 조항이 있는가"라는 질문에 지금은 4개 안에서만 답할 수 있어 사실상 답이 되지 못한다.

---

## 2. 현행 방식과 한계
_1차 산출물의 무엇이 아쉬운가_

- **커버리지 한계 (핵심):** 대상 협정 24개 중 **4개(17%)만** 수록. 나머지 20개는 여전히 수작업 대조가 필요하다. 〔v2.1: 대상 수를 24개로 확정해 갱신〕
- **조항 유무 판정의 불완전성:** 화면의 "없음"이 *협정에 조항이 없다*는 뜻인지 *아직 수록하지 않았다*는 뜻인지 구분되지 않는다. 대상이 전체가 되어야 이 모호함이 사라진다.
- **데이터 확보 병목:** 1차에서는 사용자가 협정문 PDF를 직접 첨부해야 했다. 20개분을 같은 방식으로 처리하는 것은 현실적이지 않다.
- **협정 명칭 혼선:** 1차의 `한-싱가포르`는 실제로 **싱가포르 DPA(디지털동반자협정)** 기준으로 수록돼 있다. 포털에는 **한-싱가포르 FTA**와 **싱가포르 DPA**가 별개 협정으로 존재하므로 확대 시 반드시 분리해야 한다.
- **UI 규모 한계:** 협정 선택 체크박스와 조항 유무 배지가 **한 줄 나열** 구조다. 4개에는 맞지만 24개에서는 상단 고정 바가 화면을 덮어 쓸 수 없게 된다.
- **표기 일관성 부재 〔v2.1 추가〕:** 1차는 `한-미`·`한-중`·`한-싱 DPA`처럼 임의로 줄인 약칭을 썼다. 협정이 24개가 되면 이런 약칭이 뒤섞여 같은 협정을 다르게 부르게 되므로 **표기 규칙을 정해야 한다**(→ 9-5).

---

## 3. 목표와 기대효과 (성공 기준)
_측정 가능한 숫자로_

- **커버리지:** 대상 협정 **24개 × 주제 수록률 100%**. 협정에 해당 조항이 없으면 "해당 조항 없음"으로 명시하며, **미수록 상태로 남기지 않는다.** → **달성 〔v3.2〕**: **168개 칸(24×7) 전부 판정**, 조문 **72건** 수록. (2차 사이클 시점 기준은 72칸(24×3) · 44건이었다.)
- **원문 무결성:** 수록한 전 조항의 영문·한국어가 **포털 원문과 100% 일치**(요약·수정·의역 없음). 대조 검증 미통과 **0건**. → **달성**([CHECK.md](CHECK.md))
- **탐색 성능:** 24개 협정 중 원하는 협정을 **3회 이내의 조작**으로 찾아 선택할 수 있다(이름 검색 또는 "있음만 보기" 활용).
- **비교 가독성 유지:** 협정이 24개로 늘어도 **동시 비교는 최대 4개**를 지켜 좌우 스크롤 부담을 만들지 않는다.
- **표기 일관성 〔v2.1〕:** 한 협정을 부르는 이름이 **화면 어디서나 같다.** 좁은 자리는 상대국명, 넓은 자리는 정식 명칭 — 두 가지만 쓰고 임의 약칭을 만들지 않는다(9-5).
- **무회귀:** 1차에서 통과한 기능·예외 처리·보안 헤더가 **그대로 동작**한다.
- **처리 시간:** 주제 1개에 대해 협정 3~4개 비교를 **3분 이내**(1차 기준 유지).

---

## 4. 사용자와 이용 흐름

- **주 사용자:** 재정경제부 내부 실무자 (확장: 통상 실무자·연구자)

**이용 흐름 (협정 24개 기준)**

- **방식 1 (조항 있는 협정 찾기 → 비교):** 앱 접속 → 주제 1개 선택 → **24개 협정의 조항 유무가 한눈에 표시됨** → "있음" 협정 중 2~4개 선택 → 좌우 나란히 비교
- **방식 2 (특정 협정 확인):** 앱 접속 → 주제 선택 → **협정 이름 검색**(예: "베트") → 해당 협정 확인 → 필요하면 다른 협정을 추가해 비교
- **(공통)** 키워드 검색으로 원문 내 단어 찾기·하이라이트 → 주제를 바꿔 같은 협정 조합으로 계속 비교

> 24개로 늘면서 **"조항을 가진 협정만 추려 보는 것"** 이 가장 흔한 시작점이 된다. 이용 흐름의 무게중심이 기능 3으로 옮겨간다.

---

## 5. 주요 기능 (must / nice)

### [must] 1) 조항별 협정 비교 뷰 — *1차 규칙 유지, 변경 없음*
- 설명: 주제 1개를 고르면 그 조항을 여러 협정에 대해 좌우로 나란히 표로 보여준다. 각 칸에 영문 원문과 한국어본을 함께 표시한다.
- 규칙:
  - 한 화면에 **최대 4개 협정**까지만 나란히 표시. 1개만 선택하면 한 칸만.
  - 각 칸은 **영문 원문 + 한국어본을 한 쌍**으로. 한쪽만 있으면 "번역 없음" 명시.
  - 한 협정에 같은 주제 조항이 여러 개면 **누락 없이 모두** 조항 번호 순으로.
  - 좌우 배치 순서는 **사용자가 고른 순서**를 따른다.
  - 각 조항에 **기준 시점(`baseline_date`) 라벨** 표시.
  - 원문이 길면 접어 두고 "더보기"로 펼침. **검색어가 포함된 카드는 자동으로 펼침.**
  - 원문 텍스트는 절대 요약·수정하지 않는다.

### [must] 2) 주제·협정 선택 필터 + 키워드 검색 — *협정 선택 UI 개편*
- 설명: 상단에서 주제(3개 중 1개)와 협정들을 골라 비교 뷰를 구성하고, 키워드로 원문 안에서 단어를 찾는다.
- **〔v2.0 신규〕 협정 선택 UI를 24개 규모에 맞게 개편한다.** 요구사항:
  - **협정 이름 검색** — 이름 일부를 입력해 좁힐 수 있어야 한다(예: "베트" → 베트남).
  - **"조항 있는 협정만 보기"** — 현재 주제 기준으로 "있음"인 협정만 추려 보는 수단을 제공한다. **기본 켜짐.**
  - **선택한 협정은 항상 보이게** — 목록을 좁히거나 스크롤해도 이미 고른 협정을 놓치지 않아야 한다.
  - **컨트롤 바가 화면을 덮지 않아야 한다** — 24개를 한 줄에 나열하지 않는다. (구체 방식은 DESIGN에서 결정)
- **〔v2.1 확정〕 as-built:** 컨트롤 바에는 **고른 협정만 칩**으로 두고, 추가는 **`＋ 협정 추가` 패널**(이름 검색 · "있음만 보기") 또는 **조항 유무 배지 클릭** 두 경로로 한다. 칩의 `✕`로 해제하며, 칩의 좌→우 순서가 비교 뷰의 좌우 순서다.
- 규칙 (유지):
  - 주제는 **7개로 고정 〔v3.2〕**. 표기는 `Customs Duties(관세)` / `Express shipments(특송화물)` / `Paperless trading(종이 없는 무역)` / `E-invoicing(전자송장)` / `Electronic payments(전자지급)` / `Government procurement(정부조달)` / `Taxation exception(과세 예외)`. 순서는 협정문 흐름을 따른다(관세 → 통관 → 무역서류 → 결제 → 조달 → 예외).
  - 한 번에 1개 주제만. **주제를 바꿔도 선택한 협정 목록은 유지.**
  - 원문 검색은 **현재 선택한 주제·협정 범위 안**에서만. 부분 일치, 영문 대소문자 무시.
  - 찾은 단어는 노란 형광펜으로 **하이라이트만** — AI 요약·해석 없음.

> **주의:** 이번에 추가되는 **협정 이름 검색**과 기존 **원문 키워드 검색**은 **서로 다른 기능**이다. 화면에서 두 입력창이 헷갈리지 않도록 구분해야 한다(DESIGN에서 처리).

### [must] 3) 주제별 조항 보유 협정 자동 찾기 — *24개 규모 대응*
- 설명: 주제를 고르면 대상 협정 각각에 그 조항이 있는지(있음/없음)를 자동 표시한다.
- 규칙:
  - 판정은 데이터의 `topic` 태그 기준. 하나라도 있으면 "있음", 없으면 "없음". **임의 추론 금지.**
  - 화면 라벨은 **"협정별 조항 유무"**.
  - **〔v2.0 신규〕 24개를 한눈에 파악할 수 있는 배치**로 바꾼다. **보유 개수 요약**(예: `「Customs Duties(관세)」 — 24개 중 15개 보유`)을 함께 제공한다.
  - **〔v2.0 신규〕 "있음" 표시에서 바로 비교 대상으로 추가**할 수 있게 해 기능 3 → 기능 1 연결을 강화한다.
  - **〔v2.1 확정〕 배지에는 협정 이름만 적는다.** `있음 ＋` 같은 꼬리표를 붙이지 않는다 — 있음/없음은 **요약 문구**와 **"조항 없는 협정 N개 보기" 접기 영역**으로 이미 갈라지고, 선택 여부는 **색상**(선택 시 네이비)으로 드러난다. 상태 설명은 `title`(마우스 오버)과 `aria-pressed`(스크린리더)로 제공한다.
  - **〔v2.1 확정〕 "없음" 협정은 접어 두되 반드시 펼쳐 볼 수 있게** 한다(숨기는 것이 아니라 접는 것). "없음"도 실무에서 필요한 정보다.
  - **〔v2.1 확정〕 표시 순서는 발효일 오름차순.** 오래된 협정부터 최신 협정 순으로 고정한다(가나다순·보유순으로 바꾸지 않는다) — 협정 간 조항 변화를 시간 흐름으로 읽을 수 있어야 하기 때문이다.

### [must] 4) 비교 결과 내보내기 — *3차 신규, 구현 완료*

- 설명: 현재 화면의 비교 결과를 **인쇄하거나 PDF로 저장**한다. 회의·보고에 그대로 돌려 쓸 수 있는 형태여야 한다.
- **방식: 브라우저 인쇄 전용 스타일 + `window.print()`.** PDF 라이브러리를 **추가하지 않는다** — 이 앱은 외부 통신이 없는 정적 앱이고 CSP가 `'self'`로 묶여 있으며, 한글 폰트를 내장하면 번들이 커지고 원문 렌더링이 깨질 위험이 있다. 종이 인쇄와 "PDF로 저장"은 브라우저에서 같은 경로다.
- 규칙:
  - **레이아웃은 A4 가로**, 협정은 화면처럼 **좌우로 나란히**. "한눈에 비교"라는 핵심 가치를 종이에서도 지킨다. (실측: 협정 4개까지 A4 가로 인쇄 폭에 들어간다)
  - **접힌 원문은 인쇄에서 전부 펼친다.** 종이에서는 "더보기"를 누를 수 없어 접힌 채 인쇄되면 **원문이 잘려 나가고, 이는 원문 무결성 위반**이다.
  - **조항 출처(`source`)를 인쇄물에 표시한다.** 인용 근거가 없으면 실무에서 쓸 수 없다. **화면 표시는 여전히 범위 밖**이다(아래 nice 항목).
  - **인쇄물 머리글에 조건을 남긴다** — 주제 · 비교 협정(정식 명칭) · 원문 검색어 · 출력 시점. 종이에는 컨트롤 바가 없어 무엇을 기준으로 뽑은 표인지 알 수 없다.
  - **원문은 여기서도 손대지 않는다.** 요약·발췌 없이 전문을 그대로 싣는다.
  - 인쇄할 내용이 없으면(협정 0개 · 검색 0건) 버튼을 **잠근다.**

### [must] 5) 주제 범위 — 7개 확정 〔v3.2〕

주제를 늘릴 때는 **어디까지를 그 주제로 볼지 먼저 정한다.** 정하지 않으면 협정마다 다른 기준으로 수록돼 화면의 "없음"을 믿을 수 없게 된다.

| 주제 | 범위 결정 | 근거 |
|------|----------|------|
| **정부조달** | **디지털 경제와 관련된 조항만.** 전통 FTA의 정부조달 **장(章) 전체는 대상 아님** | 장을 통째로 담으면 한 칸에 조문이 15~20개가 되어 좌우 비교가 무의미해진다. 이 앱은 「디지털 통상 조항 비교」가 목적이다 |
| **과세 예외** | **24개 협정 전부.** 협정 전체에 걸리는 일반 예외라 디지털 통상 조항은 아니지만 수록한다 | 재정경제부 소관이라 실무 가치가 크고, 협정당 조문이 하나라 좌우 비교가 깔끔하다 |

- **정부조달 판정 시 유의:** 다른 협정의 "정부조달" 언급은 대부분 **적용 제외**(“이 장은 정부조달에 적용되지 않는다”)이거나 **상호참조**다. 실질 의무가 아니므로 엄격 기준상 **"없음"** 이다.
- **과세 예외 판정 시 유의:** 조문 제목이 `과세` · `과세조치` · `과세 예외`로 협정마다 다르다. 또 **한 협정에 조문이 둘일 수 있다**(튀르키예 = 기본협정 제8.1조 + 투자협정 제1.18조). 이때는 조항 번호 순으로 **모두** 나열한다.

### 9-6. 〔v4.0 갱신〕 주제별 수록 현황 (29개 협정 × 7개 주제 = 203칸)

| 주제 | 보유 | 없음 | 조문 수 |
|------|:---:|:---:|:---:|
| Customs Duties(관세) | 20 | 9 | 20 |
| Express shipments(특송화물) | 19 | 10 | **20** |
| Paperless trading(종이 없는 무역) | 18 | 11 | 18 |
| E-invoicing(전자송장) | 7 | 22 | 7 |
| Electronic payments(전자지급) | 7 | 22 | 7 |
| Government procurement(정부조달) | 1 | 28 | 1 |
| Taxation exception(과세 예외) | 25 | 4 | **26** |
| **합계** | **97칸** | **106칸** | **99건** |

> 조문 수가 보유 칸 수보다 많은 두 곳은 **한 칸에 조문이 둘**이기 때문이다 — 튀르키예 과세 예외(기본협정 제8.1조 + 투자협정 제1.18조), 에콰도르 특송화물(통관 장 제4.7조 + 디지털 무역 장 제12.11조).

**주제별 성격 차이가 드러난다.** 전자송장·전자지급은 v3.2 시점에는 최근 디지털 협정 3개(DEPA·싱가포르 DPA·UAE)에만 있었는데, **미발효 5개를 더하니 7개로 늘었다** — 최근 타결되는 협정은 모두 디지털 무역 장에 이 조항을 넣고 있다. 정부조달(디지털 관련)은 여전히 **DEPA 하나뿐**이고, 과세 예외는 거의 모든 협정에 있다(없는 곳은 EFTA·ASEAN·인도·싱가포르 DPA 넷 — 미발효 5개는 전부 보유).

### [nice] 있으면 좋은 기능 (이번에도 범위 밖)
- 협정별 조항 출처(원문 링크·페이지) **화면 표시** — 출처는 `source`에 기록만 하고 **화면에는** 표시하지 않는다 (단, 위 기능 4에 따라 **인쇄물에는 표시**한다)
- 서명했으나 미발효인 협정 대응

---

## 6. 범위와 비범위

### 범위 (이번에 만들 것)
- **대상 협정 24개 × 7개 주제 데이터 전량 수록** (조항 부재 시 "없음" 명시) 〔v3.2 — 2차 사이클 시점에는 3개 주제였다〕
- **협정 선택 UI 개편** (이름 검색 · "있음만 보기" · 선택 유지)
- **조항 유무 표시 개편** (24개 대응 · 보유 개수 요약 · 비교로 바로 연결 · 배지는 이름만)
- 한-싱가포르 FTA와 싱가포르 DPA **분리** (FTA는 대상 제외, DPA만 수록)
- **협정 표기 규칙 확정 〔v2.1〕** (상대국명 / 정식 명칭 두 가지)
- 기존 기능 **무회귀 확인** 및 재배포

### 비범위 (이번에도 만들지 않을 것)
- 협정 개정 이력·최신 버전 **자동 추적** (단, `baseline_date` 라벨 표시는 함)
- **AI 요약·해설·해석** 기능
- 재정경제부 소관 **7개 주제 외의 조항** 〔v3.2〕 (다른 부처 소관 — 개인정보 보호·국경 간 데이터 이전·소스코드 등)
- **전통 FTA의 정부조달 장 전체** 〔v3.2〕 — 정부조달은 **디지털 경제 관련 조항만** 담는다(5절 `[must] 5)`)
- 사용자가 원문을 직접 추가·편집하는 기능 (데이터는 읽기 전용)
- 로그인·회원가입·즐겨찾기 저장
- **서명/타결 단계 협정**(한-EU DTA·한-말레이시아·한-GCC 등 6개) — 발효 전이라 문안이 바뀔 수 있어 이번 범위 밖
- 모바일 화면 최적화 (PC 넓은 화면 우선)

---

## 7. 보안·개인정보 검토

1차와 동일한 원칙을 유지한다.

- **공개등급:** FTA 협정문은 정부가 공식 게시·공표하는 **공개 자료**. 대외비 아님.
- **개인정보:** 조약 조문 텍스트만 다루므로 **수집·저장·처리하지 않는다.** 로그인 기능 없음.
- **보안 응답 헤더 5종**(CSP·X-Frame-Options·X-Content-Type-Options·Referrer-Policy·HSTS)을 개발·프리뷰(`vite.config.js`)와 프로덕션(`vercel.json`) 양쪽에 유지한다. **임의로 완화하지 않는다.**
- **비밀 정보:** `.env` 등은 `.gitignore`로 제외하고 커밋하지 않는다. 토큰·키 값은 출력에 노출하지 않는다.
- **〔v2.0 유의〕** 데이터 수집을 위해 외부 사이트(FTA 포털)에 접속하지만, **받아오는 것은 공개 협정문 PDF뿐**이고 앱 실행 시점에는 외부 통신이 없다(정적 JSON에 수록). CSP의 `default-src 'self'`를 그대로 유지한다.

---

## 8. 기술 스택

**변경 없음** — React + Vite + 정적 JSON + CSS.

- **React 18** — 화면을 컴포넌트로 분리
- **Vite 5** — 개발 서버·빌드
- **정적 JSON** — `src/data/articles.json` (별도 서버·DB 없음)
- **CSS** — 네이비 기반 공공 문서 톤, PC 넓은 화면 우선

> 협정이 24개, 주제가 7개로 늘어도 조항 데이터는 **72건 수준** 〔v3.2〕 이라 **정적 JSON으로 충분**하다. 백엔드·DB를 도입하지 않는다.

- **실행 환경:** 로컬 `npm run dev` + GitHub·Vercel 연결. `main` push 시 자동 재배포.

---

## 9. 데이터 준비 (대상 협정·출처·구조)

### 9-1. 대상 협정 목록 (**확정 24개**)

산업통상자원부 FTA 포털 "발효" 목록 24개 + **RCEP**(포털 발효 목록에 별도 표기되지 않으나 2022-02-01 발효, 관세청 FTA 포털 확인) = 25개에서, **한-싱가포르 FTA를 대상 제외**해 **최종 24개**다(사유는 표 아래 참조).

아래 표의 `#`는 **포털 목록 기준 연번**이므로 제외된 2번이 비어 있다. 앱의 실제 표시 순서는 **발효일 오름차순**이며 `src/constants.js`의 `AGREEMENTS` 배열 순서가 그대로 화면 순서가 된다.

| # | 협정 (앱 표기 = 상대국명) | 정식 명칭 (비교 뷰 칸 머리) | 포털 코드 | 발효일 | 수록 |
|---|---------------|------------------|-----------|--------|:---:|
| 1 | 칠레 | 한-칠레 FTA | `cl` | 2004-04-01 | ✅ (3주제 모두 조항 없음 확정) |
| 2 | ~~한-싱가포르 FTA~~ | ~~한-싱가포르 FTA~~ | ~~`sg`~~ | — | **대상 제외** (22번 싱가포르 DPA로 대체) |
| 3 | EFTA | 한-EFTA FTA | `efta` | 2006-09-01 | ✅ (3주제 모두 조항 없음 확정) |
| 4 | ASEAN | 한-ASEAN FTA | `asean` | 2007-06-01 | ✅ (3주제 모두 조항 없음 확정) |
| 5 | 인도 | 한-인도 CEPA | `in` | 2010-01-01 | ✅ **1건** (0·1·0) |
| 6 | EU | 한-EU FTA | `eu` | 2011-07-01(잠정) / 2015-12-13(전체) | ✅ **1건** (1·0·0) |
| 7 | 페루 | 한-페루 FTA | `pe` | 2011-08-01 | ✅ **3건** (1·1·1) |
| 8 | 미국 | 한-미국 FTA | `us` | 2012-03-15 (개정의정서 2019-01-01) | ✅ **3건** (1·1·1) |
| 9 | 튀르키예 | 한-튀르키예 FTA | `tr` | 2013-05-01 / 2018-08-01 | ✅ (3주제 모두 조항 없음 확정) |
| 10 | 호주 | 한-호주 FTA | `au` | 2014-12-12 | ✅ **2건** (1·0·1) |
| 11 | 캐나다 | 한-캐나다 FTA | `ca` | 2015-01-01 | ✅ **2건** (1·0·1) |
| 12 | 중국 | 한-중국 FTA | `cn` | 2015-12-20 | ✅ **3건** (1·1·1) |
| 13 | 뉴질랜드 | 한-뉴질랜드 FTA | `nz` | 2015-12-20 | ✅ **1건** (0·1·0) |
| 14 | 베트남 | 한-베트남 FTA | `vn` | 2015-12-20 | ✅ **3건** (1·1·1) |
| 15 | 콜롬비아 | 한-콜롬비아 FTA | `co` | 2016-07-15 | ✅ **3건** (1·1·1) |
| 16 | 영국 | 한-영국 FTA | `uk` | 2021-01-01 | ✅ **1건** (1·0·0) |
| 17 | 중미 | 한-중미 FTA | `c_america` | 2021-03-01 | ✅ **3건** (1·1·1) |
| 18 | RCEP | RCEP (역내포괄적경제동반자협정) | (별도) | 2022-02-01 | ✅ **3건** (1·1·1) |
| 19 | 이스라엘 | 한-이스라엘 FTA | `il` | 2022-12-01 | ✅ **3건** (1·1·1) |
| 20 | 캄보디아 | 한-캄보디아 FTA | `kh` | 2022-12-01 | ✅ (3주제 모두 조항 없음 확정) |
| 21 | 인도네시아 | 한-인도네시아 CEPA | `id` | 2023-01-01 | ✅ **1건** (0·1·0) |
| 22 | 싱가포르 DPA | 한-싱가포르 DPA | `ksdpa` | 2023-01-14 | ✅ **3건** (1·1·1) |
| 23 | DEPA | DEPA (디지털경제동반자협정) | `depa` | **2024-05-03** (한국 가입 발효) | ✅ **3건** (1·1·1) |
| 24 | 필리핀 | 한-필리핀 FTA | `ph` | 2024-12-31 | ✅ **2건** (0·1·1) |
| 25 | UAE | 한-UAE CEPA | `ucp` | 2026-05-01 | ✅ **3건** (1·1·1) |

**진행 현황: 24 / 24 판정 완료 🎉** — 조항을 수록한 협정 **19개**, 3주제 모두 조항 없음으로 확정한 협정 **5개**(칠레·EFTA·ASEAN·튀르키예·캄보디아). 총 **44건** 수록(무관세 15 · 특송화물 15 · 종이 없는 무역 14) · 전부 포털 원문 대조 통과.

> **〔v3.2〕 위 표의 건수 열과 아래 두 줄은 최초 3개 주제 기준이다.** 주제가 7개로 늘어난 뒤의 전체 현황(**168칸 · 72건**)은 **9-6절**을 본다. 특히 3주제 모두 "없음"인 5개 협정 중 **칠레·튀르키예·캄보디아는 과세 예외 조항이 있다.**

**주제별 보유 협정 수 (24개 기준):** 무관세 **15개 보유 / 9개 없음** · 특송화물 **15 / 9** · 종이 없는 무역 **14 / 10**

> 표의 `수록` 열 괄호는 **(무관세·특송화물·종이 없는 무역)** 순서의 건수다.

> **전 협정 판정 완료.** 수록한 44개 조항 전부 정부 공식 원문과 대조 검증됨. 텍스트 레이어가 불완전한 협정(한-칠레·한-인도·한-EU)은 사용자가 원문을 직접 확인해 제공한 것을 수록했고, 그 경위를 각 조항 `source`에 기록함. **〔v3.4〕 이 3건은 이후 OCR로 전부 기계 재검증했다** — EU는 기존 값이 정확했고, 칠레 2곳·인도 3곳은 오류가 발견돼 바로잡았다([CHECK.md](CHECK.md) v4.2).

- **협정 표기:** **상대국명만** 사용(→ 9-5). 데이터의 `agreement` 값과 `src/constants.js`의 `AGREEMENTS`가 **정확히 일치**해야 한다.
- **〔v2.0 정정 / v2.1 표기 갱신〕 싱가포르 처리:** 1차의 `한-싱가포르`는 실제로 **싱가포르 DPA** 조문이므로 개명했다(1차 표기 `한-싱 DPA` → **`싱가포르 DPA`**). **한-싱가포르 FTA(`sg`)는 사용자 결정으로 대상에서 제외**하고 싱가포르 DPA로 대체한다 — 디지털 통상 조항은 DPA가 담고 있고, FTA(2006) 협정문은 국문 추출 품질이 나빠 원문 무결성을 지킬 수 없다.
- **한-영국 유의:** 한-EU FTA 조문을 승계한 협정이라 한-EU와 조문이 유사할 수 있다. 각각 **자기 협정문 기준**으로 따로 수록한다.
- **오래된 협정 유의:** 한-칠레(2004)·한-EFTA(2006) 등 초기 협정은 전자상거래 장 자체가 없을 수 있다. 그런 경우 억지로 끼워 맞추지 않고 **"해당 조항 없음"으로 두는 것이 정답**이다.

### 9-2. 조항 데이터의 출처·확보 방법

**〔v2.0 전환〕 사용자 PDF 첨부 → 포털 직접 수집**

산업통상자원부 FTA 포털이 협정문을 **장(章)별 국문·영문 PDF**로 공개하며 주소 규칙이 일정하다는 것을 확인했다.

```
https://fta.motir.go.kr/webmodule/_PSD_FTA/{코드}/1/kor/CH_{n}_{제목}_kor.pdf   ← 국문
https://fta.motir.go.kr/webmodule/_PSD_FTA/{코드}/1/eng/CH_{n}_{제목}_eng.pdf   ← 영문
```

**수집 절차 (협정 1개당)**
1. 협정 페이지(`/ftamain/kfta/lov5/{코드}/2`)에서 장 목록과 PDF 링크를 얻는다.
2. 대상 주제에 해당하는 장을 찾는다. **장 위치는 협정마다 다르다** — 예: 한-중국 FTA는 무관세·종이없는무역이 제13장(전자상거래), 특송화물은 제4장(통관절차·무역원활화).
3. 해당 장의 국문·영문 PDF를 내려받아 텍스트를 추출한다.
4. 조문 본문을 그대로 옮겨 적고, 출처·특이사항을 `source`에 기록한다.
5. **수록 후 원문과 대조 검증**한다(9-4).

**지켜야 할 출처 원칙 (1차와 동일, 절대 완화 금지)**
- **정부 공식 1차 출처에서 그대로 확보한 것만** 입력한다. **검색엔진 요약·2차 설명 자료를 원문으로 쓰지 않는다.**
- 확보·검증이 안 되면 값을 비워("번역 없음"/미입력) 둔다.
- 한국어본이 없으면 **임의 번역을 만들지 않고** "번역 없음"으로 명시한다.
- **〔v3.3 신설〕 폰트 인코딩이 깨져 텍스트가 추출되지 않는 국문 PDF는 OCR로 복원할 수 있다.** 단 **OCR 결과를 그대로 쓰지 않는다** — ① 배율을 달리한 OCR 여러 회 대조 ② 깨진 텍스트 레이어가 보존한 **글자 수·공백 구조**와 대조 ③ 대조 가능한 다른 문서(승계 협정 등)와 비교, **셋을 모두 통과한 것만** 수록한다. 확정 못 한 글자가 하나라도 남으면 비워 둔다. OCR은 **줄 끝에 홀로 떨어진 한 글자를 배율과 무관하게 똑같이 버리므로**, ②를 생략하면 누락을 알아챌 수 없다. 상세 절차는 [CLAUDE.md](CLAUDE.md).

### 9-3. 데이터 구조(스키마)

| 필드 | 설명 |
|------|------|
| `agreement` | 협정 **짧은 이름 = 상대국명만** (`AGREEMENTS` 24개 중 하나와 글자까지 정확히 일치. 영문 명칭인 협정도 있다 — `EFTA`·`ASEAN`·`RCEP`·`DEPA`·`UAE`. 규칙은 9-5) |
| `topic` | **7개 중 1개 〔v3.2〕** — `customs_duties` / `express_shipments` / `paperless_trading` / `e_invoicing` / `electronic_payment` / `government_procurement` / `taxation_exception`. **협정 간 매칭 기준** |
| `article_no` | 조항 번호·제목 (표시용) |
| `text_en` | 영문 원문 |
| `text_ko` | 한국어본 (없으면 빈 값 → "번역 없음") |
| `source` | 출처·특이사항 (문서명·장·추출 경로·각주 유무 등) |
| `baseline_date` | 데이터 기준 시점(발효일·판본) |

### 9-4. 〔v2.0 신규〕 원문 수록 규칙과 검증 절차

협정 24개로 늘면서 반복 판단이 필요한 지점을 규칙으로 고정한다.

**(1) 각주 처리 — 조문 본문만 수록한다**
- 협정문에는 각주 번호가 **위첨자**로 붙는 경우가 많다. PDF에서 추출하면 이 숫자가 본문 흐름에 섞여 `…관행2을 유지할 것이다.3` 처럼 문장이 깨진다.
- 따라서 **각주 번호와 각주 본문은 `text_en`/`text_ko`에 넣지 않는다.** 조문 본문만 수록한다.
- 각주가 존재한다는 사실은 **`source`에 기록**한다. (예: "원문에 각주 3개가 달려 있으나 조문 본문만 수록함")
- 근거: 각주 번호는 조문의 문장 내용이 아니라 참조 표시이며, 그대로 끼워 넣으면 오히려 원문을 왜곡한다. "메모는 `source`에만"이라는 기존 규칙과도 일치한다.

**(2) 조문 부재·간접 규정 처리**
- 해당 주제의 **독립 조문이 없으면** 그 협정·주제 레코드를 만들지 않는다 → 화면에 "해당 조항 없음"으로 표시된다.
- **독립 조문은 없으나 다른 조항의 특정 항에 규정된 경우**(예: 한-영국·한-EU 제7.48조 제3항)에는 수록하되 `article_no`에 **해당 항까지 명시**한다.

**(2-1) 〔v2.0 확정〕 간접 규정 수록 판정 기준 — 엄격 기준**

간접 규정을 수록할지는 **실질 의무 유무**로 가른다.

| 판정 | 조건 | 사례 |
|:---:|------|------|
| **수록** | 그 항이 주제에 관한 **실질적 의무·약속**을 담은 경우 | 한-필리핀 제5.12조 제3~6항("무역행정문서를 전자적 형태로 이용 가능하도록 노력한다") · 한-EU·한-영국 제7.48조 제3항("전자적 수단에 의한 전달에 대해 관세를 부과하지 아니하기로 합의한다") · 중미 제14.3조 제1항 |
| **없음** | 주제를 **대화 주제·맥락으로만 언급**하고 의무가 없는 경우 | 규제협력 조항의 대화 주제 열거(한-EU·한-영국 제7.49조 제1항 마호) · "자동화" 조항의 "종이 없는 무역의 맥락에서" 언급(한-뉴질랜드·한-튀르키예 제3.4조) |

- 이 기준을 **협정 간에 예외 없이 동일하게** 적용한다. 기준이 섞이면 화면의 "없음"을 신뢰할 수 없게 된다.
- **〔소급 적용〕** 1차 사이클에서 수록했던 **한-영국 제7.49조(종이 없는 무역)** 는 대화 주제 열거일 뿐 의무가 없어 이 기준에 따라 **제거**했다. 한-EU도 같은 이유로 종이 없는 무역은 **"없음"** 으로 확정한다.
- 판단이 애매하면 **임의로 결정하지 않고 사용자에게 확인**을 요청한다.

**(3) 대조 검증 (수록 후 필수)**
- 수록한 `text_en`/`text_ko`가 **포털 원문 PDF 추출본에 그대로 포함되는지** 프로그램으로 대조한다.
- 비교 시 공백·줄바꿈과 **숫자만 있는 줄**(각주 번호·페이지 번호)만 제거하고, 글자는 그대로 비교한다.
- **전 조항이 통과해야** 수록 완료로 본다. 불일치가 나오면 원인을 확인해 바로잡는다.

### 9-5. 〔v2.1 신규〕 협정 표기 규칙

협정이 24개로 늘어나면서, 같은 협정을 자리마다 다르게 부르는 문제가 생겼다(`한-미`·`한-미국 FTA`·`미국`). **표기는 아래 두 가지만 쓰고, 새 약칭을 만들지 않는다.**

| 표기 | 값 | 쓰는 자리 | 예 |
|------|-----|-----------|-----|
| **짧은 이름** (= 데이터 `agreement` 값) | **상대국명만.** `한-` 접두사를 붙이지 않고, `FTA`·`CEPA` 같은 협정 종류도 붙이지 않는다 | 조항 유무 배지 · 선택 칩 · 추가 패널 목록 | `미국` · `EU` · `중국` · `싱가포르 DPA` |
| **정식 명칭** | 협정의 공식 명칭 | **비교 뷰 칸 머리** (자리가 넓어 전체가 들어감) | `한-미국 FTA` · `한-EU FTA` · `한-인도 CEPA` · `RCEP (역내포괄적경제동반자협정)` |

- **단일 출처:** 짧은 이름은 `src/constants.js`의 `AGREEMENTS`, 정식 명칭은 같은 파일의 `AGREEMENT_FULL_NAMES`에만 둔다. 화면·데이터·문서가 모두 이 두 곳을 따른다.
- **데이터의 `agreement`는 짧은 이름**이어야 하고 `AGREEMENTS`와 **글자까지 일치**해야 한다. 어긋나면 그 조항은 어떤 협정에도 매칭되지 않아 화면에서 사라진다.
- **예외 표기 3건:** 상대국이 국가가 아닌 협정은 그 이름을 그대로 짧은 이름으로 쓴다 — `EFTA`·`ASEAN`·`중미`(다국가 블록), `RCEP`·`DEPA`(다자 협정), `싱가포르 DPA`(같은 상대국과 FTA·DPA가 별개로 있어 구분이 필요).
- **문서에서 협정문 조문을 인용할 때**는 `한-EU 제7.48조`처럼 협정문을 가리키는 관례 표기를 써도 된다. 위 규칙은 **화면에 표시되는 값**과 **데이터 값**에 적용된다.
- **표시 순서:** `AGREEMENTS` 배열 순서 = **발효일 오름차순**. 배열 순서가 곧 화면 순서이므로, 협정을 추가할 때는 **발효일 위치에 끼워 넣는다**(맨 뒤에 붙이지 않는다).

---

## 10. 예외 상황 처리 원칙

1차에서 확정한 동작을 그대로 유지한다.

| 상황 | 표시 위치 | 문구 |
|------|-----------|------|
| 협정 0개 선택 | **화면 전체 대체**(`EmptyState`) | **"비교할 협정을 선택하세요."** |
| 협정 1개 선택 | 비교 뷰 | (안내 없이 한 칸만 정상 표시) |
| 해당 조항 없음 | **그 협정 칸 안** | **"해당 조항 없음"** |
| 한국어본 없음 | 그 칸의 KO 자리 | **"번역 없음"** |
| 검색 0건 | **화면 전체 대체**(`EmptyState`) | **"검색 결과가 없습니다. (검색어: "○○○")"** |

- 칸 내부 표시와 화면 전체 대체는 **서로 다른 층위**이므로 섞지 않는다.
- 검색어 공백 정책: 앞뒤 공백은 trim, 단어 사이 공백은 그대로 매칭.
- **〔v2.0 신규〕 협정 이름 검색 결과 0건:** 일치하는 협정이 없으면 **협정 목록 자리에만** "일치하는 협정이 없습니다."를 표시한다. 이미 선택한 협정의 비교 뷰는 **그대로 유지**한다(화면 전체를 대체하지 않는다).

---

## 11. 개발 단위 (2차 사이클 작업 순서)

| # | 작업 | 상태 |
|---|------|:---:|
| 1 | 확장 범위 확정 + 데이터 수집 방식 검증 (시범 1건: 한-중국 FTA) | ✅ 완료 |
| 2 | **PRD v2.0** | ✅ 완료 |
| 3 | PLAN v2.0 · DESIGN v2.0 — 협정 선택 UI 설계, 작업 분해 | ✅ 완료 |
| 4 | 협정 선택 UI 개편 구현 (이름 검색 · "있음만 보기" · 선택 유지) | ✅ 완료 |
| 5 | 조항 유무 표시 개편 구현 (보유 개수 요약 · 비교 연결 · "없음" 접기) | ✅ 완료 |
| 6 | 한-싱가포르 FTA / 싱가포르 DPA 분리 (FTA 제외, DPA 수록) | ✅ 완료 |
| 7 | 나머지 협정 데이터 수집·수록 | ✅ 완료 (24개 전량 판정 · 44건 수록) |
| 8 | 전 조항 원문 대조 검증 | ✅ 완료 (44/44 통과) |
| 9 | Check — 갭 분석 · 보안 점검 · 무회귀 확인 | ✅ 완료 ([CHECK.md](CHECK.md)) |
| 10 | Act — 발견된 문제 수정 | ✅ 완료 |
| 11 | 배포(`main` push → 자동 재배포) 및 문서 최종 갱신 | ✅ 완료 |
| 12 | **〔v2.1 추가〕 표기·UI 다듬기** — 협정명 상대국명 통일 · 배지 이름만 · 칸 머리 정식 명칭 | ✅ 완료 |
| 13 | **〔v2.1 추가〕 PRD·PLAN·DESIGN·CHECK·CLAUDE·README as-built 갱신** | ✅ 완료 |

> **데이터(7번)보다 UI(4·5번)를 먼저** 했다. 협정이 다 들어가도 화면이 무너지지 않는지 확인한 뒤 데이터를 채워, 나중에 대량 수정하는 일을 피했다. 더미 데이터 없이 실제 수록분으로만 검증했다.
>
> **12번 경위:** 실제 화면을 쓰면서 드러난 표기 문제를 잡은 단계다. ① 배지의 `있음 ＋` 꼬리표 제거 ② `한-미`·`한-중`·`한-싱 DPA` → `미국`·`중국`·`싱가포르 DPA` ③ 비교 뷰 칸 머리에 정식 명칭 표시. 이 과정에서 `＋ 협정 추가` 패널을 없애 봤으나, 배지만으로는 **현재 주제에 조항이 없는 협정을 새로 고를 수 없다**는 제약이 드러나 **사용자 결정으로 원복**했다(패널 유지).

---

*작성 근거: 1차 사이클 산출물(PRD v1.1 · CHECK v1.1) · 산업통상자원부 FTA 포털 발효 협정 목록 및 협정문 · 관세청 FTA 포털 발효일 · 중국 FTA 시범 수집 결과(2026-07-25).*
