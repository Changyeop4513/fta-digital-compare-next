# FTA 디지털협정 조항 비교 서비스 기획서 (PRD)

- 작성자: 이창엽
- 작성일: 2026-07-22
- 최종 개정: 2026-07-24 (**v1.1 — 구현 결과 반영본**)

## 개정 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| v1.0 | 2026-07-22 | 최초 기획 |
| **v1.1** | **2026-07-24** | **구현 완료 후 실제 산출물에 맞춰 갱신.** 주요 변경: ① 실행 환경을 로컬 → **웹 배포까지 확장**(8·6번) ② 한-싱가포르는 **DPA(디지털동반자협정)** 기준으로 확정(9-1번) ③ **정부 공식 1차 출처 원칙 + 포털 원문 대조 검증** 절차 추가(9-2번) ④ 데이터 형식 **JSON 확정**(9-3번) ⑤ **보안 응답 헤더 5종** 반영(7번) ⑥ 확정된 UI 표기·UX 개선 2건 반영(5번) ⑦ 예외 처리 실제 동작·문구 반영(10번) ⑧ 개발 단위 완료 표시 및 배포 단계 추가(11번) |

> 이 문서는 **as-built(구현된 그대로)** 기준으로 갱신되었습니다. 기획 당시 판단과 달라진 부분은 해당 절에 **〔v1.1 변경〕**으로 표시했습니다.

---

## 1. 추진 배경 (문제 정의)
_누가, 어떤 업무에서, 어떤 불편을 겪는가_

재정경제부 내부 실무자를 우선 사용자로 하며, 나아가 통상 실무자·연구자(로펌, 연구기관, 무역 관련 대학원생 등)까지 확대해 사용한다. 이들은 한국이 체결한 여러 FTA의 디지털 통상 조항을 검토하는 업무를 수행한다.

문제는 디지털 통상 조항이 협정마다 흩어져 있어, 같은 주제라도 협정별 조문을 원문에서 일일이 찾아 대조해야 한다는 점이다. 또한 지금 보는 조항이 최신 버전인지 헷갈려 확인에 시간이 든다. 특히 재정경제부 소관인 ① 전자적 전송물 무관세(Customs Duties), ② 특송화물(Express Shipments), ③ 종이 없는 무역(Paperless Trading) 세 주제를 협정 간에 비교할 때 이 불편이 반복된다.

단, 이 앱은 협정 개정 이력을 자동 추적하지는 않는다. 대신 각 조항에 데이터의 기준 시점(버전/기준일) 라벨을 함께 표기해 "지금 보는 것이 언제 기준본인지"를 알 수 있게 하는 방식으로 최신본 확인의 불편을 덜어준다(개정 자동 추적과는 구분되며, 자동 추적은 6번 비범위 참조).

---

## 2. 현행 방식과 한계
_지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가_

- **현행 방식:** 협정별 원문(PDF·웹 게시본)이나 정부 게시 자료를 각각 열어, 필요한 조항을 찾아 문서 편집기·엑셀에 수작업으로 옮겨 나란히 붙여놓고 비교한다.
- **한계 1 (분산):** 협정마다 문서가 따로 있어 같은 주제를 한눈에 모아 보기 어렵고, 조항을 찾아 오가는 데 시간이 걸린다.
- **한계 2 (수작업 오류):** 복사·붙여넣기 과정에서 조문 누락·오기 가능성이 있고, 영문 원문과 한국어본을 짝지어 관리하기 번거롭다.
- **한계 3 (버전 혼선):** 지금 보는 조항이 최신본인지 확인이 어렵다.
- **한계 4 (재현성):** 비교 결과가 개인 파일에 흩어져 있어, 다른 담당자가 같은 비교를 다시 하려면 처음부터 반복해야 한다.

---

## 3. 목표와 기대효과 (성공 기준)
_측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)_

- **처리 시간:** 세 주제 중 하나에 대해 협정 3~4개를 비교하는 데 걸리는 시간을 기존 수작업 대비 **20~30분 → 3분 이내**로 단축.
- **정확성:** 화면에 표시되는 조항 원문은 준비된 원본과 **100% 일치**(요약·변형 없음), 영문–한국어본 짝 표시 누락 0건.
- **커버리지:** 3개 주제 × 대상 협정 전체에 대해 조항 데이터 **수록률 100%**(원문이 없는 경우 "번역 없음" 등으로 명시).
- **재사용성:** 동일 비교를 다른 담당자가 재현하는 데 걸리는 준비 시간 **0분**(선택만 하면 즉시 동일 화면 재현).

> ※ 위 수치는 실습용 목표치이며, 실제 데이터 확보 범위에 따라 조정한다.

**〔v1.1 달성 결과〕** (근거: [CHECK.md](CHECK.md))

| 성공 기준 | 결과 |
|-----------|------|
| 처리 시간 3분 이내 | ✅ 달성 — 주제·협정 선택 즉시 좌우 비교 렌더 |
| 원문 100% 일치 | ✅ 달성 — 11개 조항 영·한 전부가 **산업통상부 FTA 포털 원문과 글자 단위 일치** 검증 |
| 커버리지 수록률 100% | ✅ 달성 — **11/11 조항** 영문·한국어 정본 완비 (한-영국 특송화물은 실제 조항 부재 → "해당 조항 없음"으로 정확 표시) |
| 재현 준비 0분 | ✅ 달성 — 선택만 하면 동일 화면 재현 |

---

## 4. 사용자와 이용 흐름
_누가 쓰는지와 사용 순서 (화살표로)_

- **주 사용자:** 재정경제부 내부 실무자 (확장: 통상 실무자·연구자)

**이용 흐름:**

주제를 먼저 고르면 각 협정의 조항 보유 여부(있음/없음)가 자동 표시되며, 상황에 따라 두 가지 방식으로 사용한다.

- **방식 1 (바로 비교):** 앱 접속 → 주제 1개 선택(무관세 / 특송화물 / 종이 없는 무역) → 조항을 보유한 협정 중 2~4개 선택 → 조항이 협정별로 좌우 나란히 표시(영문 원문 + 한국어본)
- **방식 2 (확인 후 비교):** 앱 접속 → 주제 1개 선택 → 해당 조항을 보유한 협정이 자동 표시됨 → 관심 협정 1개만 먼저 확인 → 있으면 다른 협정을 추가 선택해 비교
- **(공통)** (필요 시) 키워드 검색으로 원문 내 단어 찾기·하이라이트 → 주제를 바꿔 다른 조항도 같은 협정 조합으로 계속 비교

---

## 5. 주요 기능 (must / nice로 구분)
_반드시 필요한 기능(must)과 있으면 좋은 기능(nice)을 나눠서_

### [must] 1) 조항별 협정 비교 뷰
- 설명: 세 주제(전자적 전송물 무관세 / 특송화물 / 종이 없는 무역) 중 하나를 고르면, 그 조항을 여러 협정에 대해 좌우로 나란히 표로 보여준다. 각 칸에는 영문 원문과 한국어본을 함께 표시한다.
- AI가 꼭 지켜야 할 규칙:
  - 한 화면에 최대 4개 협정까지만 나란히 표시한다 (그 이상은 가로 스크롤 부담이라 금지). 1개만 선택하면 그 협정 한 칸만 보여준다.
  - 각 조항 칸은 반드시 영문 원문 + 한국어본을 한 쌍으로 표시하고, 한쪽만 있으면 "번역 없음"이라고 명시한다.
  - 한 협정에 같은 주제 조항이 여러 개면 누락 없이 모두 표시한다 (해당 칸에 조항 번호 순으로 나열).
  - 협정 이름은 한글 약칭("한-싱가포르 / RCEP / 한-영국 / 한-미")으로 표기하고, 좌우 배치 순서는 사용자가 고른 순서를 따른다.
  - 원문 텍스트는 절대 요약·수정하지 않고 협정 원문 그대로 표시한다.
  - 각 조항에는 데이터의 기준 시점(버전/기준일) 라벨을 함께 표시한다 (개정 자동 추적은 하지 않음).
  - 원문이 길면 기본은 앞 몇 줄만 접어서 보여주고, "더보기"를 누르면 전체를 펼친다 (칸 높이 과다 늘어남 방지).
  - **〔v1.1 추가〕 검색어가 포함된 조항 카드는 자동으로 펼쳐진다.** (접힌 상태에서는 하이라이트된 검색어가 보이지 않아 검색 결과를 놓치는 문제를 해소)

### [must] 2) 주제·협정 선택 필터 + 키워드 검색
- 설명: 상단에서 ① 비교할 주제(3개 중 1개)와 ② 비교할 협정들(체크박스)을 골라 비교 뷰를 구성하고, 키워드로 조항 원문 안에서 단어를 찾을 수 있다.
  **〔v1.1 추가〕** 이 컨트롤 바(주제·협정·검색)는 **스크롤해도 화면 상단에 고정(sticky)** 되어, 긴 원문을 내려 읽는 중에도 주제·협정을 바로 바꾸거나 검색할 수 있다.
- AI가 꼭 지켜야 할 규칙:
  - 주제는 3개로 고정한다 (무관세, 특송화물, 종이 없는 무역) — 임의로 추가·변경하지 않는다.
  - **〔v1.1 확정〕 주제는 라디오(단일 선택)로 제공하고**, 화면 표기는 **영문 병기** 형식으로 고정한다 — `Customs Duties(관세)` / `Express shipments(특송화물)` / `Paperless trading(종이 없는 무역)`. (접근성을 위해 네이티브 `<input type="radio">` + `role="radiogroup"` 사용)
  - 한 번에 1개 주제만 볼 수 있게 하고, 주제를 바꿔도 선택한 협정 목록은 그대로 유지한다.
  - 검색은 현재 선택한 주제·협정 범위 안의 조항 원문(영문·한국어본)에서만 단어를 찾아 하이라이트한다 (전체 데이터 검색이 아님). 영문·한국어를 모두 대상으로 하고, 영문은 대소문자를 구분하지 않는다.
  - 검색은 부분 일치로 찾고(예: "무관세" → "무관세화"도 포함), 찾은 단어는 노란 형광펜 느낌으로 하이라이트한다.
  - 검색은 단어를 찾아 하이라이트만 한다 — AI 요약·해석은 절대 하지 않는다.

### [must] 3) 주제별 조항 보유 협정 자동 찾기
- 설명: 주제(무관세 / 특송화물 / 종이 없는 무역)를 고르면, 대상 협정 각각에 그 조항이 있는지(있음/없음)를 자동으로 표시한다. 사용자는 조항을 보유한 협정을 바로 파악하고, 그 협정들만 골라 비교로 넘어갈 수 있다.
- AI가 꼭 지켜야 할 규칙:
  - 판정은 데이터의 `topic` 태그를 기준으로 하며, 조항이 하나라도 있으면 "있음", 데이터에 없으면 "없음"으로만 표시한다.
  - **〔v1.1 확정〕** 이 영역의 화면 라벨은 **"협정별 조항 유무"**로 한다. ("보유 현황"보다 뜻이 분명해 확정)
  - 조항 유무를 임의로 추론·요약하지 않는다 — 준비된 데이터에 있는 그대로만 판정한다.
  - "있음"인 협정을 선택하면 그대로 비교 뷰(기능 1)로 연결된다.

### [nice] 있으면 좋은 기능 (이번 범위 밖, 참고용)
- 비교 결과를 표 형태로 내보내기(인쇄·PDF) — **〔v1.1〕 미구현 (범위 밖 유지)**
- 협정별 조항 출처(원문 링크·페이지) 표시 — **〔v1.1〕 미구현.** 출처는 데이터의 `source` 필드에 **기록만** 하고 화면에는 표시하지 않는다(설계 시 결정, [DESIGN.md](DESIGN.md) 2번).

---

## 6. 범위와 비범위
_이번 실습에서 만들 것 / 만들지 않을 것_

### 범위 (이번에 만들 것)
- 조항별 협정 비교 뷰 (주제 3개 × 협정 1~4개, 영문 원문 + 한국어본 나란히)
- 주제·협정 선택 필터 + 원문 내 키워드 검색·하이라이트
- 주제별 조항 보유 협정 자동 찾기 (주제 선택 시 협정별 있음/없음 자동 표시)
- **〔v1.1 범위 추가〕 웹 배포** — 정적 사이트를 공개 URL로 배포하고, 보안 응답 헤더를 적용한다. (당초 "범위 밖"이었으나 실제로 수행함 → 8번 참조)
- **〔v1.1 범위 추가〕 저장소 공개 문서화** — 공개 GitHub 저장소와 README를 산출물에 포함한다.

### 비범위 (이번에 만들지 않을 것)
- 개정 이력·최신 버전 자동 추적 기능 (협정 개정 시 변경점 자동 비교·알림) — 이번 범위에서 제외. 단, 각 조항에 '데이터 기준 시점/버전 라벨'은 표시해 최신본 확인을 돕는다 (자동 추적과는 구분).
- AI 요약·해설 기능 — 조항 해석이나 요약은 제공하지 않고, 원문 비교와 키워드 검색만 한다
- 재경부 소관 3개 주제 외의 조항 (데이터 이전, 소스코드, 개인정보보호 등 다른 부처 소관) — 다루지 않음
- 사용자가 원문을 직접 추가·편집하는 기능 — 데이터는 준비된 것만 읽기 전용으로 제공
- 로그인·회원가입, 즐겨찾기 저장 — 계정 없이 바로 쓰는 단순 도구로 유지

---

## 7. 보안·개인정보 검토
_사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법_

- **사용 문서의 공개등급:** FTA 협정문은 정부가 공식 게시·공표하는 **공개 자료**이므로 대외비·비밀 자료가 아니다. (실제 데이터 입력 시 출처가 공개 게시본인지 최종 확인)
- **개인정보 여부:** 조약 조문 텍스트만 다루므로 **개인정보를 수집·저장·처리하지 않는다.** 로그인·회원 기능도 없어 개인정보 이슈가 발생하지 않는다.
- **API 키 관리:** 이번 범위는 준비된 조항 데이터를 읽어 보여주는 방식이라 외부 유료 API가 필수는 아니다. 만약 외부 서비스(예: 배포 호스팅)를 쓰게 되면, 키는 소스코드에 직접 넣지 않고 **환경변수(.env)로 분리하고, .env는 버전관리(git)에서 제외**한다.

**〔v1.1 추가〕 배포 보안 (실제 적용됨)**

웹 배포를 하게 되면서 아래를 추가로 적용한다.

- **보안 응답 헤더 5종**을 개발·프리뷰(`vite.config.js`)와 프로덕션(`vercel.json`) 양쪽에 적용한다.

| 헤더 | 값 |
|------|-----|
| `Content-Security-Policy` | `default-src 'self'` 기반 (외부 스크립트·프레임 차단). HMR과 충돌하므로 **프리뷰·프로덕션에만** 적용 |
| `X-Content-Type-Options` | `nosniff` |
| `X-Frame-Options` | `SAMEORIGIN` |
| `Referrer-Policy` | `strict-origin-when-cross-origin` |
| `Strict-Transport-Security` | `max-age=63072000; includeSubDomains` (HTTPS 강제) |

- **배포 토큰·비밀값은 `.env`에 두고 `.gitignore`로 제외**하며, 저장소에 올리지 않는다.
- **보안 점검 결과(CHECK.md):** Critical/High/Medium **0건**. 하드코딩 키 0건, `dangerouslySetInnerHTML`·`eval` 0건(React 자동 이스케이프), 개인정보 처리 코드 0건, 외부 CDN 0건.
- 단, 기획·설계 문서(PRD·DESIGN·PLAN·CHECK·CLAUDE)는 **사용자 결정에 따라 공개 저장소에 함께 공개**한다. 비밀 정보가 없는 공개 협정문 기반 문서이므로 공개에 문제가 없다.

---

## 8. 기술 스택
_개발에 사용할 도구 모음 (난이도별 옵션 중 내가 고른 것과 그 이유)_

**선택: 옵션 2 (보통) — React + Vite + JSON(또는 CSV) 데이터**

- **구성 도구:**
  - **React** — 화면을 '검색창·표·버튼' 같은 부품(컴포넌트)으로 나눠 만드는 도구
  - **Vite** — React 앱을 빠르게 실행·개발하도록 도와주는 도구
  - **JSON 파일** — 협정 조항 데이터(영문 원문·한국어본)를 담아두는 파일 **〔v1.1 확정: JSON 채택, CSV 미채택〕**
  - **CSS** — 네이비 기반의 차분한 공공 문서 느낌으로 화면을 꾸미는 도구

- **고른 이유:**
  - 이 앱은 협정 칸, 주제·협정 선택 필터, 키워드 검색처럼 화면 요소가 여러 개인데, React로 부품을 나눠 만들면 **각 요소를 깔끔하게 관리**할 수 있다.
  - 비교 대상 협정이 2~4개로 늘거나 표가 많아져도 **화면이 헝클어지지 않고 정리가 잘 된다.**
  - 지금 범위(읽기 전용 비교)에는 데이터베이스·서버(옵션 3)까지는 과하고, 옵션 1보다 **확장성이 좋아** 나중에 기능을 붙이기 쉽다.
  - 데이터는 별도 서버 없이 JSON/CSV 파일로 관리해, 준비·유지가 단순하다.

- **실행 환경:** ~~이번 실습은 **내 PC(로컬)에서 실행·확인**하는 것을 기준으로 한다 (웹 배포·사내 공유는 이번 범위 밖, 필요 시 추후 확장).~~

  **〔v1.1 변경 — 배포까지 확장〕** 로컬 실행 확인 후 **웹 배포까지 수행**했다.

  | 항목 | 내용 |
  |------|------|
  | 공개 주소 | **https://fta-digital-compare.vercel.app** |
  | 호스팅 | **Vercel** (정적 사이트, 서버·DB 없음) |
  | 소스 저장소 | **https://github.com/Changyeop4513/fta-digital-compare** (공개) |
  | 배포 방식 | GitHub 연동 — `main` 브랜치에 push하면 **자동 재배포** |
  | 배포 설정 | `vercel.json` (빌드 명령·출력 디렉터리 + 보안 응답 헤더 5종 → 7번 참조) |

  로컬 실행은 그대로 유효하다: `npm install` → `npm run dev` (http://localhost:5173).

---

## 9. 데이터 준비 (대상 협정·출처·구조)
_어떤 협정을, 어디서 구해, 어떤 형식으로 담을지_

### 9-1. 대상 협정 목록
1차 실습에 넣을 협정을 아래 4개로 확정한다. (세 주제 조항 유무는 데이터 입력 시 재확인)
- **한-싱가포르 FTA, RCEP, 한-영국 FTA, 한-미 FTA(KORUS)**
- 추후 확장 후보: 한-EU, 한-중, 한-베트남, 한-인도네시아 CEPA 등

**〔v1.1 확정 — 실제 수록한 협정문과 조항〕**

디지털 통상 조항은 협정마다 담긴 문서가 달라, 아래 기준본으로 확정했다. **특히 한-싱가포르는 FTA가 아니라 별도 협정인 `한-싱 DPA(디지털동반자협정)`가 해당 조항을 담고 있어 DPA를 기준으로 수록**한다.

| 협정 | 실제 기준 협정문 | 기준 시점(`baseline_date`) |
|------|------------------|---------------------------|
| 한-싱가포르 | **한-싱 DPA(디지털동반자협정)** — FTA 아님 | 서명 2022-11-21 · 발효 2023-01-14 |
| RCEP | 2020-11-15 서명본(법적 검증본) | 한국 발효 2022-02-01 |
| 한-영국 | 한-EU FTA 2011 발효본 조문을 **한-영국 FTA가 승계** | 2021-01-01 발효 |
| 한-미 | KORUS (USTR 게재 최종본) | 2019-01-01 개정 발효 기준 |

**수록 결과 — 총 11개 조항 (4개 협정 × 3개 주제 − 부재 1건)**

| 주제 | 한-싱가포르(DPA) | RCEP | 한-영국 | 한-미 |
|------|---|---|---|---|
| 무관세 | 제14.5조 | 제12.11조 | 제7.48조 제3항 | 제15.3조 |
| 특송화물 | 제14.13조 | 제4.15조 | **해당 조항 없음** | 제7.7조 |
| 종이 없는 무역 | 제14.12조 | 제12.5조 | 제7.49조 제1항 마호 | 제15.6조 |

> 한-영국은 전자상거래 절(제7장 제6절)에 **독립된 특송화물 조항이 없다.** 무관세·종이 없는 무역도 독립 조문이 아니라 목적·협력 조항의 특정 항에 규정돼 있어, 조항 번호에 해당 항을 함께 명시했다. (임의 추론 없이 데이터에 있는 그대로 "없음" 표시)

### 9-2. 조항 데이터의 출처·확보 방법
- **출처:** 정부가 공식 게시·공표한 협정문(영문 원문 및 한국어본) — 예: 산업통상자원부/재정경제부 FTA 포털 등 공개 게시본.
- **확보 방법:** 공개 게시본에서 해당 조항을 찾아 텍스트로 옮겨 데이터 파일에 입력한다. 입력 시 출처(문서·페이지/링크)를 함께 기록한다.

**〔v1.1 추가 — 1차 출처 원칙과 대조 검증 (반드시 지킬 것)〕**

- **정부 공식 1차 출처(협정문 원본 PDF 등)에서 그대로 확보한 것만 입력한다.** **검색엔진 요약·2차 설명 자료를 원문으로 쓰지 않는다.** 확보·검증이 안 되면 값을 비워("번역 없음"/미입력) 둔다.
  > 근거 사례: 작업 중 KORUS 제15.3조에 검색 요약본을 넣었다가 실제 원문과 달라 정정한 일이 있었다. 이후 이 원칙을 규칙으로 고정했다. ([CLAUDE.md](CLAUDE.md) 동일 규칙)
- **주석 분리:** 출처·기준일·보정 메모는 **`source` 필드에만** 적고, `text_en`/`text_ko`에는 원문 외 어떤 표시도 섞지 않는다.
- **입력 후 대조 검증을 실시한다.** 실제 확보 경로와 검증 결과는 아래와 같다.

| 협정 | 영문 원문 확보처 | 한국어본 확보처 |
|------|------------------|-----------------|
| 한-싱가포르 | 싱가포르 MTI 게재 공식 영문본 | KSDPA 국문 정본 (한국어·영어 동등 정본) |
| RCEP | 뉴질랜드 외교통상부(MFAT) 법적 검증본 | RCEP 제4·12장 국문본 |
| 한-영국 | 한-EU FTA 전문 (산업통상자원부 게재, 한-영국 FTA가 조문 승계) | 한-영 FTA 국문본 |
| 한-미 | USTR 공식 Final Text | 한-미 FTA 국문본 |

- **검증 결과:** 11개 조항의 **영문·한국어 전부가 산업통상부 FTA 포털 공식 PDF와 글자 단위로 일치**함을 확인했다. (RCEP 제4장 국문본은 포털 파일과 **해시값 완전 동일**) 원문의 특이 표기(예: RCEP 4.15 국문본의 호 번호 표기)도 **포털 원본과 동일**함을 확인한 뒤 원문 그대로 수록했다. 상세는 [CHECK.md](CHECK.md) 참조.
- **한국어본 기준:** 정부 공식 한국어 정본(FTA 포털 등 게시본)을 기준으로 입력한다.
- **한국어본이 없는 경우:** 임의 번역을 만들지 않고 "번역 없음"으로 명시한다.
- **기준 시점:** 각 협정은 **현재 유효한 최신본**(개정이 있었으면 반영된 지금 유효 조문)을 기준으로 입력하고, 그 기준일을 `baseline_date`에 기록한다.
- **입력 순서:** 처음에는 **최소 샘플(협정 2개 × 주제 1개)**만 넣어 화면·기능을 완성한 뒤, 동작 확인 후 나머지 협정·주제 데이터를 채운다 (개발 단위 2 → 12로 연결).

### 9-3. 데이터 구조(스키마) — 〔v1.1 확정〕
한 조항을 아래 항목으로 저장한다 (JSON 필드). **이 스키마 그대로 `src/data/articles.json`에 11건이 수록되어 있다.**
- `agreement` — 협정명. **〔v1.1 확정〕 한글 약칭을 그대로 값으로 쓴다** (`"한-싱가포르"` / `"RCEP"` / `"한-영국"` / `"한-미"` — `src/constants.js`의 목록과 정확히 일치해야 함)
- `topic` — 주제 (customs_duties / express_shipments / paperless_trading 중 1개). **이 태그가 협정 간 조항을 나란히 묶는 기준**이며, 조항 번호는 표시용일 뿐 매칭에 쓰지 않는다.
- `article_no` — 조항 번호·제목
- `text_en` — 영문 원문
- `text_ko` — 한국어본 (없으면 빈 값 + 번역 없음 표시)
- `source` — 출처 (문서명·페이지 또는 링크)
- `baseline_date` — 데이터 기준 시점(버전/기준일)

---

## 10. 예외 상황 처리 원칙
_사용 중 생길 수 있는 예외를 화면에서 어떻게 다룰지_

- **협정을 1개만 선택한 경우:** 안내 문구 없이 선택한 그 협정을 그대로 보여준다 (한 칸만 표시).
- **협정을 0개 선택한 경우:** 보여줄 협정이 없으므로 비교 뷰를 띄우지 않고 "협정을 선택하세요"라고 안내한다.
- **특정 협정에 해당 조항이 아예 없는 경우:** "번역 없음"과 구분하여 "해당 조항 없음"으로 명확히 표시한다.
- **키워드 검색 결과가 0건인 경우:** 빈 화면 대신 "검색 결과가 없습니다"라고 안내한다.

**〔v1.1 확정 — 실제 구현된 동작과 문구〕**

| 상황 | 표시 위치 | 실제 문구 |
|------|-----------|-----------|
| 협정 0개 선택 | **화면 전체 대체** (`EmptyState`) | **"비교할 협정을 선택하세요."** |
| 협정 1개 선택 | 비교 뷰 | (안내 없이 한 칸만 정상 표시) |
| 해당 조항 없음 | **그 협정 칸 안** | **"해당 조항 없음"** |
| 한국어본 없음 | 그 칸의 KO 자리 | **"번역 없음"** |
| 검색 0건 | **화면 전체 대체** (`EmptyState`) | **"검색 결과가 없습니다. (검색어: "○○○")"** |

- **중요:** 검색 0건은 "배너만 띄우고 비교 뷰는 그대로" 두면 *0건인데 표가 남아 있는* 모순이 생긴다. 따라서 협정 0개와 **동일하게 비교 뷰 자체를 화면 전체 안내로 대체**한다.
- **칸 내부 표시("해당 조항 없음"·"번역 없음")와 화면 전체 대체(`EmptyState`)는 서로 다른 층위**임을 구분한다.
- **검색어 공백 정책:** 앞뒤 공백은 `trim`하고, 단어 사이 공백은 그대로 매칭한다(하이라이트도 동일).

---

## 11. 개발 단위
_핵심 기능을 만들기 좋은 작은 작업 단위로 나눈 것 (만들 순서대로)_

1. React + Vite 프로젝트 기본 틀 만들기 (빈 화면이 브라우저에 뜨는 것까지)
2. 조항 데이터 스키마(9-3) 확정하고, 최소 샘플(협정 2개 × 주제 1개) JSON 파일 만들기
3. JSON 데이터를 앱으로 불러와 화면에 목록으로 뿌려보기 (연결 확인용)
4. 주제 선택 UI 만들기 (무관세 / 특송화물 / 종이 없는 무역 중 1개 고르기)
5. 주제 선택 시 각 협정의 조항 보유 현황(있음/없음) 자동 표시하기
6. 협정 선택 UI 만들기 (체크박스, 최대 4개 제한 · 1개 이상 선택 · "있음" 협정 위주로 선택)
7. 조항별 비교 뷰 만들기 (선택한 협정을 좌우로 나란히 표로 표시)
8. 각 칸에 영문 원문 + 한국어본 한 쌍으로 표시하기
9. 각 조항에 기준 시점(버전/기준일) 라벨 표시하기
10. 키워드 검색 기능 만들기 (선택 범위 안에서 영/한 검색 + 하이라이트)
11. 예외 처리 붙이기 (협정 0개 선택 / 해당 조항 없음 / 검색 0건 안내)
12. 디자인 입히기 (네이비 기반 공공 문서 느낌, PC 화면 우선 — prd_lite.md 5번 참고)
13. 나머지 협정 데이터 채워 넣고 최종 점검

**〔v1.1 진행 결과 — 1~13단계 전부 완료 ✅〕**

이후 실제로 아래 단계를 추가 수행했다.

| 추가 단계 | 내용 | 상태 |
|-----------|------|:---:|
| 14. UX 보완 | 검색어 포함 조항 **자동 펼침**, 컨트롤 바 **상단 고정(sticky)** | ✅ |
| 15. 설계 정합 | 주제 UI를 버튼 토글 → **네이티브 라디오**로, `EmptyState` **컴포넌트 분리**, 검색 0건 **화면 전체 대체**로 정합 | ✅ |
| 16. 원문 검증 | 산업통상부 FTA 포털 원문과 **11/11 대조 검증** | ✅ |
| 17. 보안 적용 | 보안 응답 헤더 5종 (`vite.config.js` + `vercel.json`) | ✅ |
| 18. 배포 | GitHub 공개 저장소 + **Vercel 배포** (push 시 자동 재배포) | ✅ |
| 19. 문서화 | **README.md** 작성(기능·수록 현황·데이터 출처·보안·구조), 기획 문서 공개 | ✅ |

> 각 단계는 [CLAUDE.md](CLAUDE.md)의 검증 루프(변경 → 브라우저로 직접 확인 → 코드 리뷰 → 통과 시 한 줄 요약)를 거쳤다. 종합 점검 결과는 [CHECK.md](CHECK.md) — **남은 조치 0건**.
