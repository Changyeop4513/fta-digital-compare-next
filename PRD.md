# FTA 디지털협정 조항 비교 서비스 기획서 (PRD)

- 작성자: 이창엽
- 작성일: 2026-07-22
- 최종 개정: 2026-07-25 (**v2.0 — 2차 사이클: 협정 전면 확대**)

## 개정 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| v1.0 | 2026-07-22 | 최초 기획 |
| v1.1 | 2026-07-24 | 1차 사이클 완료 후 as-built 반영 (구현·검증·배포 결과) |
| **v2.0** | **2026-07-25** | **2차 사이클 기획.** 주제 3개는 그대로 두고, **대상 협정을 4개 → 한국이 체결한 발효 협정 전체(25개)로 확대**한다. 이에 따라 ① 데이터 확보 방식을 **포털 직접 수집**으로 전환(9-2) ② **협정 25개용 선택 UI** 신설(5-2·5-3) ③ **각주 처리 규칙** 명문화(9-4) ④ **한-싱가포르 FTA와 한-싱 DPA 분리**(9-1) |

> **1차 사이클 산출물(운영 중, 별개):** https://fta-digital-compare.vercel.app
> **2차 사이클(이 문서):** 저장소 `Changyeop4513/fta-digital-compare-next` · 사이트 https://fta-digital-compare-next.vercel.app

---

## 1. 추진 배경 (문제 정의)
_누가, 어떤 업무에서, 어떤 불편을 겪는가_

재정경제부 내부 실무자를 우선 사용자로 하며, 통상 실무자·연구자(로펌, 연구기관, 무역 관련 대학원생 등)까지 확대해 사용한다. 이들은 한국이 체결한 여러 FTA의 디지털 통상 조항을 검토한다.

1차 사이클에서 **협정 4개**(한-싱 DPA·RCEP·한-영국·한-미)를 수록해 "한 화면에서 원문 그대로 비교"라는 핵심 가치는 검증했다. 그러나 실무에서 검토 대상이 되는 협정은 4개가 아니라 **한국이 체결한 발효 협정 전체**다. 4개 밖의 협정을 확인하려면 결국 협정문 PDF를 직접 열어야 하므로, 1차에서 없애려 했던 수작업이 그대로 남는다.

특히 기능 3(**조항 보유 협정 자동 찾기**)은 대상이 전체일 때 비로소 제 값을 한다. "어느 협정에 전자적 전송물 무관세 조항이 있는가"라는 질문에 지금은 4개 안에서만 답할 수 있어 사실상 답이 되지 못한다.

---

## 2. 현행 방식과 한계
_1차 산출물의 무엇이 아쉬운가_

- **커버리지 한계 (핵심):** 발효 협정 25개 중 **4개(16%)만** 수록. 나머지 21개는 여전히 수작업 대조가 필요하다.
- **조항 유무 판정의 불완전성:** 화면의 "없음"이 *협정에 조항이 없다*는 뜻인지 *아직 수록하지 않았다*는 뜻인지 구분되지 않는다. 대상이 전체가 되어야 이 모호함이 사라진다.
- **데이터 확보 병목:** 1차에서는 사용자가 협정문 PDF를 직접 첨부해야 했다. 21개분을 같은 방식으로 처리하는 것은 현실적이지 않다.
- **협정 명칭 혼선:** 1차의 `한-싱가포르`는 실제로 **한-싱 DPA(디지털동반자협정)** 기준으로 수록돼 있다. 포털에는 **한-싱가포르 FTA**와 **한-싱 DPA**가 별개 협정으로 존재하므로 확대 시 반드시 분리해야 한다.
- **UI 규모 한계:** 협정 선택 체크박스와 조항 유무 배지가 **한 줄 나열** 구조다. 4개에는 맞지만 25개에서는 상단 고정 바가 화면을 덮어 쓸 수 없게 된다.

---

## 3. 목표와 기대효과 (성공 기준)
_측정 가능한 숫자로_

- **커버리지:** 발효 협정 **25개 × 3개 주제 수록률 100%**. 협정에 해당 조항이 없으면 "해당 조항 없음"으로 명시하며, **미수록 상태로 남기지 않는다.**
- **원문 무결성:** 수록한 전 조항의 영문·한국어가 **포털 원문과 100% 일치**(요약·수정·의역 없음). 대조 검증 미통과 **0건**.
- **탐색 성능:** 25개 협정 중 원하는 협정을 **3회 이내의 조작**으로 찾아 선택할 수 있다(이름 검색 또는 "있음만 보기" 활용).
- **비교 가독성 유지:** 협정이 25개로 늘어도 **동시 비교는 최대 4개**를 지켜 좌우 스크롤 부담을 만들지 않는다.
- **무회귀:** 1차에서 통과한 기능·예외 처리·보안 헤더가 **그대로 동작**한다.
- **처리 시간:** 주제 1개에 대해 협정 3~4개 비교를 **3분 이내**(1차 기준 유지).

---

## 4. 사용자와 이용 흐름

- **주 사용자:** 재정경제부 내부 실무자 (확장: 통상 실무자·연구자)

**이용 흐름 (협정 25개 기준으로 갱신)**

- **방식 1 (조항 있는 협정 찾기 → 비교):** 앱 접속 → 주제 1개 선택 → **25개 협정의 조항 유무가 한눈에 표시됨** → "있음" 협정 중 2~4개 선택 → 좌우 나란히 비교
- **방식 2 (특정 협정 확인):** 앱 접속 → 주제 선택 → **협정 이름 검색**(예: "베트") → 해당 협정 확인 → 필요하면 다른 협정을 추가해 비교
- **(공통)** 키워드 검색으로 원문 내 단어 찾기·하이라이트 → 주제를 바꿔 같은 협정 조합으로 계속 비교

> 25개로 늘면서 **"조항을 가진 협정만 추려 보는 것"** 이 가장 흔한 시작점이 된다. 이용 흐름의 무게중심이 기능 3으로 옮겨간다.

---

## 5. 주요 기능 (must / nice)

### [must] 1) 조항별 협정 비교 뷰 — *1차 규칙 유지, 변경 없음*
- 설명: 주제 1개를 고르면 그 조항을 여러 협정에 대해 좌우로 나란히 표로 보여준다. 각 칸에 영문 원문과 한국어본을 함께 표시한다.
- 규칙:
  - 한 화면에 **최대 4개 협정**까지만 나란히 표시. 1개만 선택하면 한 칸만.
  - 각 칸은 **영문 원문 + 한국어본을 한 쌍**으로. 한쪽만 있으면 "번역 없음" 명시.
  - 한 협정에 같은 주제 조항이 여러 개면 **누락 없이 모두** 조항 번호 순으로.
  - 좌우 배치 순서는 **사용자가 고른 순서**를 따른다.
  - 각 조항에 **기준 시점(`baseline_date`) 라벨** 표시.
  - 원문이 길면 접어 두고 "더보기"로 펼침. **검색어가 포함된 카드는 자동으로 펼침.**
  - 원문 텍스트는 절대 요약·수정하지 않는다.

### [must] 2) 주제·협정 선택 필터 + 키워드 검색 — *협정 선택 UI 개편*
- 설명: 상단에서 주제(3개 중 1개)와 협정들을 골라 비교 뷰를 구성하고, 키워드로 원문 안에서 단어를 찾는다.
- **〔v2.0 신규〕 협정 선택 UI를 25개 규모에 맞게 개편한다.** 요구사항:
  - **협정 이름 검색** — 이름 일부를 입력해 좁힐 수 있어야 한다(예: "베트" → 한-베트남).
  - **"조항 있는 협정만 보기"** — 현재 주제 기준으로 "있음"인 협정만 추려 보는 수단을 제공한다.
  - **선택한 협정은 항상 보이게** — 목록을 좁히거나 스크롤해도 이미 고른 협정을 놓치지 않아야 한다.
  - **컨트롤 바가 화면을 덮지 않아야 한다** — 25개를 한 줄에 나열하지 않는다. (구체 방식은 DESIGN에서 결정)
- 규칙 (유지):
  - 주제는 3개로 고정. 표기는 `Customs Duties(관세)` / `Express shipments(특송화물)` / `Paperless trading(종이 없는 무역)`.
  - 한 번에 1개 주제만. **주제를 바꿔도 선택한 협정 목록은 유지.**
  - 원문 검색은 **현재 선택한 주제·협정 범위 안**에서만. 부분 일치, 영문 대소문자 무시.
  - 찾은 단어는 노란 형광펜으로 **하이라이트만** — AI 요약·해석 없음.

> **주의:** 이번에 추가되는 **협정 이름 검색**과 기존 **원문 키워드 검색**은 **서로 다른 기능**이다. 화면에서 두 입력창이 헷갈리지 않도록 구분해야 한다(DESIGN에서 처리).

### [must] 3) 주제별 조항 보유 협정 자동 찾기 — *25개 규모 대응*
- 설명: 주제를 고르면 대상 협정 각각에 그 조항이 있는지(있음/없음)를 자동 표시한다.
- 규칙:
  - 판정은 데이터의 `topic` 태그 기준. 하나라도 있으면 "있음", 없으면 "없음". **임의 추론 금지.**
  - 화면 라벨은 **"협정별 조항 유무"**.
  - **〔v2.0 신규〕 25개를 한눈에 파악할 수 있는 배치**로 바꾼다. **보유 개수 요약**(예: `무관세 — 25개 중 12개 보유`)을 함께 제공한다.
  - **〔v2.0 신규〕 "있음" 표시에서 바로 비교 대상으로 추가**할 수 있게 해 기능 3 → 기능 1 연결을 강화한다.

### [nice] 있으면 좋은 기능 (이번에도 범위 밖)
- 비교 결과 내보내기(인쇄·PDF)
- 협정별 조항 출처(원문 링크·페이지) **화면 표시** — 출처는 `source`에 기록만 하고 표시하지 않는다

---

## 6. 범위와 비범위

### 범위 (이번에 만들 것)
- **발효 협정 25개 × 3개 주제 데이터 전량 수록** (조항 부재 시 "없음" 명시)
- **협정 선택 UI 개편** (이름 검색 · "있음만 보기" · 선택 유지)
- **조항 유무 표시 개편** (25개 대응 · 보유 개수 요약 · 비교로 바로 연결)
- 한-싱가포르 FTA와 한-싱 DPA **분리 수록**
- 기존 기능 **무회귀 확인** 및 재배포

### 비범위 (이번에도 만들지 않을 것)
- 협정 개정 이력·최신 버전 **자동 추적** (단, `baseline_date` 라벨 표시는 함)
- **AI 요약·해설·해석** 기능
- 재정경제부 소관 3개 주제 **외의 조항** (다른 부처 소관) — 주제는 3개 그대로
- 사용자가 원문을 직접 추가·편집하는 기능 (데이터는 읽기 전용)
- 로그인·회원가입·즐겨찾기 저장
- **서명/타결 단계 협정**(한-EU DTA·한-말레이시아·한-GCC 등 6개) — 발효 전이라 문안이 바뀔 수 있어 이번 범위 밖
- 모바일 화면 최적화 (PC 넓은 화면 우선)

---

## 7. 보안·개인정보 검토

1차와 동일한 원칙을 유지한다.

- **공개등급:** FTA 협정문은 정부가 공식 게시·공표하는 **공개 자료**. 대외비 아님.
- **개인정보:** 조약 조문 텍스트만 다루므로 **수집·저장·처리하지 않는다.** 로그인 기능 없음.
- **보안 응답 헤더 5종**(CSP·X-Frame-Options·X-Content-Type-Options·Referrer-Policy·HSTS)을 개발·프리뷰(`vite.config.js`)와 프로덕션(`vercel.json`) 양쪽에 유지한다. **임의로 완화하지 않는다.**
- **비밀 정보:** `.env` 등은 `.gitignore`로 제외하고 커밋하지 않는다. 토큰·키 값은 출력에 노출하지 않는다.
- **〔v2.0 유의〕** 데이터 수집을 위해 외부 사이트(FTA 포털)에 접속하지만, **받아오는 것은 공개 협정문 PDF뿐**이고 앱 실행 시점에는 외부 통신이 없다(정적 JSON에 수록). CSP의 `default-src 'self'`를 그대로 유지한다.

---

## 8. 기술 스택

**변경 없음** — React + Vite + 정적 JSON + CSS.

- **React 18** — 화면을 컴포넌트로 분리
- **Vite 5** — 개발 서버·빌드
- **정적 JSON** — `src/data/articles.json` (별도 서버·DB 없음)
- **CSS** — 네이비 기반 공공 문서 톤, PC 넓은 화면 우선

> 협정이 25개로 늘어도 조항 데이터는 수십 건 수준이라 **정적 JSON으로 충분**하다. 백엔드·DB를 도입하지 않는다.

- **실행 환경:** 로컬 `npm run dev` + GitHub·Vercel 연결. `main` push 시 자동 재배포.

---

## 9. 데이터 준비 (대상 협정·출처·구조)

### 9-1. 대상 협정 목록 (발효 25개)

산업통상자원부 FTA 포털 "발효" 목록 24개 + **RCEP**(포털 발효 목록에 별도 표기되지 않으나 2022-02-01 발효, 관세청 FTA 포털 확인).

| # | 협정 (앱 표기) | 포털 코드 | 발효일 | 수록 |
|---|---------------|-----------|--------|:---:|
| 1 | 한-칠레 | `cl` | 2004-04 | ✅ (3주제 모두 조항 없음 확정) |
| 2 | 한-싱가포르 | `sg` | 2006-03 | |
| 3 | 한-EFTA | `efta` | 2006-09 | ✅ (3주제 모두 조항 없음 확정) |
| 4 | 한-ASEAN | `asean` | 2007-06 | ✅ (3주제 모두 조항 없음 확정) |
| 5 | 한-인도 | `in` | 2010-01 | |
| 6 | 한-EU | `eu` | 2011-07-01(잠정) / 2015-12-13(전체) | |
| 7 | 한-페루 | `pe` | 2011-08-01 | ✅ |
| 8 | 한-미 | `us` | 2012-03-15 (개정의정서 2019-01-01) | ✅ |
| 9 | 한-튀르키예 | `tr` | 2013-05-01 / 2018-08-01 | ✅ (3주제 모두 조항 없음 확정) |
| 10 | 한-호주 | `au` | 2014-12-12 | ✅ |
| 11 | 한-캐나다 | `ca` | 2015-01-01 | ✅ |
| 12 | 한-중 | `cn` | 2015-12-20 | ✅ |
| 13 | 한-뉴질랜드 | `nz` | 2015-12-20 | ✅ |
| 14 | 한-베트남 | `vn` | 2015-12-20 | ✅ |
| 15 | 한-콜롬비아 | `co` | 2016-07-15 | ✅ |
| 16 | 한-영국 | `uk` | 2021-01-01 | ✅ |
| 17 | 한-중미 | `c_america` | 2021-03-01 | ✅ |
| 18 | RCEP | (별도) | 2022-02-01 | ✅ |
| 19 | 한-이스라엘 | `il` | 2022-12-01 | ✅ |
| 20 | 한-캄보디아 | `kh` | 2022-12-01 | ✅ (3주제 모두 조항 없음 확정) |
| 21 | 한-인도네시아 | `id` | 2023-01-01 | ✅ |
| 22 | 한-싱 DPA | `ksdpa` | 2023-01-14 | ✅ |
| 23 | DEPA | `depa` | **2024-05-03** (한국 가입 발효) | ✅ |
| 24 | 한-필리핀 | `ph` | 2024-12-31 | ✅ |
| 25 | 한-UAE | `ucp` | 2026-05-01 | ✅ |

**진행 현황: 22 / 25 판정 완료(수록 17개 + 3주제 모두 없음 확정 5개), 3개 남음.** 총 43개 조항 수록 · 전부 포털 원문 대조 통과.

> **남은 3개(추출 품질 문제로 보류):** 한-EU(영문 추출 손상 — cross/border 등 문자 깨짐) · 한-싱가포르 FTA(국문 문장부호 흩어짐) · 한-인도(국문 쉼표 위치 깨짐). 조항 위치는 모두 특정했으나(한-EU 제7.48조 제3항·제7.49조 / 한-싱 제14.4조 제1항 / 한-인도 제5.5조), 추정 복원은 원문 무결성 규칙 위반이므로 수록 보류.

- **협정 표기:** 한글 약칭 사용. 데이터의 `agreement` 값과 `src/constants.js`의 `AGREEMENTS`가 **정확히 일치**해야 한다.
- **〔v2.0 중요〕 한-싱가포르 분리:** 1차의 `한-싱가포르`는 실제로 **한-싱 DPA** 조문이다. 이를 `한-싱 DPA`로 개명하고, 별도로 **한-싱가포르 FTA**(`sg`)를 새로 수록한다.
- **한-영국 유의:** 한-EU FTA 조문을 승계한 협정이라 한-EU와 조문이 유사할 수 있다. 각각 **자기 협정문 기준**으로 따로 수록한다.
- **오래된 협정 유의:** 한-칠레(2004)·한-EFTA(2006) 등 초기 협정은 전자상거래 장 자체가 없을 수 있다. 그런 경우 억지로 끼워 맞추지 않고 **"해당 조항 없음"으로 두는 것이 정답**이다.

### 9-2. 조항 데이터의 출처·확보 방법

**〔v2.0 전환〕 사용자 PDF 첨부 → 포털 직접 수집**

산업통상자원부 FTA 포털이 협정문을 **장(章)별 국문·영문 PDF**로 공개하며 주소 규칙이 일정하다는 것을 확인했다.

```
https://fta.motir.go.kr/webmodule/_PSD_FTA/{코드}/1/kor/CH_{n}_{제목}_kor.pdf   ← 국문
https://fta.motir.go.kr/webmodule/_PSD_FTA/{코드}/1/eng/CH_{n}_{제목}_eng.pdf   ← 영문
```

**수집 절차 (협정 1개당)**
1. 협정 페이지(`/ftamain/kfta/lov5/{코드}/2`)에서 장 목록과 PDF 링크를 얻는다.
2. 3개 주제에 해당하는 장을 찾는다. **장 위치는 협정마다 다르다** — 예: 한-중은 무관세·종이없는무역이 제13장(전자상거래), 특송화물은 제4장(통관절차·무역원활화).
3. 해당 장의 국문·영문 PDF를 내려받아 텍스트를 추출한다.
4. 조문 본문을 그대로 옮겨 적고, 출처·특이사항을 `source`에 기록한다.
5. **수록 후 원문과 대조 검증**한다(9-4).

**지켜야 할 출처 원칙 (1차와 동일, 절대 완화 금지)**
- **정부 공식 1차 출처에서 그대로 확보한 것만** 입력한다. **검색엔진 요약·2차 설명 자료를 원문으로 쓰지 않는다.**
- 확보·검증이 안 되면 값을 비워("번역 없음"/미입력) 둔다.
- 한국어본이 없으면 **임의 번역을 만들지 않고** "번역 없음"으로 명시한다.

### 9-3. 데이터 구조(스키마) — *변경 없음*

| 필드 | 설명 |
|------|------|
| `agreement` | 협정 한글 약칭 (`AGREEMENTS`와 정확히 일치) |
| `topic` | `customs_duties` / `express_shipments` / `paperless_trading` 중 1개. **협정 간 매칭 기준** |
| `article_no` | 조항 번호·제목 (표시용) |
| `text_en` | 영문 원문 |
| `text_ko` | 한국어본 (없으면 빈 값 → "번역 없음") |
| `source` | 출처·특이사항 (문서명·장·추출 경로·각주 유무 등) |
| `baseline_date` | 데이터 기준 시점(발효일·판본) |

### 9-4. 〔v2.0 신규〕 원문 수록 규칙과 검증 절차

협정 25개로 늘면서 반복 판단이 필요한 지점을 규칙으로 고정한다.

**(1) 각주 처리 — 조문 본문만 수록한다**
- 협정문에는 각주 번호가 **위첨자**로 붙는 경우가 많다. PDF에서 추출하면 이 숫자가 본문 흐름에 섞여 `…관행2을 유지할 것이다.3` 처럼 문장이 깨진다.
- 따라서 **각주 번호와 각주 본문은 `text_en`/`text_ko`에 넣지 않는다.** 조문 본문만 수록한다.
- 각주가 존재한다는 사실은 **`source`에 기록**한다. (예: "원문에 각주 3개가 달려 있으나 조문 본문만 수록함")
- 근거: 각주 번호는 조문의 문장 내용이 아니라 참조 표시이며, 그대로 끼워 넣으면 오히려 원문을 왜곡한다. "메모는 `source`에만"이라는 기존 규칙과도 일치한다.

**(2) 조문 부재·간접 규정 처리**
- 해당 주제의 **독립 조문이 없으면** 그 협정·주제 레코드를 만들지 않는다 → 화면에 "해당 조항 없음"으로 표시된다.
- **독립 조문은 없으나 다른 조항의 특정 항에 규정된 경우**(예: 한-영국 제7.48조 제3항)에는 수록하되 `article_no`에 **해당 항까지 명시**한다.
- 판단이 애매하면 **임의로 결정하지 않고 사용자에게 확인**을 요청한다.

**(3) 대조 검증 (수록 후 필수)**
- 수록한 `text_en`/`text_ko`가 **포털 원문 PDF 추출본에 그대로 포함되는지** 프로그램으로 대조한다.
- 비교 시 공백·줄바꿈과 **숫자만 있는 줄**(각주 번호·페이지 번호)만 제거하고, 글자는 그대로 비교한다.
- **전 조항이 통과해야** 수록 완료로 본다. 불일치가 나오면 원인을 확인해 바로잡는다.

---

## 10. 예외 상황 처리 원칙

1차에서 확정한 동작을 그대로 유지한다.

| 상황 | 표시 위치 | 문구 |
|------|-----------|------|
| 협정 0개 선택 | **화면 전체 대체**(`EmptyState`) | **"비교할 협정을 선택하세요."** |
| 협정 1개 선택 | 비교 뷰 | (안내 없이 한 칸만 정상 표시) |
| 해당 조항 없음 | **그 협정 칸 안** | **"해당 조항 없음"** |
| 한국어본 없음 | 그 칸의 KO 자리 | **"번역 없음"** |
| 검색 0건 | **화면 전체 대체**(`EmptyState`) | **"검색 결과가 없습니다. (검색어: "○○○")"** |

- 칸 내부 표시와 화면 전체 대체는 **서로 다른 층위**이므로 섞지 않는다.
- 검색어 공백 정책: 앞뒤 공백은 trim, 단어 사이 공백은 그대로 매칭.
- **〔v2.0 신규〕 협정 이름 검색 결과 0건:** 일치하는 협정이 없으면 **협정 목록 자리에만** "일치하는 협정이 없습니다."를 표시한다. 이미 선택한 협정의 비교 뷰는 **그대로 유지**한다(화면 전체를 대체하지 않는다).

---

## 11. 개발 단위 (2차 사이클 작업 순서)

| # | 작업 | 상태 |
|---|------|:---:|
| 1 | 확장 범위 확정 + 데이터 수집 방식 검증 (시범 1건: 한-중) | ✅ 완료 |
| 2 | **PRD v2.0** (이 문서) | ✅ 완료 |
| 3 | PLAN v2.0 · DESIGN v2.0 — 25개 협정 선택 UI 설계, 작업 분해 | |
| 4 | 협정 선택 UI 개편 구현 (이름 검색 · "있음만 보기" · 선택 유지) | |
| 5 | 조항 유무 표시 개편 구현 (25개 대응 · 보유 개수 요약 · 비교 연결) | |
| 6 | 한-싱가포르 FTA / 한-싱 DPA 분리 | |
| 7 | 나머지 협정 데이터 수집·수록 (20개) | |
| 8 | 전 조항 원문 대조 검증 | |
| 9 | Check — 갭 분석 · 보안 점검 · 무회귀 확인 | |
| 10 | Act — 발견된 문제 수정 | |
| 11 | 배포(`main` push → 자동 재배포) 및 문서 최종 갱신 | |

> **데이터(7번)보다 UI(4·5번)를 먼저** 한다. 25개가 들어가도 화면이 무너지지 않는지 확인한 뒤 데이터를 채워야, 나중에 대량 수정하는 일을 피할 수 있다. UI 검증에는 이미 수록된 5개 + 더미 없이 실제 데이터로 확인한다.

---

*작성 근거: 1차 사이클 산출물(PRD v1.1 · CHECK v1.1) · 산업통상자원부 FTA 포털 발효 협정 목록 및 협정문 · 관세청 FTA 포털 발효일 · 한-중 FTA 시범 수집 결과(2026-07-25).*
