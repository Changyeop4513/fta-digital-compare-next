# PLAN.md — FTA 디지털협정 조항 비교 서비스

> 출처: [PRD.md](PRD.md) v2.0 (작성자: 이창엽)
> bkit PDCA `plan` 단계 산출물. PRD v2.0에 정의된 **2차 사이클(협정 전면 확대)** 작업 계획서.
> **최종 개정: 2026-07-25 (v2.0)** — 1차 사이클 계획(v1.1)은 완료돼 [원본 저장소](https://github.com/Changyeop4513/fta-digital-compare)에 보존돼 있고, 이 문서는 **2차 사이클 계획으로 새로 작성**했다.

---

## 이번 사이클 목표

주제 3개는 **그대로 두고**, 대상 협정을 **4개 → 한국이 체결한 발효 협정 전체 25개**로 확대한다.

핵심은 두 가지다.

1. **데이터** — 25개 협정 × 3개 주제를 빠짐없이 판정한다. 조항이 없는 협정은 "해당 조항 없음"으로 확정 표시하고, **미수록 상태로 남기지 않는다.**
2. **UI** — 협정 25개를 다룰 수 있는 선택·탐색 수단을 만든다. 지금의 "한 줄 체크박스 나열"은 25개에서 무너진다.

> **동시 비교는 최대 4개를 그대로 유지한다.** 협정 수가 늘어난 것은 *고를 수 있는 후보*가 늘어난 것이며, 한 화면에 4개 이상을 늘어놓으면 원문 가독성이 무너진다는 1차 판단은 여전히 유효하다.

---

## 성공 기준 (이 사이클이 "됐다"의 정의)

| # | 기준 | 판정 방법 |
|---|------|-----------|
| ① | **커버리지 100%** — 25개 협정 × 3개 주제 전부 판정 완료 (수록 또는 "없음" 확정) | 데이터 집계로 25×3 = 75칸이 모두 결정됐는지 확인 |
| ② | **원문 무결성 100%** — 수록 전 조항의 영·한이 포털 원문과 일치 | 대조 검증 스크립트 전 조항 통과 |
| ③ | **탐색 3회 이내** — 25개 중 원하는 협정을 3회 조작 안에 찾아 선택 | 브라우저에서 실제 조작 수 확인 |
| ④ | **가독성 유지** — 동시 비교 최대 4개, 컨트롤 바가 화면을 덮지 않음 | 25개 상태에서 화면 높이·스크롤 확인 |
| ⑤ | **무회귀** — 1차 기능(비교·검색·하이라이트·예외 5종·보안 헤더 5종) 정상 | 1차 CHECK 항목 재확인 |
| ⑥ | **동작** — `npm run dev` / 배포본에서 오류 없이 작동, 콘솔 에러 0건 | 로컬 + 배포 사이트 확인 |

---

## 해야 할 작업 (만들 순서대로)

> **원칙: UI를 먼저, 데이터를 나중에.** 협정 25개가 들어가도 화면이 무너지지 않는지 확인한 뒤 데이터를 채워야, 나중에 대량으로 되돌리는 일을 피할 수 있다.

### A. 준비 (완료)

1. ~~확장 범위 확정 — 포털 발효 목록 24개 + RCEP = **25개**로 확정~~ ✅
2. ~~데이터 수집 방식 검증 — 포털 장별 PDF 직접 수집 + 텍스트 추출이 가능함을 **시범 1건(한-중)으로 실증**~~ ✅
3. ~~PRD v2.0 작성 (각주 규칙·UI 요구사항·협정 목록 확정)~~ ✅

### B. UI 개편 (데이터보다 먼저)

4. **협정 선택 UI 개편** — 선택된 협정은 칩으로 항상 노출하고, "협정 추가" 패널 안에 **이름 검색**과 **"조항 있는 협정만 보기"**를 둔다. 최대 4개 제한과 선택 순서 유지는 그대로.
5. **조항 유무 표시 개편** — **보유 개수 요약**(`무관세 — 25개 중 N개 보유`)을 넣고, 기본은 "있음"만 노출하며 "없음"은 접어 둔다(펼쳐 확인 가능). **배지에서 바로 비교 대상에 추가**할 수 있게 한다.
6. **25개 상태로 UI 검증** — `AGREEMENTS`에 25개를 모두 등록해(데이터는 아직 5개뿐인 상태) 화면이 견디는지 확인한다. 이 시점엔 대부분이 "없음"으로 표시되는 것이 정상이다.

### C. 데이터 수집 (UI 확인 후)

7. **한-싱가포르 분리** — 현재 `한-싱가포르`(실제로는 DPA 조문)를 `한-싱 DPA`로 개명하고, **한-싱가포르 FTA**(`sg`)를 새로 수집·수록한다.
8. **나머지 협정 수집·수록 (19개)** — 협정 1개당: 장 목록 확인 → 3개 주제 해당 장 찾기 → 국·영문 PDF 내려받기 → 조문 추출 → 수록 → 대조 검증. 조항이 없으면 "없음"으로 확정한다.
9. ~~**전 조항 원문 대조 검증**~~ ✅ 44건 전수 통과 (자동 33 · 사용자 확인 제공본 11 · 불일치 0)

### D. 점검·마무리

10. ~~**Check**~~ ✅ 성공 기준 **6/6 통과** · 보안 Critical/High 0건 · 무회귀 전항목 정상 ([CHECK.md](CHECK.md) v2.0)
11. ~~**Act**~~ ✅ 발견 6건 전부 조치 (판정 기준 통일 · 각주 소급 정리 7건 · 선택 유실 버그 · 검증기 결함 등)
12. **배포·문서 갱신** — `main` push(자동 재배포) 후 DESIGN·PLAN·CHECK·CLAUDE·README를 as-built로 갱신한다.

---

## 작업 분해 — 데이터 수집(8번) 세부

협정 19개를 순서 없이 처리하면 진행 상황을 놓친다. **디지털 통상 조항이 있을 가능성이 높은 최근 협정부터** 처리해 초기에 가치를 확보한다.

| 우선순위 | 협정 | 이유 |
|:---:|------|------|
| **1군** (최근·디지털 조항 유력) | DEPA · 한-UAE · 한-필리핀 · 한-인도네시아 · 한-캄보디아 · 한-이스라엘 | 최근 체결로 전자상거래·디지털 장이 있을 가능성이 높다 |
| **2군** (중기) | 한-중미 · 한-콜롬비아 · 한-베트남 · 한-뉴질랜드 · 한-캐나다 · 한-호주 · 한-튀르키예 | 전자상거래 장을 둔 경우가 많다 |
| **3군** (초기·조항 부재 가능) | 한-EU · 한-페루 · 한-인도 · 한-ASEAN · 한-EFTA · 한-칠레 · 한-싱가포르 | 2000년대 협정으로 전자상거래 장이 없을 수 있다. **"없음" 확정도 결과다** |

각 협정 처리 시 기록할 것: 조항 위치(장·조), 발효일, 각주 유무, 특이사항 → 모두 `source`/`baseline_date`에 반영.

---

## 지켜야 할 핵심 제약 (작업 내내)

1차와 동일하며, **하나도 완화하지 않는다.**

- 원문(`text_en`/`text_ko`)을 **절대 요약·수정·의역하지 않는다.** 표시만 한다.
- **AI 요약·해설 기능을 만들지 않는다.** 원문 비교와 키워드 검색만.
- 원문은 **정부 공식 1차 출처에서 그대로 확보한 것만** 입력한다. 검색엔진 요약·2차 자료 금지.
- **각주 번호·각주 본문은 원문 필드에 넣지 않는다.** 조문 본문만 수록하고, 각주 존재 사실은 `source`에 적는다.
- 주제는 **3개로 고정**, 임의 추가·변경 금지.
- 한 화면 **최대 4개 협정**까지만 나란히.
- 조항 보유 판정은 **데이터의 `topic` 태그 그대로만** — 임의 추론 금지.
- 조문 위치 판단이 애매하면 **혼자 결정하지 않고 사용자에게 확인**한다.
- 보안 응답 헤더 5종을 **완화하지 않는다.** 비밀 정보는 커밋하지 않는다.
- 매 단계 [CLAUDE.md](CLAUDE.md)의 **검증 루프**(변경 → 브라우저로 직접 확인 → 코드 리뷰 → 통과 시 한 줄 요약)를 돈다.

---

## 범위 밖 (이번에 만들지 않음)

- 협정 개정 이력·최신 버전 **자동 추적** (단, `baseline_date` 라벨 표시는 함)
- AI 요약·해설 / 재경부 소관 3개 주제 외 조항 / 사용자의 원문 직접 편집 / 계정 기능
- **서명·타결 단계 협정 6개**(한-EU DTA·한-말레이시아·한-GCC·한-에콰도르·한-조지아·한-세르비아) — 발효 전이라 문안 변동 가능
- 비교 결과 내보내기(인쇄·PDF), 조항 출처 화면 표시 — PRD의 nice 기능
- 모바일 최적화

---

## 위험과 대응

| 위험 | 대응 |
|------|------|
| **협정마다 조항 위치가 달라** 장을 잘못 짚을 수 있다 | 장 목록을 먼저 확보해 확인하고, 조문 제목까지 대조. 애매하면 사용자에게 확인 |
| **오래된 협정에 조항이 아예 없다** | "없음" 확정도 정당한 결과다. 억지로 유사 조항을 끼워 넣지 않는다 |
| **PDF 추출 품질 문제**(각주 섞임·줄바꿈) | 각주 규칙(PRD 9-4)을 적용하고, 수록 후 **대조 검증 스크립트로 반드시 확인** |
| **UI가 25개를 못 버틴다** | 데이터보다 UI를 먼저 만들고 25개 상태에서 검증(6번) |
| **한-EU와 한-영국 조문 혼동** | 승계 관계지만 각자 협정문 기준으로 따로 수집·기록 |
| 작업량이 많아 **중간 상태로 방치** | 협정 1개 단위로 수록→검증→커밋을 끝내 항상 일관된 상태를 유지 |

---

## 다음 단계

- 이 계획 확정 후 → [DESIGN.md](DESIGN.md) v2.0에서 **협정 선택 UI·조항 유무 표시 화면 설계**를 확정
- 이어서 작업 4번(협정 선택 UI 개편)부터 구현 착수
