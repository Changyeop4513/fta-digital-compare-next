# PLAN.md — FTA 디지털협정 조항 비교 서비스

> 출처: [PRD.md](PRD.md) v2.1 (작성자: 이창엽)
> bkit PDCA `plan` 단계 산출물. PRD v2.1에 정의된 **2차 사이클(협정 전면 확대)** 작업 계획서.
> **최종 개정: 2026-07-25 (v2.1)** — 2차 사이클이 **완료**되어 계획서를 **as-built(실제 구현 결과)로 갱신**했다. v2.0(계획 시점) 대비 바뀐 것: ① 대상 협정 **25개 → 24개**(한-싱가포르 FTA 대상 제외) ② 협정 표기를 **상대국명만**으로 통일 ③ 작업 1~13번 **전부 완료 표시 + 실제 결과 수치** 반영 ④ **계획 외 추가 수행**과 **표기·UI 다듬기 단계** 추가 ⑤ "다음 단계"를 실제 남은 항목으로 교체. 1차 사이클 계획(v1.1)은 [원본 저장소](https://github.com/Changyeop4513/fta-digital-compare)에 보존돼 있다.

---

## 이번 사이클 목표

주제 3개는 **그대로 두고**, 대상 협정을 **4개 → 한국이 체결한 발효 협정 전체**로 확대한다.

범위는 처음 **포털 발효 목록 24개 + RCEP = 25개**로 산정했으나, 작업 중 **한-싱가포르 FTA를 대상에서 제외**해 **최종 24개**로 확정했다. (국문 추출 품질이 나빠 원문 무결성을 지킬 수 없다고 판단, 사용자 결정으로 제외. 한-싱가포르의 디지털 통상 조항은 **싱가포르 DPA**가 담고 있어 커버리지 공백은 없다.) 〔v2.1 확정: **24개**〕

핵심은 두 가지다.

1. **데이터** — 24개 협정 × 3개 주제를 빠짐없이 판정한다. 조항이 없는 협정은 "해당 조항 없음"으로 확정 표시하고, **미수록 상태로 남기지 않는다.**
2. **UI** — 협정 24개를 다룰 수 있는 선택·탐색 수단을 만든다. 지금의 "한 줄 체크박스 나열"은 24개에서 무너진다.

> **동시 비교는 최대 4개를 그대로 유지한다.** 협정 수가 늘어난 것은 *고를 수 있는 후보*가 늘어난 것이며, 한 화면에 4개 이상을 늘어놓으면 원문 가독성이 무너진다는 1차 판단은 여전히 유효하다.

---

## 성공 기준 (이 사이클이 "됐다"의 정의)

| # | 기준 | 판정 방법 |
|---|------|-----------|
| ① | **커버리지 100%** — 24개 협정 × 3개 주제 전부 판정 완료 (수록 또는 "없음" 확정) | 데이터 집계로 24×3 = 72칸이 모두 결정됐는지 확인 |
| ② | **원문 무결성 100%** — 수록 전 조항의 영·한이 포털 원문과 일치 | 대조 검증 스크립트 전 조항 통과 |
| ③ | **탐색 3회 이내** — 24개 중 원하는 협정을 3회 조작 안에 찾아 선택 | 브라우저에서 실제 조작 수 확인 |
| ④ | **가독성 유지** — 동시 비교 최대 4개, 컨트롤 바가 화면을 덮지 않음 | 24개 상태에서 화면 높이·스크롤 확인 |
| ⑤ | **무회귀** — 1차 기능(비교·검색·하이라이트·예외 5종·보안 헤더 5종) 정상 | 1차 CHECK 항목 재확인 |
| ⑥ | **동작** — `npm run dev` / 배포본에서 오류 없이 작동, 콘솔 에러 0건 | 로컬 + 배포 사이트 확인 |

---

## 해야 할 작업 (만들 순서대로) 〔v2.1 — 2차 사이클 전부 완료 ✅〕

> **원칙: UI를 먼저, 데이터를 나중에.** 협정 24개가 들어가도 화면이 무너지지 않는지 확인한 뒤 데이터를 채워야, 나중에 대량으로 되돌리는 일을 피할 수 있다.

### A. 준비 (완료)

1. ~~확장 범위 확정 — 포털 발효 목록 24개 + RCEP = **25개**로 확정~~ ✅ → 이후 **한-싱가포르 FTA 대상 제외**로 **최종 24개** 확정
2. ~~데이터 수집 방식 검증 — 포털 장별 PDF 직접 수집 + 텍스트 추출이 가능함을 **시범 1건(중국)으로 실증**~~ ✅
3. ~~PRD v2.0 작성 (각주 규칙·UI 요구사항·협정 목록 확정)~~ ✅ → 완료 후 **PRD v2.1**로 as-built 갱신

### B. UI 개편 (데이터보다 먼저)

4. ~~**협정 선택 UI 개편**~~ ✅ 컨트롤 바에 **선택 칩**(`✕`로 해제) + **`＋ 협정 추가` 패널** 구현 — 패널 안에 **협정 이름 부분 일치 검색**과 **"조항 있는 협정만 보기"(기본 켜짐)**, 바깥 클릭·`Esc`로 닫힘. **최대 4개** 제한과 선택 순서 유지 그대로.
5. ~~**조항 유무 표시 개편**~~ ✅ 보유 개수 요약을 `「Customs Duties(관세)」 — 24개 중 15개 보유` 형태로 표시하고, "없음" 협정은 `▸ 조항 없는 협정 N개 보기` 접기 안에 둔다(무관세 9 · 특송화물 9 · 종이 없는 무역 10). **조항 유무 배지 클릭으로 바로 비교 대상에 추가** 가능.
6. ~~**전체 협정 등록 상태로 UI 검증**~~ ✅ `AGREEMENTS`에 전 협정을 등록한 상태(데이터는 아직 일부뿐)로 화면이 견디는지 확인 — 대부분이 "없음"으로 표시되는 것이 정상임을 확인하고 통과.

### C. 데이터 수집 (UI 확인 후)

7. ~~**한-싱가포르 분리**~~ ✅ 결과: 기존 `한-싱가포르`(실제로는 DPA 조문)를 **`싱가포르 DPA`로 개명**하고, **한-싱가포르 FTA는 대상에서 제외**했다 — 국문 추출 품질이 나빠 원문 무결성을 지킬 수 없어 **신규 수록하지 않음**(사용자 결정). 디지털 통상 조항은 싱가포르 DPA가 담고 있다.
8. ~~**나머지 협정 수집·수록**~~ ✅ (계획 19개 → 한-싱가포르 FTA 제외 확정 후 **24개 전 협정 판정 완료**) 협정 1개당: 장 목록 확인 → 3개 주제 해당 장 찾기 → 국·영문 PDF 내려받기 → 조문 추출 → 수록 → 대조 검증. 결과 **데이터 44건**(무관세 15 · 특송화물 15 · 종이 없는 무역 14), **조항 보유 19개 협정** / **3주제 모두 "없음" 확정 5개**(칠레 · EFTA · ASEAN · 튀르키예 · 캄보디아), **24 × 3 = 72칸 전부 판정**.
9. ~~**전 조항 원문 대조 검증**~~ ✅ 44건 전수 통과 (자동 33 · 사용자 확인 제공본 11 · 불일치 0)

### D. 표기·UI 다듬기 〔v2.1 추가 — 계획 외〕

10. ~~**표기·UI 다듬기**~~ ✅ ① 조항 유무 배지에서 옛 **`있음 ＋` 꼬리표 제거** → **협정 이름만** 표시 ② 협정명을 **상대국명만**으로 통일(`한-` 접두사 제거) ③ 비교 뷰 **칸 머리에만 정식 명칭** 표시(`src/constants.js`의 `AGREEMENT_FULL_NAMES` — 예: `한-미국 FTA` · `한-EU FTA` · `한-인도 CEPA` · `RCEP (역내포괄적경제동반자협정)`).
    - 경위: 이 과정에서 `＋ 협정 추가` 패널을 제거해 봤으나 **배지만으로는 현재 주제에 조항이 없는 협정을 새로 고를 수 없다**는 제약이 드러나 **사용자 결정으로 원복**(패널 유지)했다.

### E. 점검·마무리

11. ~~**Check**~~ ✅ 성공 기준 **6/6 통과** · 보안 Critical/High 0건 · 무회귀 전항목 정상 ([CHECK.md](CHECK.md))
12. ~~**Act**~~ ✅ 발견 6건 전부 조치 (판정 기준 통일 · 각주 소급 정리 7건 · 선택 유실 버그 · 검증기 결함 등)
13. ~~**배포·문서 갱신**~~ ✅ **https://fta-digital-compare-next.vercel.app** 배포(`main` push 시 자동 재배포) · 보안 응답 헤더 5종 적용(`vercel.json` 5종 / `vite.config.js`는 dev 4종 · preview 5종 — CSP는 HMR 충돌 때문에 preview 전용) · PRD·DESIGN·PLAN·CHECK·CLAUDE·README를 as-built로 갱신.

---

## 계획 외 추가 수행 〔v2.1〕

계획서에 없었지만 작업 중 필요해져 실제로 수행한 것들이다.

- **각주 처리 규칙 확정** — 각주 번호·본문은 원문 필드에 넣지 않고, 각주 존재 사실만 `source`에 적는다.
- **간접 규정 엄격 판정 기준 신설** — 실질 의무가 있으면 수록하고, 대화 주제·맥락 언급뿐이면 **"없음"**으로 판정한다. 1차 사이클 기록도 이 기준으로 **소급 정정**했다.
- **텍스트 추출 불량 협정 3건**(칠레 · 인도 · EU) — 자동 추출로 원문 무결성을 담보할 수 없어 **사용자가 원문을 직접 확인해 제공**한 값을 수록했다.
- **협정 표기 규칙 신설** — 목록·칩·배지는 **상대국명만**(`한-` 접두사 금지), 비교 뷰 **칸 머리에만 정식 명칭**.
- **표기·UI 다듬기 단계**(위 10번) — 배지 꼬리표 제거·협정명 통일·정식 명칭 표시.

---

## 작업 분해 — 데이터 수집(8번) 세부 〔v2.1 — 1·2·3군 전부 완료 ✅〕

협정을 순서 없이 처리하면 진행 상황을 놓친다. **디지털 통상 조항이 있을 가능성이 높은 최근 협정부터** 처리해 초기에 가치를 확보한다.

| 우선순위 | 협정 | 이유 |
|:---:|------|------|
| **1군** (최근·디지털 조항 유력) ✅ | DEPA · UAE · 필리핀 · 인도네시아 · 캄보디아 · 이스라엘 | 최근 체결로 전자상거래·디지털 장이 있을 가능성이 높다 |
| **2군** (중기) ✅ | 중미 · 콜롬비아 · 베트남 · 뉴질랜드 · 캐나다 · 호주 · 튀르키예 | 전자상거래 장을 둔 경우가 많다 |
| **3군** (초기·조항 부재 가능) ✅ | EU · 페루 · 인도 · ASEAN · EFTA · 칠레 | 2000년대 협정으로 전자상거래 장이 없을 수 있다. **"없음" 확정도 결과다** |

> **〔v2.1〕** 실제 진행은 **1군 → 2군 → 3군** 순으로 나눠 수집했고 **전부 완료**했다. 3군은 처음 7개였으나 **한-싱가포르 FTA가 대상에서 제외**되어(위 7번) **6개**가 되었다.
>
> **셈 맞추기 (24개):** 1군 6 + 2군 7 + 3군 6 = **19개**, 여기에 수집 방식을 실증한 **시범 1건(중국)**, 그리고 1차 사이클에서 이미 수록해 둔 **4개**(싱가포르 DPA · RCEP · 영국 · 미국)를 더해 **24개**가 된다. 협정 표기는 확정 규칙대로 **상대국명만** 쓴다.

각 협정 처리 시 기록할 것: 조항 위치(장·조), 발효일, 각주 유무, 특이사항 → 모두 `source`/`baseline_date`에 반영.

---

## 지켜야 할 핵심 제약 (작업 내내)

1차와 동일하며, **하나도 완화하지 않는다.**

- 원문(`text_en`/`text_ko`)을 **절대 요약·수정·의역하지 않는다.** 표시만 한다.
- **AI 요약·해설 기능을 만들지 않는다.** 원문 비교와 키워드 검색만.
- 원문은 **정부 공식 1차 출처에서 그대로 확보한 것만** 입력한다. 검색엔진 요약·2차 자료 금지.
- **각주 번호·각주 본문은 원문 필드에 넣지 않는다.** 조문 본문만 수록하고, 각주 존재 사실은 `source`에 적는다.
- 주제는 **3개로 고정**, 임의 추가·변경 금지.
- 한 화면 **최대 4개 협정**까지만 나란히.
- 조항 보유 판정은 **데이터의 `topic` 태그 그대로만** — 임의 추론 금지.
- 조문 위치 판단이 애매하면 **혼자 결정하지 않고 사용자에게 확인**한다.
- 보안 응답 헤더 5종을 **완화하지 않는다.** 비밀 정보는 커밋하지 않는다.
- 매 단계 [CLAUDE.md](CLAUDE.md)의 **검증 루프**(변경 → 브라우저로 직접 확인 → 코드 리뷰 → 통과 시 한 줄 요약)를 돈다.

---

## 범위 밖 (이번에 만들지 않음)

- 협정 개정 이력·최신 버전 **자동 추적** (단, `baseline_date` 라벨 표시는 함)
- AI 요약·해설 / 재경부 소관 3개 주제 외 조항 / 사용자의 원문 직접 편집 / 계정 기능
- **서명·타결 단계 협정 6개**(EU DTA · 말레이시아 · GCC · 에콰도르 · 조지아 · 세르비아) — 발효 전이라 문안 변동 가능
- 비교 결과 내보내기(인쇄·PDF), 조항 출처 화면 표시 — PRD의 nice 기능
- 모바일 최적화

---

## 위험과 대응

| 위험 | 대응 |
|------|------|
| **협정마다 조항 위치가 달라** 장을 잘못 짚을 수 있다 | 장 목록을 먼저 확보해 확인하고, 조문 제목까지 대조. 애매하면 사용자에게 확인 |
| **오래된 협정에 조항이 아예 없다** | "없음" 확정도 정당한 결과다. 억지로 유사 조항을 끼워 넣지 않는다 |
| **PDF 추출 품질 문제**(각주 섞임·줄바꿈) | 각주 규칙(PRD 9-4)을 적용하고, 수록 후 **대조 검증 스크립트로 반드시 확인** |
| **UI가 24개를 못 버틴다** | 데이터보다 UI를 먼저 만들고 전 협정 등록 상태에서 검증(6번) |
| **EU와 영국 조문 혼동** | 승계 관계지만 각자 협정문 기준으로 따로 수집·기록 |
| 작업량이 많아 **중간 상태로 방치** | 협정 1개 단위로 수록→검증→커밋을 끝내 항상 일관된 상태를 유지 |

---

## 다음 단계 〔v2.1 갱신 — 2차 사이클 종료, 아래는 다음 사이클 후보〕

착수 시 이 목록을 새로 작성한다.

- **비교 결과 내보내기** (인쇄 · PDF)
- **조항 출처(`source`) 화면 표시**
- **서명했으나 미발효인 협정 대응** (발효 시 편입 방식 결정)
