# PLAN.md — FTA 디지털협정 조항 비교 서비스

> 출처: [PRD.md](PRD.md) (작성자: 이창엽, 2026-07-22)
> bkit PDCA `plan` 단계 산출물. PRD에 정의된 기능 **전체**를 만들기 위한 작업 계획서.
> **최종 개정: 2026-07-24 (v1.1 — 진행 결과 반영본).** 계획 원문은 보존하고, 완료 여부와 계획에 없던 추가 수행분을 **〔v1.1〕**로 표기했다. (같은 방식의 갱신: [PRD.md](PRD.md) 개정 이력 · [DESIGN.md](DESIGN.md) v1.1 참조)

## 이번 사이클 목표

PRD의 **3대 필수 기능**을 로컬(내 PC)에서 실제로 동작하는 **React + Vite 앱**으로 완성한다.

1. 조항별 협정 비교 뷰 (주제 3개 × 협정 1~4개, 영문 원문 + 한국어본 나란히)
2. 주제·협정 선택 필터 + 원문 내 키워드 검색·하이라이트
3. 주제별 조항 보유 협정 자동 찾기 (주제 선택 시 협정별 있음/없음 자동 표시)

> 데이터는 **최소 샘플(협정 2개 × 주제 1개)**로 먼저 화면·기능을 완성한 뒤, 마지막에 나머지를 채운다.

**〔v1.1 결과〕 목표 달성 — 그리고 확장.** 3대 기능이 모두 동작하는 앱을 완성했고, 당초 목표(로컬)를 넘어 **웹 배포까지 수행**했다 (https://fta-digital-compare.vercel.app · [PRD.md](PRD.md) 8번). 데이터도 최소 샘플 전략대로 진행한 뒤 최종 **11개 조항 전체**를 채웠다.

## 성공 기준 (이 사이클이 "됐다"의 정의)

- **원문 무결성:** 화면에 표시되는 조항 원문이 준비된 원본과 **100% 일치**(요약·수정 없음), 영문–한국어본 짝 표시 누락 **0건**.
- **처리 시간:** 세 주제 중 하나에 대해 협정 3~4개 비교를 **3분 이내**에 마칠 수 있다.
- **커버리지:** 3개 주제 × 대상 협정 전체 조항 **수록률 100%**(원문이 없으면 "번역 없음"·"해당 조항 없음"으로 명시).
- **재사용성:** 다른 담당자가 동일 비교를 재현하는 준비 시간 **0분**(선택만 하면 즉시 동일 화면).
- **동작:** `npm run dev`로 로컬에서 3대 기능이 오류 없이 모두 작동한다.

**〔v1.1 판정 결과 — 5 / 5 통과〕** (상세 근거: [CHECK.md](CHECK.md))

| 성공 기준 | 판정 |
|-----------|:----:|
| ① 원문 무결성 — 11개 조항 영·한 전부 산업통상부 FTA 포털 원문과 **글자 단위 일치** 검증 | ✅ |
| ② 처리 시간 — 선택 즉시 좌우 비교 렌더 | ✅ |
| ③ 커버리지 — **11/11 조항** 영·한 정본 완비 (한-영국 특송은 실제 조항 부재 → "해당 조항 없음" 정확 표시) | ✅ |
| ④ 재사용성 — 선택만 하면 동일 화면 재현 | ✅ |
| ⑤ 동작 — 3대 기능 정상, 콘솔 에러 0건 (배포본에서도 재확인) | ✅ |

## 해야 할 작업 (만들 순서대로)

> 원칙: **먼저 되면 나머지가 쉬워지는 것**부터. 토대 → 데이터 → 화면 연결 → 선택 → 비교 → 검색 → 마무리.

1. React + Vite 프로젝트 기본 틀을 만들어 빈 화면이 브라우저에 뜨게 한다.
2. 조항 데이터 스키마(`agreement`/`topic`/`article_no`/`text_en`/`text_ko`/`source`/`baseline_date`)를 확정한다.
3. 최소 샘플 JSON(협정 2개 × 주제 1개)을 스키마에 맞춰 작성한다.
4. JSON 데이터를 앱으로 불러와 화면에 목록으로 뿌려 데이터 연결을 확인한다.
5. 주제 선택 UI를 만든다(무관세 / 특송화물 / 종이 없는 무역 중 1개, 3개 고정).
6. 주제를 고르면 각 협정의 조항 보유 현황(있음/없음)을 `topic` 태그 기준으로 자동 표시한다. **(기능 3)**
7. 협정 선택 UI를 만든다(체크박스, 최대 4개·최소 1개, 주제를 바꿔도 선택 유지).
8. 선택한 협정을 좌우로 나란히 표로 보여주는 조항별 비교 뷰를 만든다(선택 순서 유지). **(기능 1)**
9. 각 칸에 영문 원문 + 한국어본을 한 쌍으로 표시하고, 한쪽만 있으면 "번역 없음"으로 명시한다.
10. 같은 주제의 조항이 여러 개면 조항 번호 순으로 누락 없이 표시하고, 긴 원문은 "더보기"로 접었다 편다.
11. 각 조항에 데이터 기준 시점(`baseline_date`) 라벨을 함께 표시한다.
12. 선택 범위 안에서 키워드 검색·하이라이트를 만든다(영/한 대상, 부분 일치, 영문 대소문자 무시, 요약·해석 없음). **(기능 2)**
13. 예외 처리를 붙인다(협정 1개 선택 시 안내 없이 한 칸 표시 / 협정 0개 선택 안내 / 협정 칸 안 "해당 조항 없음" 구분 표시 / 검색 0건 안내).
14. 네이비 기반 공공 문서 톤으로 디자인을 입힌다(PC 넓은 화면 우선).
15. 나머지 협정·주제 데이터를 모두 채우고 성공 기준으로 최종 점검한다.

**〔v1.1 진행 결과〕 1~15번 전부 완료 ✅** — 각 단계는 [CLAUDE.md](CLAUDE.md)의 검증 루프(변경 → 브라우저 확인 → 코드 리뷰 → 한 줄 요약)를 거쳤다.

**〔v1.1 추가 수행 — 계획에 없던 작업〕**

| 추가 작업 | 내용 | 상태 |
|-----------|------|:---:|
| 16. 설계 정합(gap 해소) | 검색 0건을 **화면 전체 대체**로, `EmptyState` **컴포넌트 분리**, 주제 UI를 **네이티브 라디오**로 — 설계 쪽에 맞춰 코드 정합 | ✅ |
| 17. UX 보완 | 검색어 포함 조항 **자동 펼침**, 컨트롤 바 **상단 고정(sticky)**, 헤더 로고·그라디언트 개선 | ✅ |
| 18. 원문 검증 | 산업통상부 FTA 포털 공식 PDF와 **11/11 조항 영·한 대조 — 글자 단위 일치** 확인 | ✅ |
| 19. 보안 적용 | 보안 응답 헤더 5종(`vite.config.js` + `vercel.json`), 보안 점검 Critical/High/Medium 0건 | ✅ |
| 20. 배포 | GitHub 공개 저장소 + **Vercel 프로덕션 배포** (`main` push 시 자동 재배포) | ✅ |
| 21. 문서화 | README 상세화 + 기획·설계·점검 문서 v1.1(as-built) 갱신·공개 | ✅ |

## 지켜야 할 핵심 제약 (작업 내내)

- 원문(`text_en`/`text_ko`)을 **절대 요약·수정·의역하지 않는다.** 표시만 한다.
- **AI 요약·해설 기능을 만들지 않는다.** 원문 비교와 키워드 검색만.
- 주제는 **3개로 고정**, 임의 추가·변경 금지.
- 한 화면 **최대 4개 협정**까지만 나란히.
- 조항 보유(있음/없음) 판정은 **데이터의 `topic` 태그 그대로만** — 조항 유무를 임의로 추론하지 않는다.
- 로그인·회원가입·즐겨찾기·개정 자동 추적은 **이번 범위 밖**.

## 범위 밖 (이번에 만들지 않음)

- 협정 개정 이력·최신 버전 자동 추적 (단, `baseline_date` 라벨 표시는 함)
- AI 요약·해설 / 재경부 소관 3개 주제 외 조항 / 사용자의 원문 직접 편집 / 계정 기능

## 다음 단계

- 이 계획 확정 후 → bkit PDCA `design` 단계(`/pdca design`)로 화면·컴포넌트 구조 설계
- 또는 작업 1번(React + Vite 기본 틀)부터 바로 착수

**〔v1.1 결과 — 이번 사이클 종료〕**

- 설계([DESIGN.md](DESIGN.md)) → 구현 → 점검([CHECK.md](CHECK.md), 남은 조치 **0건**) → 배포까지 **이번 사이클을 완주**했다.
- 완성본: **https://fta-digital-compare.vercel.app** · 저장소: https://github.com/Changyeop4513/fta-digital-compare
- **다음 사이클 후보** (착수 시 이 계획서를 v2.0으로 갱신):
  - 협정 확장 — 한-EU, 한-중, 한-베트남, 한-인도네시아 CEPA 등 (PRD 9-1 확장 후보)
  - nice 기능 — 비교 결과 내보내기(인쇄·PDF), 조항 출처 화면 표시
  - 별도 폴더(바탕화면 `프로젝트`)에 복제해 둔 확장용 사본에서 진행 가능
