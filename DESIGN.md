# DESIGN.md — FTA 디지털협정 조항 비교 서비스

> 출처: [PLAN.md](PLAN.md) v2.1, [PRD.md](PRD.md) v2.1
> bkit PDCA `design` 단계 산출물. **무엇을 · 어디에 · 어떻게 흐르게** 만들지 설계한다.
> 기술: **React 18 + Vite 5** (1차에서 확정, 변경 없음)
> **최종 개정: 2026-08-01 (v4.0 — 5차: 미발효 협정 5개 확장 반영).** v2.0은 협정 24개 대응 설계였고, v2.1은 실제로 만들어진 화면에 맞춰 협정 표기·배지 표기·순서를 확정했다. **v4.0(5차)에서 대상이 29개(발효 24 + 미발효 5)가 되었다** — 미발효 협정은 선택 패널·유무 배지에서 「미발효(서명·타결) — 문안 변동 가능」 그룹으로 분리(점선 구분, `UNRATIFIED` 상수 기준)하고, 칸 머리 정식 명칭에 "(미발효)"를 병기하며, 기준일 라벨에 타결/서명 상태를 명시한다. 본문의 "24개" 수치는 2차 설계 당시 기준으로 보존한다. 1차 설계(v1.1)는 [원본 저장소](https://github.com/Changyeop4513/fta-digital-compare)에 보존돼 있고, 이 문서는 **2차 사이클 설계로 새로 작성**했다.

## 이번 설계에서 바뀌는 것

협정이 **4개 → 24개**가 되면서 무너지는 두 곳만 다시 설계한다. 나머지(비교 뷰·검색·하이라이트·예외 처리)는 1차 설계를 그대로 유지한다.

| 영역 | 1차 (4개 기준) | **2차 (24개 기준)** |
|------|----------------|---------------------|
| 협정 선택 | 체크박스 4개를 한 줄에 나열 | **선택 칩 + 추가 패널**(이름 검색 · "있음만 보기") |
| 조항 유무 | 배지 4개를 한 줄에 나열 | **보유 개수 요약 + "있음" 배지 + "없음" 접기**, 배지 클릭으로 비교 추가 |
| 비교 뷰 | 최대 4칸 | **동일 (최대 4칸 유지)** |
| **협정 표기 〔v2.1〕** | `한-미`·`한-중` 등 임의 약칭 | **좁은 자리는 상대국명**(`미국`), **비교 뷰 칸 머리는 정식 명칭**(`한-미국 FTA`) — 규칙은 [PRD.md](PRD.md) 9-5 |
| **표시 순서 〔v2.1〕** | (4개라 무의미) | **발효일 오름차순** = `constants.js`의 `AGREEMENTS` 배열 순서 |

---

## 1. 화면 구성

PC 넓은 화면 기준, 위에서 아래로 4개 영역이 쌓인다. **① 헤더 · ④ 비교 뷰는 1차와 동일**하다.

```
┌────────────────────────────────────────────────────────────────────┐
│  [로고] FTA 디지털협정 조항 비교 서비스                  ← ① 헤더    │
├────────────────────────────────────────────────────────────────────┤
│  주제 선택:  ◉ Customs Duties(관세)                                 │
│              ○ Express shipments(특송화물)                          │
│              ○ Paperless trading(종이 없는 무역)                    │  ← ② 컨트롤 바
│                                                                     │     (sticky 상단 고정)
│  협정 선택 (최대 4개):  [미국 ✕] [중국 ✕]  [＋ 협정 추가]           │
│  원문 검색:  🔍 [________________]                                   │
├────────────────────────────────────────────────────────────────────┤
│  협정별 조항 유무   「Customs Duties(관세)」 — 24개 중 15개 보유     │
│  [인도][EU][페루][미국][호주][캐나다][중국][베트남][영국] …          │  ← ③ 조항 유무
│  ▸ 조항 없는 협정 9개 보기                                           │     (기능 3)
├──────────────────────┬──────────────────────┬──────────────────────┤
│  한-미국 FTA          │  한-중국 FTA          │  RCEP (역내포괄적…)  │
│  제15.3조             │  제13.3조             │  제12.11조           │
│  [기준일 라벨]        │  [기준일 라벨]        │  [기준일 라벨]       │  ← ④ 비교 뷰
│  EN │ 영문 원문…      │  EN │ 영문 원문…      │  EN │ 영문 원문…     │     (기능 1, 최대 4칸)
│  KO │ 한국어본…       │  KO │ 한국어본…       │  KO │ 한국어본…      │
│  ▸ 더보기             │  ▸ 더보기             │  ▸ 더보기            │
└──────────────────────┴──────────────────────┴──────────────────────┘
```

### ② 컨트롤 바 — 협정 선택 UI 〔v2.0 개편〕

**문제:** 24개를 한 줄에 늘어놓으면 sticky 바가 화면 절반을 먹는다.

**설계:** 컨트롤 바에는 **고른 것만** 두고, 고르는 행위는 **패널**로 분리한다.

```
협정 선택 (최대 4개)   [미국 ✕] [중국 ✕]   [＋ 협정 추가]
                                                  │ 클릭
                                                  ▼
                        ┌──────────────────────────────────┐
                        │ 🔎 협정 이름 검색 (예: 베트)      │
                        │ ☑ 조항 있는 협정만 보기            │  ← 기본 켜짐
                        │ ────────────────────────────────  │
                        │ 인도                       있음 ＋ │
                        │ EU                         있음 ＋ │
                        │ 페루                       있음 ＋ │
                        │ 미국                       있음 ✓ │  ← 이미 고른 협정(누르면 해제)
                        │ …                        (스크롤)  │
                        └──────────────────────────────────┘
```

> 위 예시는 **필터가 켜진 기본 상태**라 "있음" 협정만 보인다. 필터를 끄면 `칠레 … 없음` 처럼 "없음" 협정도 함께 나온다 — **"없음"이어도 고를 수 있다**(그 칸에 "해당 조항 없음"이 표시된다). 항목이 비활성되는 것은 오직 **4개가 이미 찼고 아직 안 고른 항목**일 때다.

**규칙**
- **선택 칩은 항상 컨트롤 바에 노출**된다. 최대 4개이므로 한 줄에 들어간다. 칩의 `✕`로 해제한다.
- 칩의 **좌→우 순서가 비교 뷰의 좌우 순서**다 (선택 순서 유지, 1차 규칙 그대로).
- **4개가 차면** "＋ 협정 추가" 버튼을 비활성화하고 `(최대 4개)` 안내를 강조한다. 패널 안 항목도 추가 불가로 표시한다.
- 패널의 **이름 검색**은 부분 일치(예: "베트" → `베트남`). 일치 항목이 없으면 패널 안에 **"일치하는 협정이 없습니다."** 만 표시하고 **비교 뷰는 건드리지 않는다.**
- **"조항 있는 협정만 보기"** 는 현재 선택 주제 기준으로 "있음"인 협정만 남긴다. **기본값은 켜짐** — 실무에서 먼저 알고 싶은 것은 "조항이 있는 협정"이므로, 24개 전체를 훑는 것보다 추려진 목록으로 시작하는 편이 빠르다. 끄면 전체 24개가 보인다.
- **〔v2.1〕 이미 고른 협정은 필터·검색과 무관하게 목록에 남긴다.** 필터에 걸려 사라지면 해제할 방법이 없어지기 때문이다.
- **〔유의〕** 이 필터가 켜져 있어 검색 결과가 0건인 경우에는, 단순히 "없다"고만 하지 않고 **필터가 켜져 있다는 사실을 함께 안내**한다(끄면 찾을 수 있음을 알려야 한다).
- 패널은 **바깥을 클릭하거나 `Esc`** 로 닫는다.
- 목록 각 항목에는 현재 주제 기준 **있음/없음**을 함께 표시해, 고르면서 바로 판단할 수 있게 한다.

> **주의: 검색창이 두 개다.** 패널 안의 것은 **협정 이름 검색**, **헤더 오른쪽의 것은 원문 키워드 검색**이다. 라벨을 각각 `협정 이름 검색` / `원문 검색`으로 명확히 달아 혼동을 막는다.
>
> **〔11차 · 사용자 제안〕 원문 검색은 헤더로 올라갔다**(관세법령정보포털 상단 검색 방식). `.app-title-row` 오른쪽 끝에 `SearchBar`를 두고, 어두운 헤더에 맞춰 라벨은 밝은 색·초점 링은 흰색으로 바꿨다. 검색창 오른쪽 끝을 비교 뷰 오른쪽 끝과 맞추려고 `.app-header-inner` 최대 너비를 **1456px**(=`.app-main` 1520 − 안여백 64)로 잡았다. 컨트롤 바에는 인쇄 버튼만 남아 오른쪽 정렬한다.

### ③ 조항 유무 표시 〔v2.0 개편〕

**문제:** 24개 배지를 모두 늘어놓으면 화면을 넘긴다. 그리고 실무에서 먼저 알고 싶은 것은 **"있는 협정이 어디냐"** 다.

**설계**
- **보유 개수 요약**을 제목 옆에 둔다 — `「Customs Duties(관세)」 — 24개 중 15개 보유` (주제 라벨은 화면 표기 그대로 쓴다)
- **"있음" 협정만 기본 노출.** **클릭하면 비교 대상에 추가**된다(기능 3 → 기능 1 직결).
- **〔갱신〕 배지에는 협정 이름만 표시한다.** 있음/없음은 요약 문구("N개 중 M개 보유")와 "조항 없는 협정" 접기 영역으로 이미 구분되고, 선택 여부는 색상(선택 시 네이비 = 칩과 동일)으로 드러난다. 상태 설명은 `title`(마우스 오버)과 `aria-pressed`(스크린리더)로 제공한다.
- **"없음" 협정은 접어 둔다** — `▸ 조항 없는 협정 9개 보기` 를 누르면 흐린 배지로 펼쳐진다. "없음"도 정보이므로 **반드시 확인 가능**해야 한다(숨기는 것이 아니라 접는 것). 접힌 개수는 주제마다 다르다 — 무관세 **9** · 특송화물 **9** · 종이 없는 무역 **10**.
- 이미 선택된 협정 배지는 **선택 상태로 구분 표시**하고, 클릭하면 해제된다.
- 4개가 찬 상태에서 **미선택 배지는 버튼 자체를 `disabled` 처리**한다(배지에 `＋` 기호가 없으므로 기호를 지우는 방식이 아니다). 이때 `title`로 `최대 4개까지 비교할 수 있습니다`를 알린다.

### ⑤ 비교 결과 내보내기 〔3차 신규 — 기능 4〕

**설계:** 컨트롤 바 맨 아래 오른쪽에 **`🖨 인쇄 / PDF 저장`** 버튼 하나를 두고, 누르면 브라우저 인쇄 창을 띄운다. 별도 PDF 라이브러리를 쓰지 않는다(정적 앱 · CSP `'self'` · 한글 폰트 내장 부담).

```
        (화면)                                  (인쇄물 = A4 가로)
┌──────────── 컨트롤 바 ────────────┐   ┌──────────── A4 가로 ────────────────┐
│ 주제 ◉ Customs Duties(관세)       │   │ FTA 디지털협정 조항 비교             │
│ 협정 [미국 ✕][EU ✕]  [＋ 협정 추가]│   │ 주제      Customs Duties(관세)       │
│ 원문 검색 🔍 [________]           │   │ 비교 협정  한-미국 FTA · 한-EU FTA   │
│ ─────────────────────────────     │   │ 출력 시점  2026-07-25 22:54          │
│              [🖨 인쇄 / PDF 저장] │   │ ─────────────────────────────────── │
└───────────────────────────────────┘   │ 한-미국 FTA   │ 한-EU FTA           │
                                         │ 제15.3조      │ 제7.48조 제3항      │
   ※ 컨트롤 바·유무 바·헤더는            │ 기준일 …      │ 기준일 …            │
      인쇄에서 감춘다                    │ EN 원문(전문) │ EN 원문(전문)       │
   ※ 접힌 원문은 인쇄에서 전부 펼친다    │ KO 원문(전문) │ KO 원문(전문)       │
                                         │ 출처: …       │ 출처: …             │
                                         └─────────────────────────────────────┘
```

**규칙**
- **접힌 원문은 인쇄에서 모두 펼친다** — 종이에서는 "더보기"를 누를 수 없으므로 접힌 채 인쇄되면 **원문이 잘려 나간다**(원문 무결성 위반). 접기가 CSS(`is-clamped`) 방식이라 전문이 이미 DOM에 있어 `-webkit-line-clamp: unset`으로 해제된다.
- **`overflow`를 `visible`로 되돌린다** — 비교 뷰(`overflow-x: auto`)·협정 칸(`overflow: hidden`)을 그대로 두면 여러 장으로 나뉘는 내용이 **첫 장 분량에서 잘린다.**
- **조항 카드에 `break-inside: avoid`를 걸지 않는다** — 실측 결과 조항 원문은 한 장보다 긴 경우가 많다(한-미 제15.3조 = 4칸 기준 **약 5장** 분량). 지켜질 수 없는 요구여서 앞 장에 큰 빈 공간만 만든다. 대신 **조항 번호·기준일·EN/KO 라벨에 `break-after: avoid`**, **출처에 `break-before: avoid`**, 원문에 `orphans/widows: 2`를 걸어 "머리만 남고 넘어가는" 것만 막는다.
- **출처(`source`)는 인쇄물에만** 표시한다 — 화면은 원문 비교에 집중시키고, 종이는 회의·보고용이라 인용 근거가 필요하다. 화면 표시는 여전히 범위 밖이다.
- **머리글에 조건을 남긴다** — 주제 · 비교 협정(**정식 명칭**) · 원문 검색어(있을 때만) · 출력 시점. 종이에는 컨트롤 바가 없어 "무엇을 기준으로 뽑은 표인지" 알 수 없기 때문이다.
- **색은 살린다**(`print-color-adjust: exact`) — 칸 머리의 남색과 검색 하이라이트가 종이에서도 구분돼야 하고, 주 용도가 PDF 저장이라 색 비용이 없다.
- **인쇄할 내용이 없으면 버튼을 잠근다**(협정 0개 · 검색 0건). 빈 종이보다 잠긴 버튼이 낫다.
- **출력 시점은 상태에 먼저 넣고 반영된 뒤 `window.print()`를 부른다**(`printRequest` 카운터 + `useEffect`). `setState` 직후 바로 부르면 출력 시점이 **빈 채로** 인쇄된다.

### ①·④ 헤더와 비교 뷰 — 1차와 동일

- **헤더:** 재정경제부 로고(다크 배경용 반전본) + 제목. 네이비 그라디언트.
  - **〔3차 추가〕 로고와 제목을 누르면 첫 화면으로 새로고침**된다(고른 주제·협정·검색어가 모두 초기화). 버튼이 아니라 **링크**(`href={import.meta.env.BASE_URL}`)로 둔 이유: 키보드 이동·스크린리더 안내가 기본으로 되고, 새 탭으로 열기 같은 브라우저 기본 동작도 그대로 쓸 수 있으며, 배포 경로가 바뀌어도 따라간다.
  - 평소에는 링크처럼 보이지 않게 두고(공공 문서 톤 유지) **제목은 마우스를 올렸을 때만 밑줄**, **로고는 살짝 흐려지는 것**으로 누를 수 있음을 알린다(로고에는 밑줄을 그을 수 없다). 어두운 헤더 위이므로 키보드 초점은 **흰색 테두리**로 표시한다.
  - **〔유의〕 로고와 제목은 링크가 둘로 나뉘어 있다.** 로고가 제목+부제 **전체에 세로 가운데**로 맞춰져 있어, 하나의 링크로 묶으면 부제가 로고 아래로 내려가 배치가 무너지기 때문이다. 대신 **로고 링크는 보조기술에서 감춘다**(`aria-hidden` + `tabIndex={-1}` + `alt=""`) — 목적지·동작이 완전히 같은 중복 링크라 그대로 두면 스크린리더에서 같은 링크가 **두 번 읽히고** 탭 이동도 한 번 더 걸린다. 마우스로는 로고를 누를 수 있고 키보드·스크린리더로는 제목 링크로 같은 일을 할 수 있어 **잃는 기능이 없다.**
- **비교 뷰:** 선택 협정을 **고른 순서대로 좌우 컬럼**으로. **칸 머리에는 협정 정식 명칭**(예: `한-미국 FTA`·`한-인도 CEPA`·`한-싱가포르 DPA`·`RCEP (역내포괄적경제동반자협정)`)을 쓴다 — 칸이 넓어 전체 명칭이 들어가고, 어느 협정인지 오해 없이 읽힌다. 배지·칩처럼 좁은 자리에는 짧은 이름(`미국`·`인도`)을 유지한다. 매핑은 `constants.js`의 `AGREEMENT_FULL_NAMES`. 각 칸 = `조항번호 + 기준일 라벨 + 영문 원문 + 한국어본 + 더보기`. 검색어 포함 카드는 자동 펼침. 한 협정에 조항이 여러 개면 조항번호 순으로 모두.

### 예외 상태 (1차 유지 + 1건 추가)

| 상황 | 표시 위치 | 문구 |
|------|-----------|------|
| 협정 0개 선택 | 화면 전체 대체(`EmptyState`) | "비교할 협정을 선택하세요." |
| 협정 1개 선택 | 비교 뷰 | (안내 없이 한 칸만) |
| 해당 조항 없음 | 그 협정 칸 안 | "해당 조항 없음" |
| 한국어본 없음 | 그 칸의 KO 자리 | "번역 없음" |
| 원문 검색 0건 | 화면 전체 대체(`EmptyState`) | "검색 결과가 없습니다. (검색어: "○○○")" |
| **협정 이름 검색 0건** 〔v2.0〕 | **패널 안에만** | **"일치하는 협정이 없습니다."** |

> **층위 구분이 핵심이다.** 칸 내부 표시 · 패널 내부 표시 · 화면 전체 대체는 서로 다른 층위이며 섞지 않는다. 특히 **협정 이름 검색 0건은 비교 뷰를 대체하지 않는다** — 이미 보고 있던 비교를 검색 때문에 날려서는 안 된다.

---

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[입력: 사용자 조작]              [처리: 앱 내부 계산]                    [출력: 화면]
                              ┌────────────────────────────────────┐
 주제 선택 ───────────────────┤ (0) articles.json 전체 로드         │
 협정 선택/해제(칩·배지·패널) ─┤ (1) 선택 주제(topic)로 조항 필터    ├──▶ ③ 유무 요약·배지
 협정 이름 검색 ──────────────┤ (2) 협정별 보유 판정(있음/없음)     │
 "있음만 보기" 토글 ──────────┤ (3) 패널 목록 = 이름검색 ∩ 유무필터 ├──▶ ② 추가 패널 목록
 원문 검색어 입력 ────────────┤ (4) 선택 협정 × 조항 → 컬럼 구성    ├──▶ ④ 비교 표
                              │ (5) 원문 검색어 부분일치 → 하이라이트├──▶ 노란 형광펜
                              │ (6) 예외 검사(0개/없음/0건)         ├──▶ 안내 메시지
                              └────────────────────────────────────┘
```

**흐름 설명**
- **(0) 로드:** 앱 시작 시 `articles.json`을 한 번 읽어 메모리에 둔다.
- **(1) 필터:** 고른 주제의 `topic` 태그와 일치하는 조항만 골라낸다. **`topic`이 협정 간 매칭 기준**(조항번호는 표시용).
- **(2) 보유 판정:** 그 주제 조항을 하나라도 가진 협정은 "있음", 없으면 "없음". `AGREEMENTS` **24개 전체**에 대해 계산한다. 계산은 `App`에서 한 번만 하고 유무 바·추가 패널이 **같은 결과를 나눠 쓴다**(두 곳이 다르게 판정하는 일을 막는다).
- **(3) 〔v2.0〕 패널 목록:** `AGREEMENTS`를 **협정 이름 검색어**로 걸러내고, "있음만 보기"가 켜져 있으면 (2)의 결과로 한 번 더 걸러낸다.
- **(4) 컬럼 구성:** 선택한 협정만, 고른 순서대로 좌우 칸으로 배치 (최대 4).
- **(5) 하이라이트:** 원문 검색어가 있으면 각 칸 원문(영·한)에서 부분일치로 찾아 형광펜만 칠한다. 영문 대소문자 무시. **요약·해석 없음.**
- **(6) 예외:** 위 결과가 비면 상황별 안내로 대체 (층위 규칙 준수).

> 핵심: **데이터는 읽기 전용.** 원문(`text_en`/`text_ko`)은 절대 가공하지 않고 그대로 출력만 한다.

**데이터 한 건(조항)의 모양** — [PRD.md](PRD.md) 9-3과 동일
`agreement` · `topic` · `article_no` · `text_en` · `text_ko` · `source` · `baseline_date`

> - `source`(출처)는 데이터에 **기록만** 하고 화면에는 표시하지 않는다 (PRD nice 기능 = 범위 밖).
> - **각주는 조문 본문에 넣지 않는다** (PRD 9-4). 각주 존재 사실은 `source`에 적는다.

---

## 3. 컴포넌트 구조

1차 구조를 유지하고, **협정 선택 부분만 하위 컴포넌트를 추가**한다.

```
App                          (전체를 감싸고 공유 상태를 관리)
├─ <header>                  ① 로고 + 제목        ← App 내부 마크업(별도 컴포넌트 아님)
├─ <section .control-bar>    ② 컨트롤 바 묶음 (sticky) ← App 내부 마크업
│  ├─ TopicSelector          · 주제 33개 중 1개 (라디오 세로 목록)  〔10차: 좌측 사이드바로 이동〕
│  ├─ AgreementSelector      · 선택 칩 + "＋ 협정 추가" 버튼          〔v2.0 개편〕
│  │  └─ AgreementPicker     · 추가 패널: 이름 검색 + 있음만 보기 + 목록  〔v2.0 신규〕
│  ├─ SearchBar              · 원문 키워드 검색창
│  └─ PrintButton            · 인쇄 / PDF 저장 버튼                      〔3차 신규〕
├─ PrintHeader               ⑤ 인쇄물 머리글 (화면 숨김, 인쇄만)         〔3차 신규〕
├─ AvailabilityBar           ③ 유무 요약 + "있음" 배지 + "없음" 접기   〔v2.0 개편〕
├─ ComparisonView            ④ 비교 뷰 (최대 4칸)
│  └─ AgreementColumn         · 협정 1개 = 세로 칸
│     └─ ArticleCard          · 조항 1개 (번호·기준일·EN·KO·더보기·자동펼침)
└─ EmptyState                화면 전체 예외 안내 (협정 0개 / 원문 검색 0건)
```

> **〔10차 개정 · 사용자 제안〕 주제는 화면 왼쪽 세로 사이드바다.** 주제가 33개가 되자 가로 알약 바가 화면 위쪽 24%를 먹고 그 안에서 또 스크롤해야 했다. 관세법령정보포털 좌측 메뉴처럼 **`<aside className="topic-sidebar">`(sticky) + `<div className="app-content">`** 2단 격자(`.app-layout`, 236px / 1fr)로 바꿔 조항 영역이 **세로 100%**를 쓰게 했다. 본문 최대 너비는 1280 → **1520px**로 넓혀 가로 손실을 상쇄했다(협정 4칸 폭 289px 유지). 인쇄에서는 사이드바를 숨기고 격자를 풀어 종이 폭을 다 쓴다.
>
> 〔4~9차 경위〕 그전에는 **`<section className="topic-bar">`(sticky)** 와 **`<section className="control-bar">`(일반)** 두 카드로 나눠 **주제 바만** 상단에 고정했다. 협정 선택·검색·인쇄는 그때도 지금도 스크롤에 딸려 올라간다.
> - 한 카드 안에서 자식만 sticky로 만들면 **부모가 화면을 벗어날 때 같이 사라지므로** 카드 분리가 필요했다.
> - 결과: 고정 영역 310px → 105px(4차) → **0px(10차, 좌측 사이드바로 이동)**, 조항 영역 **세로 100%**. 주제를 더 추가해도 세로 목록이 사이드바 안에서 스크롤되므로 이 구조는 그대로 견딘다.
>
> **〔v2.1 정정〕 헤더와 컨트롤 바는 별도 컴포넌트가 아니다.** `App.jsx` 안의 `<header>`·`<section className="control-bar">` 마크업이다. 상태를 갖지 않고 자리만 잡는 껍데기라 컴포넌트로 분리할 이유가 없었다. **실제 컴포넌트 파일은 11개** — `TopicSelector` · `AgreementSelector` · `AgreementPicker` · `SearchBar` · **`PrintButton`** · **`PrintHeader`** · `AvailabilityBar` · `ComparisonView` · `AgreementColumn` · `ArticleCard` · `EmptyState`. (뒤의 두 개가 3차에서 추가됐다)

**상태(state) 배치**

공유 데이터 상태는 1차처럼 `App` 한 곳에서 관리한다. **UI 국소 상태(패널 열림·검색어·토글)는 해당 컴포넌트 안에 둔다** — 다른 영역이 알 필요가 없고, App을 불필요하게 부풀리지 않기 위함이다.

| 상태 | 위치 | 뜻 |
|------|------|-----|
| `selectedTopic` | App | 현재 고른 주제 1개 |
| `selectedAgreements` | App | 고른 협정 목록 (**순서 유지, 최대 4**) |
| `keyword` | App | **원문** 검색어 |
| `printedAt` | App | 인쇄물 머리글에 적을 **출력 시점** 〔3차〕 |
| `printRequest` | App | 인쇄 요청 카운터 — 값이 바뀔 때만 `window.print()`를 부르는 방아쇠 〔3차〕 |
| `pickerOpen` | AgreementSelector | 추가 패널 열림 여부 〔v2.0〕 |
| `query` | AgreementPicker | **협정 이름** 검색어 〔v2.0〕 |
| `onlyAvailable` | AgreementPicker | "조항 있는 협정만 보기" 토글 〔v2.0〕 **기본값 `true`(켜짐)** |
| `showUnavailable` | AvailabilityBar | "없음" 접힘/펼침 〔v2.0〕 |

**지켜야 할 것**
- **주제(`selectedTopic`)를 바꿔도 `selectedAgreements`는 절대 초기화하지 않는다.** 주제 변경 핸들러에서 협정 상태를 건드리지 말 것.
- 협정 이름 검색·"있음만 보기"·패널 개폐는 **`selectedAgreements`에 영향을 주지 않는다.** 목록을 좁히는 것과 선택을 바꾸는 것은 별개다.
- 보유 판정·컬럼 구성·하이라이트는 위 상태로부터 **그때그때 계산**한다(따로 저장하지 않음).
- 최대 4개 제한은 **한 곳에서만** 정의해 칩·패널·배지가 같은 값을 쓴다 (`MAX_SELECTION`).

---

## 4. 폴더 구조

1차 구조에 컴포넌트 1개가 추가된다.

```
프로젝트/
├─ index.html                  앱 진입 HTML
├─ package.json                의존성·실행 명령 (React 18 · Vite 5)
├─ vite.config.js              Vite 설정 + 개발/프리뷰 보안 헤더
├─ vercel.json                 배포 설정 + 프로덕션 보안 응답 헤더 5종
└─ src/
   ├─ main.jsx                 앱 시작점
   ├─ App.jsx                  공유 상태 관리 + 예외 분기
   ├─ constants.js             AGREEMENTS(24개·발효일순) · AGREEMENT_FULL_NAMES(정식 명칭 24개 매핑) · MAX_SELECTION  〔v2.1〕
   ├─ components/
   │  ├─ TopicSelector.jsx        주제 선택 (네이티브 라디오, 3개 고정)
   │  ├─ AgreementSelector.jsx    선택 칩 + 추가 버튼            〔v2.0 개편〕
   │  ├─ AgreementPicker.jsx      추가 패널 (검색·필터·목록)     〔v2.0 신규〕
   │  ├─ SearchBar.jsx            원문 키워드 검색창
   │  ├─ PrintButton.jsx          인쇄 / PDF 저장 버튼            〔3차 신규〕
   │  ├─ PrintHeader.jsx          인쇄물 머리글 (인쇄 전용)       〔3차 신규〕
   │  ├─ AvailabilityBar.jsx      유무 요약·배지·접기            〔v2.0 개편〕
   │  ├─ ComparisonView.jsx       비교 뷰 (기능 1)
   │  ├─ AgreementColumn.jsx      협정 1개 = 세로 칸
   │  ├─ ArticleCard.jsx          조항 카드
   │  └─ EmptyState.jsx           화면 전체 예외 안내
   ├─ data/articles.json       조항 데이터 (**72건 수록 완료** — 15·15·14·3·3·1·21) 〔4차 갱신〕
   ├─ assets/                  재정경제부 로고 (원본 + 헤더용 반전본)
   └─ styles/index.css         네이비 공공문서 톤 CSS
```

---

## 5. 디자인 톤 (1차 유지)

- 정부·공공 문서에 어울리는 **신뢰감 있고 차분한** 톤. 원문 비교 가독성 최우선.
- 메인 색상 **짙은 남색(네이비)**, 강조는 회색·흰색. 검색 하이라이트만 노란 형광펜.
- **PC 데스크톱 넓은 화면 우선.** 모바일 최적화는 범위 밖.
- 〔v2.0〕 새로 추가되는 칩·패널·배지도 같은 팔레트를 쓴다.
- **〔v2.1 정정〕 색 역할이 자리마다 다르다** — 배지에서 색은 **선택 여부**를, 패널 항목에서 색은 **조항 유무**를 나타낸다. 배지에는 있음/없음 텍스트가 없으므로 색으로 유무를 말하지 않는다.

| 자리 | 상태 | 색 |
|------|------|-----|
| 조항 유무 배지 | 있음(미선택) | 연한 남색 배경 `#e8edf9` + 네이비 글자 |
| 조항 유무 배지 | **선택됨** | **네이비 배경 + 흰 글자** (칩과 동일 — 어느 배지가 비교 중인지 한눈에) |
| 조항 유무 배지 | 없음(접기 안) | 흐린 회색 `#f1f2f5` + 흐린 글자, 클릭 불가 |
| 추가 패널 항목의 유무 라벨 | 있음 | **초록** `#1e874b` / 연초록 배경 |
| 추가 패널 항목의 유무 라벨 | 없음 | 흐린 회색 |

---

## 6. 구현 결과 〔v2.1〕

이 설계대로 전부 구현·검증·배포되었다.

- **순서:** UI(선택 칩·추가 패널·유무 바)를 먼저 만들고 **협정 24개를 등록한 상태로 화면을 검증**한 뒤 데이터를 채웠다. 화면이 먼저 24개를 버티는지 확인했으므로 데이터 대량 수정이 필요 없었다.
- **검증 방식:** 컴포넌트 1개 단위로 끊어 매번 [CLAUDE.md](CLAUDE.md)의 **검증 루프**(변경 → 브라우저 확인 → 코드 리뷰 → 한 줄 요약)를 돌았다. 결과는 [CHECK.md](CHECK.md).
- **설계 이후 확정된 것 (v2.1):** 협정 표기 규칙(상대국명 / 정식 명칭) · 배지는 이름만 · 발효일 오름차순 정렬 · 색 역할 분리(위 5절 표).
- **다음 사이클 후보:** 비교 결과 내보내기(인쇄·PDF) · 조항 출처(`source`) 화면 표시 · 서명했으나 미발효인 협정 대응 — [PLAN.md](PLAN.md) "다음 단계".
