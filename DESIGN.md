# DESIGN.md — FTA 디지털협정 조항 비교 서비스

> 출처: [PLAN.md](PLAN.md), [PRD.md](PRD.md)
> bkit PDCA `design` 단계 산출물. **무엇을 · 어디에 · 어떻게 흐르게** 만들지 설계한다.
> 선택 기술: **React + Vite** (아래 3번에서 선택함)
> **최종 개정: 2026-07-24 (v1.1 — 구현 결과 반영본).** 설계 원문은 보존하고, 구현하며 달라지거나 확정된 부분을 **〔v1.1〕**로 표기했다. (같은 방식의 갱신: [PRD.md](PRD.md) 개정 이력 참조 · 검증 결과: [CHECK.md](CHECK.md))

---

## 1. 화면 구성 (무엇이 어디에 보이는지)

PC 넓은 화면 기준, 위에서 아래로 4개 영역이 쌓인다.

```
┌──────────────────────────────────────────────────────────────────┐
│  FTA 디지털협정 조항 비교 서비스                        ← ① 헤더    │
├──────────────────────────────────────────────────────────────────┤
│  주제 선택:  ( ) 무관세   ( ) 특송화물   ( ) 종이 없는 무역         │
│  협정 선택:  ☑ 한-싱가포르  ☑ RCEP  ☐ 한-영국  ☐ 한-미  (최대 4)  │  ← ② 컨트롤 바
│  검색:      🔍 [________________]                                  │
├──────────────────────────────────────────────────────────────────┤
│  「무관세」 보유 현황:                                              │
│   한-싱가포르 ✅있음 · RCEP ✅있음 · 한-영국 ❌없음 · 한-미 ✅있음  │  ← ③ 보유 현황 바 (기능 3)
├───────────────────┬───────────────────┬──────────────────────────┤
│  한-싱가포르       │  RCEP              │  한-미                   │
│  제14.3조          │  제12.x조          │  제19.x조                │
│  [기준일 2024-01]  │  [기준일 2022-02]  │  [기준일 2012-03]        │  ← ④ 비교 뷰 (기능 1)
│  EN │ 영문 원문…   │  EN │ 영문 원문…   │  EN │ 영문 원문…         │     (선택 협정 좌우 나란히,
│  KO │ 한국어본…    │  KO │ "번역 없음"  │  KO │ 한국어본…          │      최대 4칸, 선택 순서 유지)
│  ▸ 더보기          │  ▸ 더보기          │  ▸ 더보기                │
└───────────────────┴───────────────────┴──────────────────────────┘
```

**각 영역 규칙**
- **① 헤더:** 앱 제목만. 고정.
  - **〔v1.1 확정〕** 제목 왼쪽에 **재정경제부 로고**(흰 바탕 제거·다크 배경용 반전본 `src/assets/moef-logo-header.svg`)를 함께 표시하고, 배경은 **네이비 그라디언트**(`#12203C → #1E3358`)로 한다.
- **② 컨트롤 바:** 주제(3개 중 1개, 고정), 협정(체크박스 최대 4·최소 1), 검색창. 항상 화면 위에 노출.
  - **〔v1.1 확정〕** "항상 화면 위에 노출"을 **`position: sticky`(상단 고정)**로 구현 — 긴 원문을 스크롤해 읽는 중에도 주제·협정 변경과 검색이 바로 가능하다.
  - **〔v1.1 확정〕** 주제는 **네이티브 라디오**(`<input type="radio">` + `role="radiogroup"`, 알약 모양 라벨)로 구현하고, 표기는 **영문 병기** — `Customs Duties(관세)` / `Express shipments(특송화물)` / `Paperless trading(종이 없는 무역)`.
- **③ 보유 현황 바:** 주제를 고르면 각 협정의 있음/없음을 배지로 표시(기능 3). "있음" 협정을 바로 골라 비교로 연결.
  - **〔v1.1 확정〕** 화면 라벨은 "보유 현황"이 아니라 **"협정별 조항 유무"**로 한다.
- **④ 비교 뷰:** 선택 협정을 **고른 순서대로 좌우 컬럼**으로. 각 칸 = `조항번호 + 기준일 라벨 + 영문 원문 + 한국어본`. 긴 원문은 접고 "더보기". 한 협정에 조항이 여러 개면 조항번호 순으로 모두.
  - **〔v1.1 추가〕 검색어가 포함된 조항 카드는 자동으로 펼친다.** (접힌 카드 속 하이라이트를 놓치는 문제 해소 — 이때 "더보기" 버튼은 숨김)
  - **〔v1.1 참고〕** 위 스케치의 조항번호·기준일은 설계 시 가상 예시였다. 실제 수록 조항은 [PRD.md](PRD.md) 9-1의 11개 조항 매트릭스(예: 무관세 = 한-싱 DPA 제14.5조 / RCEP 제12.11조 / 한-미 제15.3조) 참조.
- **예외 상태:**
  - 협정 **1개**만 선택 → 안내 없이 그 협정 **한 칸만** 표시 (정상 동작)
  - 협정 **0개** 선택 → 비교 뷰 대신 "협정을 선택하세요" (화면 전체 안내)
  - 특정 협정에 해당 조항 없음 → **그 협정 칸 안에** "해당 조항 없음" 표시 ("번역 없음"과 구분, 화면 전체 안내 아님)
  - 검색 **0건** → "검색 결과가 없습니다" (화면 전체 안내)

---

## 2. 데이터 흐름 (입력 → 처리 → 출력)

```
[입력: 사용자 조작]          [처리: 앱 내부 계산]                    [출력: 화면]
                          ┌────────────────────────────────────┐
 주제 선택 ───────────────┤ (0) articles.json 전체 로드         │
 협정 선택(체크) ─────────┤ (1) 선택 주제(topic)로 조항 필터    ├──▶ ③ 보유 현황 배지
 검색어 입력 ─────────────┤ (2) 협정별 보유 판정(있음/없음)     │
                          │ (3) 선택 협정 × 조항 → 컬럼 구성    ├──▶ ④ 비교 표
                          │ (4) 검색어 부분일치 → 하이라이트    ├──▶ 노란 형광펜
                          │ (5) 예외 검사(0개/없음/0건)         ├──▶ 안내 메시지
                          └────────────────────────────────────┘
```

**흐름 설명 (한 줄씩)**
- **(0) 로드:** 앱이 시작될 때 `articles.json`(조항 데이터)을 한 번 읽어 메모리에 둔다.
- **(1) 필터:** 고른 주제의 `topic` 태그와 일치하는 조항만 골라낸다. **`topic`이 협정 간 매칭 기준**(조항번호는 표시용).
- **(2) 보유 판정:** 그 주제 조항을 하나라도 가진 협정은 "있음", 없으면 "없음".
- **(3) 컬럼 구성:** 사용자가 체크한 협정만, 고른 순서대로 좌우 칸으로 배치.
- **(4) 하이라이트:** 검색어가 있으면 각 칸 원문(영/한)에서 부분일치로 찾아 형광펜만 칠한다(요약·해석 없음). 영문은 대소문자 무시.
- **(5) 예외:** 위 결과가 비면 상황별 안내 문구로 대체.

> 핵심: **데이터는 읽기 전용.** 원문(`text_en`/`text_ko`)은 절대 가공하지 않고 그대로 출력만 한다.

**데이터 한 건(조항)의 모양** — [CLAUDE.md](CLAUDE.md) 스키마와 동일
`agreement` · `topic` · `article_no` · `text_en` · `text_ko` · `source` · `baseline_date`

> - **데이터 형식은 JSON으로 확정**한다 (PRD의 "JSON 또는 CSV" 중 JSON 채택, CSV 미채택).
> - **`source`(출처)는 데이터에 기록만** 하고 화면에는 표시하지 않는다 (출처 표시는 PRD nice 기능 = 이번 범위 밖).
> - **〔v1.1 결과〕** 이 설계 그대로 `src/data/articles.json`에 **11개 조항**(4개 협정 × 3개 주제 − 한-영국 특송 부재 1건)이 수록됐고, 영·한 전부 **산업통상부 FTA 포털 원문과 글자 단위 일치**를 검증했다([CHECK.md](CHECK.md)). 검색어 공백 정책(앞뒤 trim, 단어 사이 공백은 그대로 매칭)은 `App.jsx` 주석으로 명시했다.

---

## 3. 기술 선택 (난이도별 3가지 옵션)

비개발자도 이해할 수 있게 1줄로 정리한 3가지. **→ 선택: 옵션 B (React + Vite)**

| | 옵션 | 쉬운 설명 (1줄) | 난이도 |
|---|------|----------------|--------|
| A | 순수 HTML/CSS/JS | 설치·빌드 없이 HTML 파일 하나로 시작 — 가장 단순하지만 화면이 복잡해지면 관리가 번거로움 | 가장 쉬움 |
| **B ✅** | **React + Vite** | **화면을 '검색창·표·버튼' 부품으로 나눠 만드는 요즘 표준 방식 — 협정 칸이 늘어도 정리가 잘 됨** | **보통** |
| C | Next.js | React에 라우팅·배포 편의를 더한 도구 — 지금 로컬 범위엔 살짝 과하나 나중에 사내 공유·확장에 유리 | 조금 더 복잡 |

**옵션 B를 고른 이유:** 이 앱은 협정 칸·주제/협정 선택·검색 등 화면 요소가 여럿인데, React로 부품을 나누면 각 요소를 깔끔히 관리하고 협정이 2~4개로 늘어도 화면이 헝클어지지 않는다. 읽기 전용 비교엔 서버·DB(옵션 C의 일부 기능)까지는 과하고, 옵션 A보다 확장이 쉽다. (PRD 8번의 선택과 일치)

> **〔v1.1 결과〕** 옵션 B 선택은 유효했다. "로컬 범위엔 Next.js가 과하다"고 봤던 배포도, 실제로는 **Vite 정적 빌드(`dist/`)를 Vercel에 그대로 올리는 방식**으로 Next.js 없이 해결됐다 (https://fta-digital-compare.vercel.app · `main` push 시 자동 재배포). 배포 시 보안 응답 헤더 5종은 `vercel.json`으로, 개발·프리뷰는 `vite.config.js`로 적용 — 상세는 [PRD.md](PRD.md) 7번.

---

## 4. 컴포넌트 구조 (React 기준)

화면 영역을 그대로 부품(컴포넌트)으로 나눈다.

```
App                         (전체를 감싸고 상태를 관리)
├─ Header                   ① 제목
├─ ControlBar               ② 컨트롤 바 묶음
│  ├─ TopicSelector         · 주제 3개 중 1개 선택
│  ├─ AgreementSelector     · 협정 체크박스 (최대 4·최소 1)
│  └─ SearchBar             · 키워드 검색창
├─ AvailabilityBar          ③ 주제별 보유 있음/없음 배지  (기능 3)
├─ ComparisonView           ④ 비교 뷰 (선택 협정 좌우 나열) (기능 1)
│  └─ AgreementColumn        · 협정 1개 = 세로 칸
│     └─ ArticleCard          · 조항 1개 = 조항번호+기준일+영문+한국어본+더보기
│                              (해당 조항이 없는 협정은 이 칸 안에 "해당 조항 없음" 표시)
└─ EmptyState               화면 전체 예외 안내(협정 0개 선택 / 검색 0건) — 칸 내부 표시와 구분
```

**상태(state)는 `App` 한 곳에서 관리** — 부품끼리 얽히지 않게 위에서 내려준다.

| 상태 | 뜻 | 예 |
|------|-----|-----|
| `articles` | 로드된 전체 조항 데이터 | (articles.json 내용) |
| `selectedTopic` | 현재 고른 주제 1개 | `"customs_duties"` |
| `selectedAgreements` | 고른 협정 목록(순서 유지, 최대 4) | `["한-싱가포르", "RCEP"]` |
| `keyword` | 검색어 | `"무관세"` |

> - 보유 현황·비교 컬럼·하이라이트는 위 4개 상태로부터 **그때그때 계산**해서 만든다(따로 저장하지 않음).
> - **주제(`selectedTopic`)를 바꿔도 `selectedAgreements`는 절대 초기화하지 않는다** (PRD 규칙: 주제 변경 시 선택 협정 유지). 주제 변경 핸들러에서 협정 상태를 건드리지 말 것.
> - **〔v1.1 결과〕** 위 컴포넌트 트리·상태 4개 구조가 **설계 그대로 구현**됐다. `EmptyState`는 처음 App 인라인으로 구현됐다가 점검(gap 분석)에서 발견되어 **설계대로 별도 컴포넌트로 분리**했고, `TopicSelector`도 버튼 토글로 구현됐던 것을 **설계(라디오 명세)대로 네이티브 라디오로 정합**했다. 협정 목록 상수는 `src/constants.js`(`AGREEMENTS`)로 분리했다.

---

## 5. 폴더 구조 초안

```
my-app/
├─ index.html              (앱이 뜨는 시작 파일)
├─ package.json            (의존성·실행 명령)
├─ vite.config.js          (Vite 설정)
└─ src/
   ├─ main.jsx             (앱 시작점)
   ├─ App.jsx              (전체·상태 관리)
   ├─ components/          (위 부품들 .jsx)
   ├─ data/
   │  └─ articles.json     (조항 데이터 — 최소 샘플부터)
   └─ styles/              (네이비 공공문서 톤 CSS)
```

**〔v1.1 확정 — 실제 폴더 구조〕** (초안 대비 추가: `vercel.json` · `constants.js` · `assets/` · 컴포넌트 8개 확정)

```
my-app/
├─ index.html                  앱 진입 HTML
├─ package.json                의존성·실행 명령 (React 18 · Vite 5)
├─ vite.config.js              Vite 설정 + 개발/프리뷰 보안 헤더
├─ vercel.json                 배포 설정 + 프로덕션 보안 응답 헤더 5종
└─ src/
   ├─ main.jsx                 앱 시작점
   ├─ App.jsx                  전체 상태 관리 (4개 상태 + 예외 분기)
   ├─ constants.js             대상 협정 목록 (AGREEMENTS)
   ├─ components/
   │  ├─ TopicSelector.jsx        주제 선택 (네이티브 라디오, 3개 고정)
   │  ├─ AgreementSelector.jsx    협정 선택 (체크박스, 최대 4)
   │  ├─ SearchBar.jsx            키워드 검색창
   │  ├─ AvailabilityBar.jsx      협정별 조항 유무 배지 (기능 3)
   │  ├─ ComparisonView.jsx       비교 뷰 (기능 1)
   │  ├─ AgreementColumn.jsx      협정 1개 = 세로 칸 ("해당 조항 없음" 처리)
   │  ├─ ArticleCard.jsx          조항 카드 (EN/KO·기준일·더보기·자동 펼침)
   │  └─ EmptyState.jsx           화면 전체 예외 안내
   ├─ data/articles.json       조항 데이터 11건 (포털 대조 검증 완료)
   ├─ assets/                  재정경제부 로고 (원본 + 헤더용 반전본)
   └─ styles/index.css         네이비 공공문서 톤 CSS (sticky 컨트롤 바 포함)
```

---

## 6. 다음 단계

- 이 설계 확정 후 → **작업 1번(React + Vite 기본 틀)**부터 구현 착수 (`/pdca do` 또는 바로 진행)
- 구현은 [PLAN.md](PLAN.md)의 1~15번 순서를 따르며, 매 단계 CLAUDE.md의 **검증 루프**(변경 → 확인 → 리뷰 → 통과 시 한 줄 요약)를 돈다.

**〔v1.1 결과 — 구현·점검 완료〕**

- PLAN 1~15번 전부 완료 + 검증·보안·배포·문서화까지 추가 수행 ([PLAN.md](PLAN.md) v1.1 진행 결과 참조).
- 설계 대비 갭 점검(gap 분석)에서 발견된 어긋남 3건(검색 0건 UX · EmptyState 인라인 · 주제 버튼 토글)은 **모두 설계 쪽에 맞춰 코드를 정합**했다 — 설계를 코드에 맞춰 굽히지 않았다. 최종 점검: [CHECK.md](CHECK.md) **남은 조치 0건**.
- 완성본: **https://fta-digital-compare.vercel.app** · 저장소: https://github.com/Changyeop4513/fta-digital-compare
