# CLAUDE.md

> ## 📌 이 폴더는 **확장 작업용 사본**입니다 (원본과 분리된 별개 프로젝트)
>
> | 구분 | 내용 |
> |------|------|
> | **이 폴더** | `바탕화면/프로젝트` — **2차 사이클(확장) 작업 공간**. 저장소 **`Changyeop4513/fta-digital-compare-next`** (공개, `main`) · 사이트 **https://fta-digital-compare-next.vercel.app** |
> | **원본** | `바탕화면/My-app` — 1차 사이클 완료·배포본. 저장소 `Changyeop4513/fta-digital-compare` · 사이트 https://fta-digital-compare.vercel.app |
>
> **둘은 완전히 분리된 저장소·사이트입니다.** 여기서 push하면 **이 폴더의 사이트(`-next`)만** 재배포되고, 원본 사이트에는 전혀 영향이 없습니다.
> **〔v2.1 갱신〕** 이 폴더의 문서(PRD·prd_lite·DESIGN·PLAN·CHECK)는 **2차 사이클 문서로 이미 재작성**되어 있습니다 — PRD/prd_lite는 **v2.1**, DESIGN/PLAN/CHECK는 각 최신판. 1차 사이클 기록은 [원본 저장소](https://github.com/Changyeop4513/fta-digital-compare)에 보존돼 있습니다.

---

> 이 파일은 Claude Code가 이 프로젝트에서 작업할 때 따르는 **프로젝트 규칙서**입니다.
> `PRD.md`(상세)와 `prd_lite.md`(요약)에서 핵심 규칙을 추출해 정리했으며,
> **최종 개정 2026-08-02 (v6.0)** — **8차 사이클(주제 30개 완성) 완료**에 맞춰 갱신했습니다. 8차에서 새로 정해진 지점은 **〔8차〕** 표기. (관련 문서: [PRD.md](PRD.md) v5.0 · [prd_lite.md](prd_lite.md) · [DESIGN.md](DESIGN.md) · [PLAN.md](PLAN.md) · [CHECK.md](CHECK.md) v6.0)

## 작업 규칙 (협업 규칙)

- **언어:** 모든 설명과 주석은 **한국어**로 작성한다.
- **파일 위치:** 새 파일은 **이 프로젝트 폴더(`바탕화면/프로젝트`) 안에만** 만든다. 폴더 밖(원본 `My-app` 포함)에는 생성하지 않는다.
- **변경 보고:** 코드를 바꾸면 반드시 **무엇을·왜 바꿨는지 한 줄**로 알려준다.
- **비밀 정보:** `.env` 등 비밀 정보 파일은 **`.gitignore`에 등록**해 두고, **절대 커밋하지 않는다**.
- **삭제 금지 → 이동:** 파일을 지워야 할 때는 바로 삭제하지 말고, **`trash-can` 폴더**를 만들어 그 안으로 **옮겨만** 둔다. 최종 삭제는 사용자가 직접 확인 후 진행한다.
- **서브에이전트 활용:** 이미 설치된 서브에이전트(bkit의 `gap-detector` 등)를 **필요할 때마다 적극 활용**한다.

## 작업 절차 (검증 루프)

코드를 만질 때는 아래 루프를 **매번 반복**한다. 통과할 때까지 1)~4)를 돈다.

1. **변경한다** — 필요한 코드를 수정한다.
2. **결과를 직접 확인한다** — 브라우저로 열거나 실행해서 실제 동작을 눈으로 확인한다. (짐작으로 넘어가지 않는다)
3. **스스로 코드 리뷰한다** — 바꾼 코드를 다시 읽으며 문제·누락·규칙 위반이 없는지 점검한다. (필요 시 `gap-detector` 등 서브에이전트 활용)
4. **문제가 있으면 고치고 다시 1)로** — 이상이 발견되면 수정 후 루프를 재시작한다.

→ **통과하면**, 무엇을·왜 바꿨는지 **한 줄로 요약**한다.

### 검증 시 주의 (오늘 배운 것)

- **설정 파일 변경은 서버 재시작**: `vite.config.js` 같은 설정을 바꾸면 HMR로 반영되지 않으니, 개발 서버를 **재시작**한 뒤 검증한다. (`.jsx`/`.css`는 저장 즉시 반영)
- **화면 렌더가 정지된 환경(프리뷰 창 숨김)에서의 UI 검증**: 스크린샷·CSS 전이(transition)가 얼어붙어 **엉뚱한 값(예: 선택색이 뒤바뀐 듯)** 이 측정될 수 있다. 이때는 (a) `getComputedStyle`로 최종 상태를 읽을 때 `transition`을 잠깐 꺼서 값을 스냅해 확인하고, (b) 클릭은 MCP 클릭 대신 **요소 좌표에 실제 마우스 이벤트를 디스패치**해 확인한다. 색·역전처럼 보여도 **가짜 버그일 수 있으니** 이 방법으로 재확인한 뒤 판단한다.

## 프로젝트 개요

한국이 체결한 여러 FTA의 **디지털 통상 조항**을 협정별로 나란히 비교하는 웹 앱입니다.
**〔6차 · 사용자 결정〕 범위를 "재경부 소관"에서 "디지털 협정 전체"로 확대**하고, **〔8차 · 사용자 결정〕 디지털 무역 장의 남은 조문 유형 13개까지 추가**해 주제는 **서른 가지**입니다. 〔3차 4·5번 · 4차 6·7번 · 6차 8~17번 · 8차 18~30번〕

1. 전자적 전송물 무관세 (`customs_duties`)
2. 특송화물 (`express_shipments`)
3. 종이 없는 무역 (`paperless_trading`)
4. 전자송장 (`e_invoicing`)
5. 전자지급 (`electronic_payment`)
6. **정부조달 (`government_procurement`)** — 디지털 경제 관련 조항만. 전통 FTA의 정부조달 장 전체는 대상 아님
7. **과세 예외 (`taxation_exception`)** — 협정 전체에 걸리는 일반 예외. 디지털 통상 조항은 아니나 수록(당초 재경부 소관 사유)
8. **디지털 제품 비차별 (`digital_product_nd`)** 〔6차〕 — 미국·중미는 디지털제품 조문(15.3/14.3)이 관세+비차별 통합형이라 **항 단위로 분리 수록**(EU 제7.48조 선례)
9. 전자인증·전자서명 (`e_authentication`) 〔6차〕
10. 핀테크 협력 (`fintech`) 〔6차〕
11. 국경 간 데이터 이전 (`cross_border_data`) 〔6차〕
12. 컴퓨터 설비 위치 (`computing_facilities`) 〔6차〕
13. 개인정보 보호 (`personal_data`) 〔6차〕
14. 소스코드 (`source_code`) 〔6차〕
15. 온라인 소비자 보호 (`consumer_protection`) 〔6차〕
16. 스팸 메시지 (`spam`) 〔6차〕
17. 사이버보안 (`cybersecurity`) 〔6차〕
18. **국내 전자거래 체계 (`domestic_framework`)** 〔8차〕 — UNCITRAL 모델법을 고려한 국내법 유지 의무
19. **암호 ICT 제품 (`cryptography`)** 〔8차〕 — 암호키·알고리즘 공개 요구 금지(소스코드의 자매 조항)
20. **온라인 안전·보안 (`online_safety`)** 〔8차〕
21. **인터넷 접근·이용 (`internet_access`)** 〔8차〕 — 소비자의 서비스·기기 선택권
22. **정부 데이터 공개 (`open_gov_data`)** 〔8차〕
23. **데이터 혁신 (`data_innovation`)** 〔8차〕
24. **디지털 신원 (`digital_identity`)** 〔8차〕
25. **인공지능 (`ai`)** 〔8차〕
26. **경쟁정책 협력 (`competition`)** 〔8차〕 — **디지털 장 안의 경쟁 조문만.** 별도 「경쟁 장」(중국·베트남·호주 등 7개)은 대상 아님
27. **중소기업·스타트업 (`sme`)** 〔8차〕
28. **디지털 포용 (`digital_inclusion`)** 〔8차〕
29. **전자상거래 물류 (`e_logistics`)** 〔8차〕
30. **표준·적합성평가 (`standards`)** 〔8차〕

- **주 사용자:** 재정경제부 내부 실무자 (확장: 통상 실무자·연구자)
- **부처명 표기:** 정식 명칭은 **재정경제부**, 짧게 쓸 필요가 있을 때는 약칭 **재경부**를 사용한다.
- **핵심 가치:** 협정마다 흩어진 조항을 한 화면에서 원문 그대로 비교 (수작업 20~30분 → 3분 이내)
- **실행 환경 〔확장 사본 기준〕:** 로컬 실행(`npm run dev`) + **GitHub·Vercel 연결 완료.**
  - 저장소: **https://github.com/Changyeop4513/fta-digital-compare-next** (공개, `origin`/`main`)
  - 사이트: **https://fta-digital-compare-next.vercel.app** (보안 헤더 5종 적용 확인됨)
  - **`main`에 push하면 이 사이트가 자동 재배포**된다 — push 전 반드시 **로컬 검증 루프를 통과**할 것.
  - 원본 사이트(`fta-digital-compare`)와는 **완전히 별개**라 서로 영향을 주지 않는다.
  - 참고 — **1차 사이클 배포본**(별개): 사이트 https://fta-digital-compare.vercel.app · 저장소 `Changyeop4513/fta-digital-compare` (원본 폴더 `My-app`에서 관리)

## 기술 스택

- **React** — UI를 컴포넌트(검색창·표·필터·버튼)로 분리
- **Vite** — 개발 서버·빌드
- **데이터: JSON 파일** — 협정 조항 데이터를 담는 정적 파일 `src/data/articles.json` (별도 서버·DB 없음) **〔v1.1 확정: JSON 채택, CSV 미채택〕**
- **CSS** — 네이비 기반의 차분한 공공 문서 스타일 (PC 화면 우선, 모바일 최적화는 범위 밖)

> 백엔드·데이터베이스·로그인은 이번 범위에 없습니다. **읽기 전용 정적 앱**입니다.

## 개발 명령어 〔v1.1 확정〕

```bash
npm install       # 의존성 설치 (React 18 · Vite 5)
npm run dev        # 로컬 개발 서버 (http://localhost:5173)
npm run build      # 프로덕션 빌드 (dist/)
npm run preview    # 빌드 결과 미리보기 (보안 헤더 + CSP 적용 상태로 확인 가능)
```

> **주의:** `vite.config.js`에 보안 헤더가 걸려 있고 **CSP는 preview에만** 적용된다(HMR 충돌 때문). CSP 관련 동작은 `npm run preview`로 검증할 것. 배포는 별도 명령 없이 **`main` push 시 자동**.

## 데이터 모델 (조항 1건 = 객체 1개)

| 필드 | 설명 |
|------|------|
| `agreement` | 협정 **짧은 이름 = 상대국명만** (실제 값 예: `"미국"` / `"EU"` / `"중국"` / `"RCEP"` / `"싱가포르 DPA"` — `src/constants.js`의 `AGREEMENTS` 24개 중 하나와 **글자까지 정확히 일치**해야 함. 어긋나면 그 조항은 어떤 협정에도 매칭되지 않아 **화면에서 사라진다**) **〔2차 확정〕** |
| `topic` | 주제 — 위 「프로젝트 개요」의 **17개 키 중 1개**. **협정 간 조항을 나란히 묶는 매칭 기준.** 값은 `TopicSelector.jsx`의 `TOPICS`와 정확히 일치해야 한다 |
| `article_no` | 조항 번호·제목 (표시용, 매칭에는 사용하지 않음) |
| `text_en` | 영문 원문 |
| `text_ko` | 한국어본 (없으면 빈 값 → "번역 없음"으로 표시) |
| `source` | 출처 (문서명·페이지 또는 링크) |
| `baseline_date` | 데이터 기준 시점(버전/기준일) |

### 대상 협정 29개 = 발효 24 + 미발효 5 〔5차 확장〕

`src/constants.js`의 `AGREEMENTS`가 **유일한 기준**이다. 배열 순서 = **발효일 오름차순**(미발효는 발효 24개 뒤에 타결·서명일 오름차순) = 화면 표시 순서.

```
발효 24: 칠레 · EFTA · ASEAN · 인도 · EU · 페루 · 미국 · 튀르키예 · 호주 · 캐나다 · 중국 ·
뉴질랜드 · 베트남 · 콜롬비아 · 영국 · 중미 · RCEP · 이스라엘 · 캄보디아 · 인도네시아 ·
싱가포르 DPA · DEPA · 필리핀 · UAE
미발효 5: 에콰도르 · GCC · 조지아 · 말레이시아 · EU DTA   ← UNRATIFIED 에도 등록돼 있다
```

- **미발효(서명·타결) 협정 취급 규칙 〔5차 신설·사용자 결정〕:**
  - 화면에서 **「미발효(서명·타결) — 문안 변동 가능」 그룹**으로 분리한다(선택 패널·조항 유무 배지, 점선 구분). 발효 협정의 확정 문안과 오해되면 안 된다.
  - 정식 명칭에 **"(미발효)"** 를 병기하고, 조항의 `baseline_date`에 **타결/서명일과 상태**를 명시한다 — 예: `2026-06-10 서명 (미발효, 문안 변동 가능)` · `2024-11-27 타결 (미발효·서명 전, 문안 변동 가능)`.
  - **발효되면:** `UNRATIFIED`에서 빼고 `AGREEMENTS`의 발효일 위치로 옮긴 뒤, 정식 명칭의 "(미발효)"를 떼고, 데이터의 `baseline_date`를 발효일로 갱신하고, **발효 협정문으로 원문을 재검증**한다(문안이 바뀌었을 수 있다 — `agreement-files.mjs check`).
  - **세르비아 CEPA**(2026-05 타결)는 협정문 미확보로 대상 밖이다. 협정문을 구하면 같은 절차로 추가한다.

- **협정 표기 규칙 (2가지만 쓴다. 새 약칭을 만들지 않는다):**
  - **짧은 이름 = 상대국명만.** `한-` 접두사도, `FTA`/`CEPA` 같은 종류 표기도 붙이지 않는다. → 조항 유무 배지 · 선택 칩 · 추가 패널 목록 · 데이터의 `agreement`
  - **정식 명칭** → **비교 뷰 칸 머리에만.** `src/constants.js`의 `AGREEMENT_FULL_NAMES` 매핑을 쓴다 (예: `한-미국 FTA` · `한-EU FTA` · `한-인도 CEPA` · `RCEP (역내포괄적경제동반자협정)`).
  - 예외 표기: 상대국이 국가가 아닌 협정은 그 이름을 그대로 쓴다 — `EFTA`·`ASEAN`·`중미`·`RCEP`·`DEPA`·`싱가포르 DPA`.
  - 협정을 **추가**할 때는 발효일 위치에 **끼워 넣는다**(맨 뒤에 붙이지 않는다). `AGREEMENTS`와 `AGREEMENT_FULL_NAMES` **양쪽에** 넣어야 한다.
- **제외 협정:** **한-싱가포르 FTA는 대상에서 제외**되어 있다(사용자 결정 — 디지털 통상 조항은 싱가포르 DPA가 담고 있고, FTA 협정문은 국문 추출 품질이 나빠 원문 무결성을 지킬 수 없음). 임의로 되살리지 않는다.
- **유의:** 영국은 한-EU FTA 조문을 승계했으나 **각각 자기 협정문 기준으로** 따로 수록한다.
- **데이터 현황 〔8차 완료〕:** **265건 수록** — 관세 20 · 비차별 10 · 특송화물 20 · 종이 없는 무역 18 · 전자송장 7 · 전자인증 14 · 전자지급 7 · 핀테크 5 · 데이터 이전 10 · 설비 위치 8 · 개인정보 17 · 소스코드 5 · 소비자 보호 17 · 스팸 10 · 사이버보안 9 · 정부조달 1 · 과세 예외 26 · **〔8차 신규〕** 국내 전자거래 체계 10 · 암호 ICT 2 · 온라인 안전 2 · 인터넷 접근 9 · 정부 데이터 공개 7 · 데이터 혁신 3 · 디지털 신원 4 · AI 6 · 경쟁 협력 3 · 중소기업 6 · 디지털 포용 2 · 물류 4 · 표준 3. **29 × 30 = 870칸 전부 판정 완료**(있음 261칸 · 없음 609칸), 전 조항 원문 대조 통과([CHECK.md](CHECK.md)).
  - **주제마다 분포가 다르다.** 6·8차 신규 주제는 **디지털 무역을 독립 장(章)으로 둔 협정에만** 있다. 초기 전자상거래 장 협정(칠레·EFTA·ASEAN·인도·EU·영국·튀르키예·캄보디아·뉴질랜드·필리핀·인도네시아)은 대부분 "없음"이며, 예외는 **국내 전자거래 체계**(RCEP·베트남·호주)와 **인터넷 접근**(미국)뿐이다. 단발 검색 히트는 전부 잡음으로 판정했다(양허표·별도 경쟁 장·협력 목록 등).
  - **한 칸에 조문이 둘일 수 있다** — 튀르키예 과세 예외, 에콰도르 특송화물, 싱가포르 DPA 설비 위치(제14.15조 일반 + 제14.16조 금융), **〔8차〕 DEPA 중소기업**(제10.2조 + 제10.4조). 그래서 조문 수(265)가 있음 칸 수(261)보다 넷 많다.
  - **국문은 265건 전부 확보돼 있다**("번역 없음" 0건). 깨진 국문 PDF(영국·미국 등)는 **OCR 3배율 다수결**로 복원했다 — 절차는 아래 「깨진 국문 PDF에서 원문을 복원할 때」 참조.
  - 전 주제(30개) 통틀어 조항이 하나도 없는 협정 **2개**: EFTA·ASEAN.
- **〔데이터 수정 시 규칙〕** 조항을 추가·수정할 때는 ① 정부 공식 1차 출처에서만 확보 ② 포털 원문과 대조 ③ `source`에 출처·특이사항 기록 ④ 아래 원문 무결성 규칙 준수 — 이 절차를 생략하지 않는다.
- **〔필수 절차〕 추출본을 근거로 쓰기 전에 그 파일이 정말 읽히는지 확인한다.** PDF 폰트 인코딩이 깨진 협정문이 실제로 여럿 있고, 그런 파일에서 검색하면 **조항이 있어도 "0회"** 가 나온다. 2026-07 CHECK v2.1에 이 때문에 무효한 근거를 적었다가 정정한 사례가 있다.
- **⚠️ 한글 '음절 수'만 세는 검사는 통하지 않는다 〔4차 정정〕.** 인코딩이 **다른 한글로 밀려서** 깨지는 경우가 있다 — 글자는 전부 한글이라 음절 수 검사는 멀쩡히 통과하지만 내용은 뜻 없는 글자다.
  - 실제 사례: 한-영국 제15장 국문 PDF는 **한글 15,462자**가 추출됐으나 본문이 `Ⲷ48ⰿ# ῎⪇⮞⭪㬦` 같은 상태였다.
  - **올바른 검사 — 흔한 낱말이 나오는지 본다.**
    ```bash
    for w in 협정 당사국 아니한다 조 항; do echo "$w: $(grep -o "$w" 파일.txt | wc -l)"; done
    ```
    정상 국문본이면 `협정`·`당사국`이 수십~수백 회 나온다. **전부 0이면 깨진 파일**이다.
  - 국문본이 깨졌으면 **영문본으로 판정**하고, 그 사실을 근거에 적는다.

### 협정문 원본은 **로컬 폴더가 1차 출처** 〔사용자 지정〕

```
바탕화면/프로젝트/FTA 협정/     ← 사용자가 FTA 포털에서 받아 둔 협정문 전문 PDF
```

- **조항을 찾거나 유무를 판정할 때는 이 폴더를 먼저 본다.** 협정별로 **국·영문 전문(full agreement)** 이 번호 순으로 들어 있어, 장(章)을 골라 받을 필요 없이 전문을 한 번에 검색할 수 있다.
- **FTA 포털 접속은 마지막 검증용으로만** 쓴다 — 이 폴더에 없는 협정을 보충하거나, 수록 직전 원문을 재확인할 때.
- 이 폴더는 **`.gitignore`로 제외**되어 있다(약 550MB). **절대 커밋하지 않는다.** `git add -A`를 쓸 때 이 규칙이 지켜지고 있는지 확인할 것.
- **RCEP은 장별로만 있다.** 폴더에는 **제4장(통관절차·무역원활화)·제12장(전자상거래)** 국·영문이 들어 있고 전체 협정 파일이 없다. **그 밖의 장(상품·서비스·일반규정 및 예외 등)은 포털에서 받는다.** — 예: 과세 예외는 제17장이라 폴더에 없다.
- **번호 26~30은 미발효 서명 협정**(에콰도르 SECA · GCC · 조지아 CEPA · **한-EU DTA** · 말레이시아)이라 **현재 대상 24개에 들어가지 않는다.** 임의로 수록하지 말 것 — 대상 확대는 사용자 결정 사항이다.
- **한 협정이 여러 파일로 나뉜 경우가 있다.** 예: **튀르키예는 기본협정·상품·서비스·투자 4종**이 따로 있다. 한 파일만 보고 "없음"으로 판정하면 틀린다 — **그 협정의 파일을 모두 확인**할 것.
  - 실제 사례: 튀르키예 **투자협정 제1.18조(과세)** 는 투자 파일에만 있고, 그 조문은 다시 **기본협정 제8.1조(과세)** 를 참조한다.

### 협정문이 개정되면 (개선협상 대응) 〔필수 절차〕

FTA 개선협상으로 협정문이 바뀌면 사용자가 **「FTA 협정」 폴더의 PDF를 새 파일로 갈아끼운다.**
이때 **수록 조항(`articles.json`)은 그대로 남으므로**, 아무 조치를 안 하면
**화면에는 옛 조문이 보이는데 근거 파일은 새 것**인 상태가 된다. 원문 무결성 위반이다.

그래서 PDF의 **지문(SHA-256)** 을 `agreement-manifest.json`(16KB)에 기록해 두고 대조한다.
PDF 자체는 용량이 커서 커밋하지 않지만, **지문은 커밋한다** — 근거의 재현성을 지키기 위함이다.

```bash
node scripts/agreement-files.mjs check              # 바뀐 협정과 영향받는 조항을 보고
node scripts/agreement-files.mjs save "YYYY-MM-DD"  # 재검증을 마친 뒤 지문 갱신
```

**협정문을 갈아끼운 뒤 반드시 이 순서로 처리한다.**

1. `check` 를 돌려 **어느 협정의 몇 건이 영향받는지** 확인한다 (도구가 조항 목록까지 뽑아 준다)
2. 바뀐 협정문에서 해당 조문을 다시 추출해 수록된 `text_en`/`text_ko` 와 **글자 단위로 대조**한다
3. 달라졌으면 **원문을 갈아끼우고** `baseline_date`·`source` 를 새 기준으로 고친다
   - `baseline_date` 는 **개정 발효일**로 바꾼다. 화면의 기준일 라벨이 곧 사용자에게 주는 신뢰의 근거다
   - `source` 에 **개정 반영 사실과 시점**을 남긴다 (예: "2027-03-01 개정 발효분 반영")
4. [CHECK.md](CHECK.md) 에 개정 반영 경위를 기록한다
5. `save` 로 지문을 갱신한다 — **재검증을 마친 뒤에만** 갱신할 것(먼저 갱신하면 변경 사실이 지워진다)

> **주의:** 조문이 그대로여도 파일이 바뀔 수 있다(오탈자 수정·재편집). 지문이 달라졌다고 반드시
> 조문이 바뀐 것은 아니므로, **2번 대조를 거쳐 실제 변경 여부를 확인**한 뒤 판단한다.
>
> **범위 밖:** 개정 **이력**을 화면에 보여 주는 기능(옛 조문과 새 조문 나란히 비교)은 만들지 않는다
> ([PRD.md](PRD.md) 비범위). 화면에는 **현행 조문 1개**만 두고 기준일로 시점을 밝힌다.

### 국문 정본을 못 구할 때 — 국가법령정보센터 〔실전 확인됨〕

산업통상자원부 FTA 포털의 국문 PDF가 **스캔 이미지이거나 폰트 인코딩이 깨져** 글자를 뽑을 수 없는 협정이 있다. 이때 **국가법령정보센터(law.go.kr)에 공포된 국문 정본**을 쓴다 — 정부 공식 1차 출처이므로 원문 무결성 원칙에 어긋나지 않는다.

```
조약 검색:  https://www.law.go.kr/LSW/trtySc.do?menuId=15
조약 본문:  https://www.law.go.kr/trtyInfoP.do?mode=4&trtySeq={번호}&chrClsCd=010202
```

- 검색은 JS로 동작한다. 검색창(`#query`)에 값을 넣고 **`keyup` 엔터**를 보내야 결과가 나온다.
- 목록의 링크는 `trtyView('숫자')` 형태다. 그 숫자가 위 주소의 `trtySeq`다.
- **협정마다 제공 방식이 다르다.** 조문이 **웹페이지 본문에 텍스트로** 실린 협정이 있고, **공포문만 싣고 조문은 첨부파일(PDF·HWP)** 로 주는 협정이 있다.

**확인된 사례**

| 협정 | 조약번호 | 결과 |
|------|---------|------|
| 칠레 | 제1665호 | ✅ 본문에 **국문 전문이 텍스트로** 있음(13만 자). 과세 조문 확보해 수록 |
| 영국 | 제2465호 | ❌ 본문은 공포문뿐. 첨부 국문 PDF는 **포털과 같은 인코딩 손상**. HWP는 별도 확인 필요 |

- 국가법령정보센터·외교부 조약정보시스템·FTA 포털이 **같은 원본 PDF를 공유**하는 경우가 있다. 한 곳이 깨졌다고 다른 곳이 멀쩡하리라 단정하지 말 것.
- 국문 정본은 **공포 당시 표현**을 쓰므로 포털본과 문구가 다를 수 있다(예: 칠레 “상호 배치”). 어느 쪽도 틀린 것이 아니므로 **어디서 가져왔는지 `source`에 반드시 밝힌다.**

### 포털에서 협정문 받는 법 〔마지막 검증용〕

```
장 목록:  https://fta.motir.go.kr/ftamain/kfta/lov5/{코드}/2/     ← 끝이 /2/ 여야 PDF 링크가 나온다
PDF:      HTML 안의 /webmodule/_PSD_FTA/... 경로를 그대로 쓴다
```

- **`/1/2/`가 아니라 `/{코드}/2/`** 다. 잘못된 주소도 200을 돌려주지만 **PDF 링크가 하나도 없는 페이지**가 온다.
- **파일명 규칙이 협정마다 다르다** — 대부분 `_eng.pdf`/`_kor.pdf`지만 **영국은 `_en.pdf`/`_kr.pdf`**, 경로도 `/uk/2/eng/`처럼 다르다. **HTML에서 실제 링크를 뽑아 쓸 것.**
- 받은 뒤 **`%PDF` 헤더와 파일 크기를 확인**한다. 오류 페이지가 PDF 이름으로 저장되는 일이 있다(같은 크기의 파일이 여러 개 나오면 의심).

## 🚫 AI가 반드시 지켜야 할 규칙 (가장 중요)

이 앱의 신뢰성은 **원문을 손대지 않는 것**에 달려 있습니다. 아래를 절대 어기지 마세요.

### 원문 무결성
- **조항 원문(`text_en`/`text_ko`)을 절대 요약·수정·의역·재작성하지 않는다.** 준비된 데이터 그대로 표시만 한다.
- **AI 요약·해설·해석 기능을 만들지 않는다.** 이 앱은 원문 비교와 키워드 검색만 한다.
- 한국어본이 없으면 임의 번역을 생성하지 말고 **"번역 없음"**으로 명시한다.
- **출처 원칙:** 원문은 **정부 공식 1차 출처(협정문 원본 PDF 등)에서 그대로 확보한 것만** 입력한다. **검색엔진 요약·2차 설명 자료를 원문으로 쓰지 않는다** (2026-07 KORUS 제15.3조에서 검색 요약을 넣었다가 실제 원문과 달라 정정한 사례). 확보·검증이 안 되면 값을 비워("번역 없음"/미입력) 둔다.
- **주석 분리:** 출처·기준일·"미검증/정규화 보정" 같은 메모는 **`source` 필드에만** 적고, `text_en`/`text_ko`에는 원문 외 어떤 표시도 섞지 않는다.

### 원문 검증 도구 〔v3.5 신규〕

아래 절차를 수행하는 **도구 8종이 [`scripts/verify/`](scripts/verify/)에 있다** — 진단(probe-pdf) · 전수 감사(audit-articles) · 쪽 찾기(find-article) · 쪽 추출(layout-page) · 렌더(render-pages.ps1, JBIG2 스캔 포함) · 글줄 분할(split-lines) · 배율 3종 OCR(ocr-lines.ps1) · 낱말 대조(compare-ocr) · 깨진 인코딩 복원(reconstruct). 사용법과 판정 요령은 [`scripts/verify/README.md`](scripts/verify/README.md). **조문을 추가·수정하면 `audit-articles.mjs`를 돌려 자동 감사를 통과시킨다.** 중간 산출물은 `.verify-cache/`(git 제외)에 쌓인다.

### 깨진 국문 PDF에서 원문을 복원할 때 〔v3.3 신규〕

정부 배포 PDF 중에는 **폰트 인코딩이 깨져** 한글이 추출되지 않는 것이 있다(한-영국·칠레·인도·EU 등). 이때 **OCR로 복원해도 되지만, OCR 결과를 그대로 넣어서는 안 된다.** 아래 3중 검산을 전부 통과한 것만 수록한다.

1. **OCR을 여러 배율로 돌려 대조한다.** 쪽을 이미지로 렌더링(`pdfjs-dist` + `@napi-rs/canvas`)한 뒤 Windows 내장 OCR(한국어)로 읽는다. 배율을 3가지로 바꿔 결과가 갈리는 줄을 찾아낸다.
2. **깨진 텍스트 레이어와 글자 수를 맞춰 본다.** 한글 코드는 깨졌어도 **글자 수·공백·줄바꿈 위치는 멀쩡**하다(공백은 `#`, 아스키는 +3 밀려 있다). 줄 길이가 맞는 줄들에서 '깨진 글자 → 진짜 글자' 대응을 모아 **다수결**로 확정하고, 그 대응표로 전체를 되살린다. 이렇게 채운 글자는 지어낸 것이 아니라 **같은 문서 다른 자리에서 OCR이 확신 있게 읽은 같은 글자**다.
3. **대조할 다른 문서가 있으면 반드시 쓴다.** 승계 관계 협정(영국↔EU)처럼 문안이 겹치는 조문이 있으면 낱말 단위로 비교해, 차이가 **당사자 표기 같은 예상된 자리에만** 몰려 있는지 확인한다.

**스캔 PDF는 렌더러부터 바꿔야 한다 〔v3.4〕** — 일부 협정문(한-칠레 국문)은 **JBIG2로 압축된 스캔**이라 pdfjs(Node)로 그리면 **빈 쪽**이 나온다. 이때는 **Windows 내장 PDF 렌더러**(`Windows.Data.Pdf`)로 그린다. 텍스트 레이어가 아예 없으므로 위 ②는 쓸 수 없고, ①(배율 3종 OCR)과 ③(외부 정본 대조)으로 확정한다.

**텍스트가 읽히는 PDF도 문장부호는 못 믿는다 〔v3.4〕** — 한-인도·한-EU 국문 PDF는 텍스트가 정상 추출되는데도 **문장부호를 뒤 낱말 앞으로 밀어** 내보낸다(`규정한다 .그리고`). 뷰어에서 복사해도 같은 값이 나오므로 **눈으로 확인해도 틀린다.** 실제로 2026-07 재검증에서 사용자 확인분의 쉼표 3곳이 오류로 드러났다. **문장부호·띄어쓰기는 반드시 OCR을 근거로 삼는다.**

**웹 본문은 PDF 원문의 대체물이 아니다 〔v3.4〕** — 국가법령정보센터 등 웹 공포본은 따옴표 모양(`"` ↔ `“”`)과 띄어쓰기가 PDF와 다를 수 있다. PDF를 읽을 수 있게 되면 **PDF 판독값으로 바로잡고**, 두 출처의 차이를 `source`에 남긴다.

**반드시 지킬 것**

- **OCR만 믿지 않는다.** OCR은 **줄 끝에 홀로 떨어진 한 글자를 통째로 버린다** — 배율을 바꿔도 같은 자리에서 똑같이 빠지므로, 여러 번 읽는 것만으로는 못 잡는다. **2번(글자 수 대조)을 생략하면 글자가 빠진 줄 모르고 수록하게 된다.**
- **눈으로 읽어 옮겨 적지 않는다.** 화면에 보이는 글자를 내가 읽어 타이핑하는 것은 OCR이 아니라 **생성**이다. 반드시 기계 추출 결과를 근거로 삼는다.
- **인코딩 밀림을 역산해 복원하지 않는다.** 한글은 자모 배열이 아니라 폰트 CID 대응이라 일정한 규칙이 없다. 규칙을 추정해 만든 글자는 원문이 아니다.
- 3중 검산으로도 확정 못 한 글자가 하나라도 남으면 **수록하지 않고 비워 둔다.**
- 복원해 수록할 때는 **어떤 파일 몇 쪽을 어떻게 검산했는지 `source`에 남긴다.**

### 비교 뷰 (기능 1)
- 한 화면에 **최대 4개 협정**까지만 좌우로 나란히 표시 (초과 금지). 1개 선택 시 한 칸만 표시.
- **〔2차 확정〕 칸 머리에는 협정 정식 명칭**을 표시한다 (`AGREEMENT_FULL_NAMES`). 칸이 넓어 전체 명칭이 들어가고, 어느 협정인지 오해 없이 읽히기 때문이다. 배지·칩처럼 좁은 자리에는 짧은 이름을 유지한다.
- 각 칸은 **영문 원문 + 한국어본을 한 쌍**으로 표시. 한쪽만 있으면 "번역 없음" 명시.
- 한 협정에 같은 주제 조항이 여러 개면 **누락 없이 모두** 조항 번호 순으로 나열.
- 좌우 배치 순서는 **사용자가 고른 순서**를 따른다.
- 각 조항에 **기준 시점(`baseline_date`) 라벨**을 함께 표시. (개정 자동 추적은 하지 않음)
- 원문이 길면 기본은 앞 몇 줄만 접어서 보여주고 "더보기"로 전체 펼치기.
- **〔v1.1 추가〕 검색어가 포함된 조항 카드는 자동으로 펼친다** (이때 "더보기" 버튼은 숨김). 접힌 카드 속 하이라이트를 놓치지 않게 하기 위한 확정 동작.

### 필터·검색 (기능 2)
- **주제는 30개로 고정 〔8차 확대〕.** 임의로 추가·변경하지 않는다. 주제를 늘리면 협정 29개를 전부 다시 판정해야 하므로(주제 1개 = 29칸) **사용자 결정 없이 추가하지 않는다.**
- **〔8차〕 주제 목록에는 최대 높이(21vh)와 내부 스크롤이 걸려 있다.** 주제가 30개라 알약이 여러 줄이 되는데, 그대로 두면 고정 바가 화면의 45%를 먹어 조항 읽을 자리가 없어진다(실측). `.topic-radios`의 `max-height`/`overflow-y`를 없애지 말 것 — 선택된 알약은 `scrollIntoView`로 항상 보이게 한다.
- **〔4차 필수〕 주제를 늘릴 때는 "어디까지를 그 주제로 볼지" 먼저 정한다.** 정하지 않으면 협정마다 다른 기준으로 수록돼 화면의 "없음"을 믿을 수 없게 된다.
  - 실제 사례: **정부조달**은 전통 FTA에 장(章) 통째로 있어, 범위를 안 정하고 담으면 한 칸에 조문이 15~20개가 되어 좌우 비교가 무의미해진다 → **디지털 관련 조항만**으로 한정했다.
  - **과세 예외**는 반대로 디지털 통상 조항이 아니지만 재경부 소관이라 담기로 했다 → 이런 판단은 **사용자가 결정**한다.
- **〔v1.1 확정 UI 표기〕** 주제는 **네이티브 라디오**(`role="radiogroup"`, 알약 라벨)이며 표기는 영문 병기로 고정 — 예: `Customs Duties(관세)` / `Personal data(개인정보 보호)`. 17개 라벨의 정본은 `TopicSelector.jsx`의 `TOPICS`다. 조항 유무 영역 라벨은 **"협정별 조항 유무"**. 임의로 바꾸지 않는다.
- **〔4차 변경〕 상단에 고정하는 것은 「주제 바」 하나뿐이다.** 협정 선택·검색·인쇄는 **고정하지 않고 스크롤에 딸려 올라가게** 둔다.
  - 이유: 주제가 7개로 늘면서 컨트롤 바 전체를 고정하니 **조항 읽을 자리가 화면 절반밖에 남지 않았다.** 주제는 "지금 무엇을 보는지"를 알려주고 자주 바꾸지만, 협정 선택·검색은 한 번 정하면 계속 볼 필요가 없다.
  - 결과: 고정 영역 **105px(15%)**, 조항 영역 **85%**. 새 주제를 추가하더라도 **조항 영역이 70% 아래로 내려가지 않게** 유지할 것.
  - 구현: `.topic-bar`(sticky) / `.control-bar`(일반)로 **분리된 두 카드**다. 한 카드 안에서 자식만 sticky로 만들면 부모가 화면을 벗어날 때 같이 사라지므로 분리했다. 주제 바 안에서는 라벨을 알약과 **같은 줄**에 두어 한 줄을 아낀다.
- **〔2차 확정〕 협정 선택 UI:** 협정이 24개라 한 줄 나열이 불가능하므로 **① 컨트롤 바에는 고른 협정만 칩**으로 두고(`✕`로 해제, 칩의 좌→우 순서 = 비교 뷰 좌우 순서), **② 추가는 `＋ 협정 추가` 패널**(협정 이름 부분 일치 검색 + **"조항 있는 협정만 보기" 기본 켜짐**, 바깥 클릭·`Esc`로 닫힘) **또는 조항 유무 배지 클릭**으로 한다. 4개가 차면 추가 버튼과 미선택 항목을 비활성화한다.
- **〔2차 확정〕 검색창이 두 개**다 — 패널 안은 **협정 이름 검색**, 컨트롤 바는 **원문 키워드 검색**. 서로 다른 기능이므로 라벨을 섞지 않는다.
- 한 번에 **1개 주제만** 표시. 주제를 바꿔도 **선택한 협정 목록은 유지**한다.
- 검색은 **현재 선택한 주제·협정 범위 안의 원문**에서만 수행 (전체 데이터 검색 아님).
- 검색은 **부분 일치**로 찾고(예: "무관세" → "무관세화" 포함), 영문은 **대소문자 구분 없음**.
- 찾은 단어는 **노란 형광펜 느낌으로 하이라이트만** 한다. 요약·해석 없음.

### 조항 보유 자동 찾기 (기능 3)
- 판정은 데이터의 **`topic` 태그 기준**. 조항이 하나라도 있으면 "있음", 없으면 "없음".
- 조항 유무를 **임의로 추론하지 않는다.** 데이터에 있는 그대로만 판정.
- **〔2차 확정〕 배지에는 협정 이름만 적는다.** `있음 ＋` 같은 꼬리표를 붙이지 않는다 — 있음/없음은 **요약 문구**(`「Customs Duties(관세)」 — 24개 중 15개 보유`)와 **"조항 없는 협정 N개 보기" 접기 영역**으로 이미 갈라지고, 선택 여부는 **색상**(선택 시 네이비 = 칩과 동일)으로 드러난다. 상태 설명은 `title`(마우스 오버)과 `aria-pressed`(스크린리더)로 제공한다.
- **〔2차 확정〕 "없음" 협정은 접어 두되 반드시 펼쳐 볼 수 있게** 한다. 숨기는 것이 아니라 접는 것 — "없음"도 실무에 필요한 정보다.
- **〔2차 확정〕 배지를 누르면 바로 비교 대상에 추가**된다 (기능 3 → 기능 1 직결).
- **〔2차 확정〕 간접 규정 판정 기준(엄격):** 주제 조항이 독립 조문이 아니라 다른 조항의 항에 들어 있을 때는 **실질 의무가 있으면 수록, 대화 주제·맥락 언급뿐이면 "없음"**으로 판정한다. 협정 간 예외 없이 동일 적용한다. (근거·사례: [PRD.md](PRD.md) 9-4)

### 비교 결과 내보내기 (기능 4) 〔3차 신규〕

- **방식은 브라우저 인쇄뿐이다.** `＋` PDF 라이브러리(jsPDF 등)를 **추가하지 않는다** — 외부 통신 없는 정적 앱이고 CSP가 `'self'`로 묶여 있으며, 한글 폰트를 내장하면 번들이 커지고 원문이 깨질 위험이 생긴다. 종이 인쇄와 "PDF로 저장"은 같은 경로다.
- **레이아웃은 A4 가로**(`@page { size: A4 landscape }`)이며 협정을 **화면처럼 좌우로 나란히** 둔다. "한눈에 비교"라는 핵심 가치를 종이에서도 지킨다.
- **접힌 원문은 인쇄에서 모두 펼친다.** 종이에서는 "더보기"를 누를 수 없으므로 접힌 채 인쇄되면 원문이 잘려 나간다 — 이는 원문 무결성 위반이다.
- **`overflow`를 반드시 `visible`로 되돌린다.** 비교 뷰·협정 칸의 `overflow: auto/hidden`을 그대로 두면 여러 장으로 나뉘는 내용이 **첫 장 분량에서 잘려** 인쇄된다.
- **조항 카드에 `break-inside: avoid`를 걸지 않는다.** 조항 원문은 한 장보다 긴 경우가 많아(실측: 한-미 제15.3조는 4칸 기준 5장 분량) 지켜질 수 없는 요구이며, 오히려 앞 장에 큰 빈 공간을 만든다. 대신 조항 번호·기준일·EN/KO 라벨에 `break-after: avoid`, 출처에 `break-before: avoid`를 걸어 **"머리만 남고 넘어가는" 것만** 막는다.
- **출처(`source`) 표시 〔7차 갱신〕:** 화면에서는 카드 하단 **접이식 「출처」 토글**(기본 접힘)로, 인쇄물에서는 **토글 없이 전문**으로 표시한다. 화면 기본 접힘은 원문 비교 집중 원칙 때문이다 — **기본 펼침으로 바꾸지 않는다.** 인쇄용 블록(`.article-card-source`)과 화면용 토글(`.article-source-toggle`)은 별도 요소다(접힌 `<details>`는 인쇄에서 내용이 잘리기 때문).
- **인쇄물 머리글**(`PrintHeader`)에 **주제·비교 협정·검색어·출력 시점**을 남긴다. 종이에는 컨트롤 바가 없어 "무엇을 기준으로 뽑은 표인지" 알 수 없기 때문이다. 협정은 자리가 넉넉하므로 **정식 명칭**을 쓴다.
- **인쇄할 내용이 없으면 버튼을 잠근다** (협정 0개 · 검색 0건). 빈 종이가 나오는 것보다 낫다.
- **출력 시점은 상태에 먼저 넣고, 반영된 뒤에 `window.print()`를 부른다** (`useEffect` 방아쇠). `setState` 직후 바로 부르면 출력 시점이 빈 채로 인쇄된다.

## 예외 처리 원칙 〔v1.1 확정 문구·동작〕

- **협정 1개 선택:** 안내 없이 그 협정 한 칸만 표시.
- **협정 0개 선택:** 비교 뷰를 **화면 전체 안내로 대체**(`EmptyState`) — 문구 **"비교할 협정을 선택하세요."**
- **해당 조항 없음:** "번역 없음"과 **구분**하여 그 협정 **칸 안에** "해당 조항 없음" 표시.
- **검색 0건:** 협정 0개와 동일하게 **화면 전체 대체** — 문구 **"검색 결과가 없습니다. (검색어: "○○○")"**. 배너+비교 뷰 동시 노출 방식은 금지(0건인데 표가 남는 모순).
- **층위 구분:** 칸 내부 표시("해당 조항 없음"·"번역 없음")와 화면 전체 대체(`EmptyState`)를 섞지 않는다.
- **검색어 공백 정책:** 앞뒤 공백은 trim, 단어 사이 공백은 그대로 매칭(하이라이트 동일).

## 이번 범위 밖 (만들지 않을 것)

- 협정 개정 이력·최신 버전 **자동 추적** (단, `baseline_date` 라벨 표시는 함)
- AI 요약·해설
- **17개 주제** 외 조항 — **〔6차 개정 · 사용자 결정〕** 당초 "재경부 소관만" 규칙은 6차에서 해제되어 개인정보 보호·데이터 이전·소스코드 등이 편입됐다. 남은 밖 영역(예: 통신 장, 지식재산 장 전체)은 여전히 사용자 결정 없이 추가하지 않는다
- **전통 FTA의 정부조달 장 전체** — 정부조달은 **디지털 경제 관련 조항만** 담는다
- 사용자가 원문을 직접 추가·편집하는 기능 (데이터는 읽기 전용)
- 로그인·회원가입·즐겨찾기 저장

## 보안·개인정보

- 다루는 데이터는 정부가 공표한 **공개 협정문**뿐. 대외비 아님.
- **개인정보를 수집·저장·처리하지 않는다.** (로그인 기능 없음)
- 외부 서비스 키가 필요해지면 **`.env`로 분리하고 git에서 제외**한다. 소스코드에 키를 직접 넣지 않는다.
- **〔v1.1 적용 현황〕** 보안 응답 헤더 5종(CSP·X-Frame-Options·X-Content-Type-Options·Referrer-Policy·HSTS)이 `vite.config.js`(dev/preview)와 `vercel.json`(프로덕션)에 걸려 있다 — **이 설정을 임의로 완화하지 않는다.** `.env`는 로컬에만 존재하며 git 미추적(전체 이력에도 없음, [CHECK.md](CHECK.md) 재검증). **토큰·키 값은 채팅/출력에 노출하지 않는다** (`${VAR:+OK}` 같은 존재 확인만 사용).
- **〔v1.1〕** 기획·설계 문서(PRD 등)는 사용자 결정으로 **공개 저장소에 공개**된 상태다. 새 문서를 추가할 때 비밀 정보가 섞이지 않는지 확인 후 커밋한다.

## 디자인 가이드

- 정부·공공 문서에 어울리는 **신뢰감 있고 차분한** 톤. 표(원문 비교) 가독성 최우선.
- 메인 색상: **짙은 남색(네이비)**, 강조는 회색·흰색.
- **PC 데스크톱 넓은 화면 우선.** 모바일 최적화는 범위 밖.

## 개발 단위 〔3차 사이클까지 전부 완료 ✅〕

**1차 사이클 (협정 4개로 화면·기능 완성):** React+Vite 틀 → 데이터 스키마·샘플 → 주제 선택 → 조항 유무 표시 → 협정 선택 → 비교 뷰 → 영·한 표시 → 기준일 라벨 → 검색·하이라이트 → 예외 처리 → 디자인 → 데이터 완성. **13단계 전부 완료** (상세 기록은 [원본 저장소](https://github.com/Changyeop4513/fta-digital-compare)).

**2차 사이클 (협정 24개로 확대):**

1. ~~확장 범위 확정 + 포털 직접 수집 방식 검증~~ ✅
2. ~~PRD·PLAN·DESIGN v2.0 작성~~ ✅
3. ~~협정 선택 UI 개편 (선택 칩 + 추가 패널: 이름 검색·"있음만 보기")~~ ✅
4. ~~조항 유무 표시 개편 (보유 개수 요약 · "없음" 접기 · 배지 클릭으로 비교 추가)~~ ✅
5. ~~한-싱가포르 FTA / 싱가포르 DPA 분리 (FTA 대상 제외)~~ ✅
6. ~~협정 데이터 전량 수집·수록~~ ✅ (24개 판정 · **44건**)
7. ~~전 조항 원문 대조 검증~~ ✅ (44/44 통과)
8. ~~Check — 갭 분석 · 보안 점검 · 무회귀 확인~~ ✅ ([CHECK.md](CHECK.md))
9. ~~배포 (`main` push → 자동 재배포)~~ ✅
10. ~~표기·UI 다듬기~~ ✅ — 협정명 **상대국명 통일** · 배지 **이름만** · 칸 머리 **정식 명칭**
11. ~~문서 as-built 갱신 (PRD·prd_lite·DESIGN·PLAN·CHECK·CLAUDE·README)~~ ✅

**추가 수행(계획 외):** 각주 처리 규칙 확정 · **간접 규정 엄격 판정 기준** 신설 및 1차 기록 소급 정정 · 텍스트 추출 불량 협정 3건(칠레·인도·EU) 사용자 협업 처리 · **협정 표기 규칙** 신설 — 상세는 [PLAN.md](PLAN.md).

**3차 사이클 (내보내기 + 주제 확대) 〔v3.2 신규〕:**

12. ~~**비교 결과 내보내기**~~ ✅ 브라우저 인쇄 → PDF 저장. A4 가로 나란히 · 접힌 원문 전부 펼침 · 출처는 인쇄물에만 · 머리글에 주제·협정·검색어·출력 시점.
13. ~~**제목·로고 클릭 새로고침**~~ ✅ 첫 화면으로 되돌리기(주제·협정·검색어 초기화).
14. ~~**주제 3개 → 5개**~~ ✅ DEPA 조문 구조 기준으로 **전자송장·전자지급** 신설 · 48칸 판정 · 6건 수록.
15. ~~**주제 5개 → 7개**~~ ✅ **정부조달·과세 예외** 신설 · 48칸 판정 · 22건 수록 (정부조달 1 · 과세 예외 21).
16. ~~**원문 출처를 로컬 폴더로 전환**~~ ✅ `프로젝트/FTA 협정`을 1차 출처로, 포털은 최종 검증용. **PDF는 `.gitignore`로 커밋 차단**(66개 553MB).
17. ~~**협정문 개정 대응 도구**~~ ✅ SHA-256 매니페스트 + `scripts/agreement-files.mjs`(`check`/`save`) — 바뀐 파일과 영향받는 조항을 짚어 준다.
18. ~~**상단 고정 바 축소**~~ ✅ **주제 바만** `sticky`로 분리 — 고정 영역 310px → **105px(15%)**, 조항 영역 **85%**.
19. ~~**문서 as-built 갱신**~~ ✅ PRD v3.2 · prd_lite v3.2 · PLAN v3.2 · DESIGN · CHECK v4.0 · CLAUDE · README.

> **최종 규모:** 협정 **24개** × 주제 **7개** = **168칸 전부 판정** · 조문 **72건** 수록.

**5차 사이클 (미발효 협정 5개 확장) 〔v4.0 · 2026-08-01〕:**

20. ~~**미발효(서명·타결) 협정 5개 수록**~~ ✅ 에콰도르 SECA · GCC FTA · 조지아 CEPA · 말레이시아 FTA · **EU DTA** — 「미발효(서명·타결)」 그룹으로 분리 표시(사용자 결정: 라벨+기준일 방식). 조지아·말레이시아는 통관·예외 장별 PDF를 포털에서 받아 폴더에 추가.
21. ~~**신규 27건 수록·검증**~~ ✅ 54칸 자동 감사 **전문 일치** · 활자 판단 2곳 OCR 확정 · 매니페스트 80파일로 갱신.

> **최종 규모 〔5차〕:** 협정 **29개**(발효 24 + 미발효 5) × 주제 **7개** = **203칸 전부 판정** · 조문 **99건** 수록.

**6차 사이클 (주제 17개 확대 — 디지털 협정 전체) 〔v5.0 · 2026-08-02〕:**

22. ~~**범위 규칙 개정(사용자 결정)**~~ ✅ "재경부 소관만" 해제 — 디지털 협정 전체로 확대, 신규 주제 10개(비차별·전자인증·핀테크·데이터 이전·설비 위치·개인정보·소스코드·소비자 보호·스팸·사이버보안).
23. ~~**290칸 신규 판정 + 105건 수록**~~ ✅ 있음 104칸(18개 협정) · 없음 확정 11개 협정(잡음 전수 검증). 미국·중미 디지털제품 조문은 **항 분리 수록**(EU 7.48 선례).
24. ~~**검증**~~ ✅ EN 조립 경계 결함 33건 자체 발견·정정(다음 조문 혼입 23 + 각주 마커 절단 10) · 자동 감사 전건 대조 · 미국 국문은 OCR 3배율 다수결로 부호 확정.

> **최종 규모 〔6차〕:** 협정 **29개** × 주제 **17개** = **493칸 전부 판정** · 조문 **204건** 수록.

**다음 사이클 후보** (착수 시 이 목록을 새로 작성): 조항 출처(`source`) **화면** 표시 · **세르비아 CEPA**(협정문 확보 시 추가) · 미발효 협정 **발효 시 편입**(위 「미발효 협정 취급 규칙」 절차) — [PLAN.md](PLAN.md) "다음 단계" 참조.

---

*작성 근거: `PRD.md`, `prd_lite.md` (작성자: 이창엽, 2026-07-22). **v2.1 (2026-07-25):** 2차 사이클(협정 24개 확대) 완료에 맞춰 ① 대상 협정 목록·데이터 현황 ② **협정 표기 규칙**(상대국명 / 정식 명칭) ③ 협정 선택 UI·배지 표기 확정 사항 ④ 개발 단위를 2차 기준으로 전면 갱신. **v3.2 (2026-07-29):** 3차 사이클 완료에 맞춰 ⑤ **주제 7개 고정**과 범위 결정 규칙 ⑥ 데이터 현황 **72건 / 168칸** ⑦ **로컬 협정문 폴더 1차 출처**·개정 대응 절차·국문 미확보 시 공란 원칙 ⑧ 내보내기(기능 4)·고정 바 규칙 ⑨ 3차 개발 단위 추가 — 근거: [PRD.md](PRD.md) v3.2 · [CHECK.md](CHECK.md) v4.0. **v4.0 (2026-08-01):** 5차 사이클 완료에 맞춰 ⑩ 대상 협정 **29개**(발효 24 + 미발효 5)와 **미발효 협정 취급 규칙** ⑪ 데이터 현황 **99건 / 203칸** ⑫ 5차 개발 단위 추가 — 근거: [PRD.md](PRD.md) v4.0 · [CHECK.md](CHECK.md) v5.0. **v5.0 (2026-08-02):** 6차 사이클(주제 17개) 완료에 맞춰 ⑬ **범위 규칙 개정**(재경부 소관 → 디지털 협정 전체, 사용자 결정) ⑭ 주제 17개·데이터 현황 **204건 / 493칸** ⑮ 6차 개발 단위 추가 — 근거: [PRD.md](PRD.md) v5.0 · [CHECK.md](CHECK.md) v6.0.*
